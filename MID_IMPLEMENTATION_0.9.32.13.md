# MID v0.9.32.13

- Regression-/CI-Fix nach v0.9.32.12.
- `test-heading-nowcast-scale-071035.mjs` schützt nun die Achse `mm/5 min` und die Balkenhöhe aus `segment.amount`.
- `test-radar-consistency-079802.mjs` erwartet `radarAmountScale()` statt des entfernten `radarRateScale()`.
- `test-cockpit-mini-precip-visibility-093211.mjs` wurde auf den aktuellen Release-Stand synchronisiert.
- Keine funktionale Rücknahme der gewünschten Radar-Nowcast-Änderung.
