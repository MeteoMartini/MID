# MID Test Report 0.9.78.56

Geprüft wurden die neue Windstufen-Präsentation, der kt-Anzeigevertrag, die probabilistische Warnlogik sowie die EPS-/Umfeldregeln. Syntaxchecks der geänderten TS/TSX-Aggregate erfolgen zusätzlich über TypeScript `transpileModule`, da die Transport-Arbeitskopie bewusst keine `node_modules` enthält.
