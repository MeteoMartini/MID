{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://mid.example/schema/native-widget-feed-v1.json",
  "title": "MID Native Widget Feed v1",
  "type": "object",
  "required": ["schema", "version", "generatedAt", "expiresAt", "location", "units", "current", "hourly", "daily", "source"],
  "properties": {
    "schema": {"const": "mid.native.widget.v1"},
    "version": {"type": "string"},
    "generatedAt": {"type": "string", "format": "date-time"},
    "expiresAt": {"type": "string", "format": "date-time"},
    "location": {"type": "object"},
    "units": {"type": "object", "properties": {"sunshineDuration": {"const": "s"}}},
    "current": {"type": "object", "properties": {"sunshineDurationSeconds": {"type": ["number", "null"], "minimum": 0}}},
    "hourly": {"type": "array", "maxItems": 12, "items": {"type": "object", "properties": {"sunshineDurationSeconds": {"type": ["number", "null"], "minimum": 0, "maximum": 3600}}}},
    "daily": {"type": "array", "maxItems": 5, "items": {"type": "object", "properties": {"sunshineDurationSeconds": {"type": ["number", "null"], "minimum": 0, "maximum": 86400}}}},
    "source": {"type": "object"}
  }
}
