import {readFile} from 'node:fs/promises';
const [cockpit,styles,pkg,baseline]=await Promise.all([readFile(new URL('../src/ForecastCockpit.tsx',import.meta.url),'utf8'),readFile(new URL('../src/styles.css',import.meta.url),'utf8'),readFile(new URL('../package.json',import.meta.url),'utf8'),readFile(new URL('../MID_BASELINE.json',import.meta.url),'utf8')]);
const failures=[];const need=(l,t,x)=>{if(!t.includes(x))failures.push(`${l}: ${x}`)};const reject=(l,t,x)=>{if(t.includes(x))failures.push(`${l} sollte fehlen: ${x}`)};
for(const token of ['24-h-Wetterprofil','function shortTermImpact(point:ShortTermForecastPoint)','function shortTermThermalFeel(point:ShortTermForecastPoint)','function shortTermPressureTrend(','function shortTermCalmWindow(','chartWidth=Math.max(980,chartViewportWidth)','className="cockpit-meteogram-pro__svg"','className="temperature-line"','className="apparent-line"','className="dewpoint-line"','className="wind-line"','className="gust-line"','className="cloud-band low"','className={`impact-band','className="cockpit-weather-profile__signals"','className="cockpit-meteogram-pro__datafield"','Thermisches Empfinden'])need('24-h-Wetterprofil',cockpit,token);
for(const token of ['.cockpit-weather-profile__signals{','.cockpit-weather-profile .dewpoint-line{','.cockpit-weather-profile .wind-line{','.cockpit-weather-profile .gust-line{','.cockpit-weather-profile .cloud-band{','.cockpit-weather-profile .impact-band{'])need('Wetterprofil-CSS',styles,token);
reject('Horizontales Profil-Scrollen',styles,'.cockpit-meteogram-pro__stage{width:100%;max-width:100%;overflow-x:auto;');reject('Alter 1h/3h-Umschalter',cockpit,'aria-label="Auflösung der Kurzfristvorhersage"');need('Package-Test',pkg,'test:cockpit-meteogram-pro');need('Baseline-Test',baseline,'scripts/test-cockpit-meteogram-pro-09180.mjs');const packageVersion=JSON.parse(pkg).version,baselineVersion=JSON.parse(baseline).releaseVersion;if(packageVersion!==baselineVersion)failures.push(`Versionen nicht synchron: ${packageVersion}/${baselineVersion}`);if(failures.length){console.error('24-h-Wetterprofil fehlgeschlagen:\n- '+failures.join('\n- '));process.exit(1)}console.log('Innovatives 24-h-Wetterprofil mit thermischem Empfinden, Niederschlag, Wind, Wolken und getrennten Wetter-Hazards geprüft.');