import {readFile} from 'node:fs/promises';const [radar,cockpit,worker,styles,baseline]=await Promise.all([readFile(new URL('../src/DwdPrecipitationTypeRadar.tsx',import.meta.url),'utf8'),readFile(new URL('../src/ForecastCockpit.tsx',import.meta.url),'utf8'),readFile(new URL('../worker/metar-proxy.js',import.meta.url),'utf8'),readFile(new URL('../src/styles.css',import.meta.url),'utf8'),readFile(new URL('../MID_BASELINE.json',import.meta.url),'utf8')]);const failures=[];const need=(l,t,x)=>{if(!t.includes(x))failures.push(`${l}: ${x}`)};for(const token of ['amtliches Kombinationsprodukt','Originalbild · zoombar','dwd-precip-type-radar__original-image'])need('DWD Original',radar,token);for(const token of ['dwdPrecipitationTypeImageResponse','x-mid-radar-at','x-mid-satellite-at'])need('Quellenzeitbindung',worker,token);need('CSS',styles,'.dwd-precip-type-radar__original-viewport{');const listIndex=cockpit.indexOf('className="cockpit-hourly-preview-shell"'),insightIndex=cockpit.indexOf('className="cockpit-short-insight-grid premium"');if(listIndex<0||insightIndex<0||insightIndex<listIndex)failures.push('Tageseckdaten stehen nicht unterhalb der 24-h-Liste.');if(!baseline.includes('scripts/test-mid-09261-dwd-original-order.mjs'))failures.push('Baseline-Test fehlt.');if(failures.length){console.error('DWD-Original-/Tageseckdatenprüfung fehlgeschlagen:\n- '+failures.join('\n- '));process.exit(1)}console.log('Zoombares DWD-Originalprodukt und Tageseckdaten unterhalb der 24-h-Liste geprüft.');