import {readFile,writeFile,mkdtemp,rm} from 'node:fs/promises';
import {tmpdir} from 'node:os';
import {join} from 'node:path';
import {pathToFileURL} from 'node:url';
import {createRequire} from 'node:module';
const require=createRequire(import.meta.url);let ts;try{ts=require('typescript')}catch{ts=require('/opt/nvm/versions/node/v22.16.0/lib/node_modules/typescript')}
const [water,panel,pkg,baseline]=await Promise.all([readFile(new URL('../src/WaterSportsPanel.tsx',import.meta.url),'utf8'),readFile(new URL('../src/EnsemblePanel.tsx',import.meta.url),'utf8'),readFile(new URL('../package.json',import.meta.url),'utf8'),readFile(new URL('../MID_BASELINE.json',import.meta.url),'utf8')]);
const failures=[];const need=(area,text,token)=>{if(!text.includes(token))failures.push(`${area}: fehlt ${token}`)};
for(const token of ['<b>Gezeiten</b>','function shiftLocalIsoMinutes(','function refinedTideEvent(','refinedTideEvent(series,candidate.index,candidate.kind,smooth)'])need('Gezeiten',water,token);
if(water.includes('<b>Gezeiten / Wendepunkte</b>'))failures.push('Alte Beschriftung „Gezeiten / Wendepunkte“ ist noch vorhanden.');
for(const token of ['hazardY=Math.max(plotTop+8,cellY-10)','hazards.slice(0,4)','maxWidth={cellWidth}','boxWidth={cellWidth} boxHeight={cellHeight}','scale=Math.max(.2,Math.min(1'])need('Ensemble-Overlay',panel,token);
const start=water.indexOf('type TideEvent='),end=water.indexOf('function tideAnalysis(');
if(start<0||end<0)failures.push('Gezeiten-Hilfslogik konnte nicht isoliert werden.');else{const isolated=`type MarineForecast={hourly:Record<string,(number|string|null)[]>;minutely_15?:Record<string,(number|string|null)[]>};\n${water.slice(start,end)}\nexport {tideEventsForDate};\n`,dir=await mkdtemp(join(tmpdir(),'mid-tides-082617-'));try{const output=ts.transpileModule(isolated,{compilerOptions:{target:ts.ScriptTarget.ES2022,module:ts.ModuleKind.ES2022},fileName:'tides.ts'}).outputText,file=join(dir,'tides.mjs');await writeFile(file,output);const {tideEventsForDate}=await import(`${pathToFileURL(file).href}?v=${Date.now()}`),date='2026-08-06',startMs=Date.parse('2026-08-05T18:00:00Z'),endMs=Date.parse('2026-08-07T06:00:00Z'),times=[],levels=[];for(let ms=startMs;ms<=endMs;ms+=15*60000){const hours=(ms-Date.parse('2026-08-06T00:00:00Z'))/3600000;times.push(new Date(ms).toISOString().slice(0,16));levels.push(.12+.09*Math.cos((hours-3.13)*2*Math.PI/12.42));}const data={hourly:{time:[],sea_level_height_msl:[]},minutely_15:{time:times,sea_level_height_msl:levels}},events=tideEventsForDate(data,date);if(events.length<3)failures.push(`Nur ${events.length} Gezeiten erkannt.`);if(!events.some(event=>{const minute=Number(event.time.slice(14,16));return minute%15!==0}))failures.push('Wendepunktzeiten bleiben trotz Zwischenwertberechnung auf Viertelstunden gerundet.');}finally{await rm(dir,{recursive:true,force:true})}}
const source=ts.createSourceFile('EnsemblePanel.tsx',panel,ts.ScriptTarget.ESNext,true,ts.ScriptKind.TSX);if(source.parseDiagnostics.length)failures.push(...source.parseDiagnostics.map(item=>`Parser: ${item.messageText}`));
const p=JSON.parse(pkg),b=JSON.parse(baseline);if(!p.scripts?.['test:water-tide-minute-hazard-layout'])failures.push('Package-Testskript fehlt.');if(!b.regressionTests?.includes('scripts/test-water-tide-minute-hazard-layout-082617.mjs'))failures.push('Baseline-Regression fehlt.');
if(failures.length){console.error('Gezeiten-Minuten/Hazard-Layout fehlgeschlagen:\n- '+failures.join('\n- '));process.exit(1)}
console.log('Gezeiten minutengenau interpoliert; Hazards oberhalb der Wetterfelder nebeneinander und Niederschlagssymbole innerhalb der Felder geprüft.');
