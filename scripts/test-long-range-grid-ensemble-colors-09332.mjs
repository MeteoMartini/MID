import {readFile} from 'node:fs/promises';
const [seasonal,panel,styles,pkg,baseline]=await Promise.all([
 readFile(new URL('../src/seasonalForecast.ts',import.meta.url),'utf8'),readFile(new URL('../src/LongRangePanel.tsx',import.meta.url),'utf8'),readFile(new URL('../src/styles.css',import.meta.url),'utf8'),readFile(new URL('../package.json',import.meta.url),'utf8'),readFile(new URL('../MID_BASELINE.json',import.meta.url),'utf8')]);
const failures=[],need=(scope,text,token)=>{if(!text.includes(token))failures.push(`${scope}: fehlt ${token}`)};
for(const token of ["cell_selection:'nearest'",'temperatureAnomalyLow','temperatureAnomalyQ25','temperatureAnomalyQ75','temperatureAnomalyHigh','precipitationAnomalyQ25','precipitationAnomalyQ75','ensembleMembers','nearestC3sGridPoint','family:string','uncertaintyLabel','NOAA CFSv2','cfsv2-seasonal-point'])need('seasonalForecast',seasonal,token);
for(const token of ['temperatureAnomalyColor','precipitationAnomalyColor','Poor-Man’s-Ensemble','long-range-family-chips','außen P10–P90 · innen P25–P75 bis Hälfte','eine Stimme je numerisch verfügbarem unabhängigen Modellsystem','quantile(centers,.1)','quantile(centers,.25)','quantile(centers,.75)','quantile(centers,.9)','pointX(index:number,count:number,left:number,width:number,padding=34)'])need('LongRangePanel',panel,token);
for(const token of ['.long-range-chart .anomaly-plume.outer{opacity:.62}','.long-range-chart .anomaly-plume.inner{opacity:.78','.long-range-scale.temperature i:first-of-type{','.long-range-scale.precipitation i:first-of-type{','.long-range-family-chips{'])need('styles',styles,token);
const pv=JSON.parse(pkg).version,bv=JSON.parse(baseline).releaseVersion;if(pv!==bv)failures.push(`Versionen nicht synchron: ${pv}/${bv}`);const parts=value=>String(value).split('.').map(part=>Number(part)||0),atLeast=(value,minimum)=>{const a=parts(value),b=parts(minimum),length=Math.max(a.length,b.length);for(let index=0;index<length;index++){const av=a[index]??0,bv=b[index]??0;if(av>bv)return true;if(av<bv)return false}return true};if(!atLeast(pv,'0.9.33.2'))failures.push(`Version ${pv} liegt vor dem Langfrist-Vertrag 0.9.33.2`);
if(failures.length){console.error('MID Langfrist-Gitter/Ensemble/Farbprüfung fehlgeschlagen:\n- '+failures.join('\n- '));process.exit(1)}console.log(`MID ${pv}: Langfristmodelle mit P10–P90 und P25–P75 sowie divergierenden Anomaliefarben geprüft.`);
