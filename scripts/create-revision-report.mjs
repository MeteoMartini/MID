import {readFile,writeFile,mkdir} from 'node:fs/promises';const pkg=JSON.parse(await readFile(new URL('../package.json',import.meta.url),'utf8'));await mkdir('artifacts',{recursive:true});await writeFile('artifacts/code-revision-report.json',JSON.stringify({schema:1,version:pkg.version,generatedAt:new Date().toISOString()},null,2)+'\n');console.log(`Revisionsbericht für MID v${pkg.version} erstellt.`);
