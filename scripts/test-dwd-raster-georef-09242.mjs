import {readFile} from 'node:fs/promises';
const [radar,worker,styles,pkg,baseline]=await Promise.all([readFile(new URL('../src/DwdPrecipitationTypeRadar.tsx',import.meta.url),'utf8'),readFile(new URL('../worker/metar-proxy.js',import.meta.url),'utf8'),readFile(new URL('../src/styles.css',import.meta.url),'utf8'),readFile(new URL('../package.json',import.meta.url),'utf8'),readFile(new URL('../MID_BASELINE.json',import.meta.url),'utf8')]);
const failures=[];const need=(l,t,x)=>{if(!t.includes(x))failures.push(`${l}: ${x}`)};const reject=(l,t,x)=>{if(t.includes(x))failures.push(`${l} sollte fehlen: ${x}`)};
for(const token of ['Originalbild · zoombar','Zoomen verändert ausschließlich die Ansicht','Bildpunkt-Auswertung bleibt an den Originalpixel gebunden','DWD_IMAGE_GEO_BILINEAR','dwdPrecipitationTypeImagePosition','geoFromImagePoint','Der Ortsmarker zentriert nur den Bildausschnitt'])need('Ansichtsgebundene Ortskalibrierung',radar,token);for(const token of ['RasterPolynomial','DWD_SOURCE_RASTER_GRID','Marker position={[latitude,longitude]}','DwdPrecipitationMap','ImageOverlay'])reject('Kein georeferenziertes Ersatzraster',radar,token);need('Baseline',baseline,'scripts/test-dwd-raster-georef-09242.mjs');
const pv=JSON.parse(pkg).version,bv=JSON.parse(baseline).releaseVersion;if(pv!==bv)failures.push(`Versionen nicht synchron: ${pv}/${bv}`);if(failures.length){console.error('DWD-Originalproduktprüfung fehlgeschlagen:\n- '+failures.join('\n- '));process.exit(1)}console.log('Das DWD-Originalbild darf ortszentriert beschnitten werden, bleibt aber ein unverändertes Bild statt eines Kartenlayers.');
