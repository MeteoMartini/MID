import {readFile} from 'node:fs/promises';
const [radar,map,worker,styles,baseline]=await Promise.all([readFile(new URL('../src/DwdPrecipitationTypeRadar.tsx',import.meta.url),'utf8'),readFile(new URL('../src/DwdPrecipitationMap.tsx',import.meta.url),'utf8'),readFile(new URL('../worker/metar-proxy.js',import.meta.url),'utf8'),readFile(new URL('../src/styles.css',import.meta.url),'utf8'),readFile(new URL('../MID_BASELINE.json',import.meta.url),'utf8')]);
const failures=[];const need=(label,text,token)=>{if(!text.includes(token))failures.push(`${label}: ${token}`)};const reject=(label,text,token)=>{if(text.includes(token))failures.push(`${label} sollte fehlen: ${token}`)};
for(const token of ['<DwdPrecipitationMap','MID-Komposit · georeferenziert','freshVerifiedHymec','radarLatestOnly','markerVisible={markerVisible}','Eine georeferenzierte Gesamtkarte'])need('Einzelkarte',radar,token);for(const token of ['DwdLocationLocator','dwd-precip-type-radar__source-image','DWD-Originalprodukt wird geladen'])reject('Doppelkarte',radar,token);for(const token of ['BKG_BASEMAP_COLOR_URL','de_basemapde_web_raster_colordem','<HymecNgOverlay','canShowRadar?<WMSTileLayer','opacity={.78} zIndex={390}','Marker position={[latitude,longitude]}','<Tooltip permanent'])need('Kartenkomposition',map,token);for(const token of ["layer:'mtg_fd:rgb_geocolour'",'DWD_REFERENCE_SATELLITE_CANDIDATES','result.dwdReferenceSatelliteProduct=satelliteProduct','dwdTimesFromCapabilities(dwdXml,timingLayer)','result.dwdRadarLatestOnly=result.dwdRadarLayer===alias||!observed.length','cf:{cacheTtl:180,cacheEverything:true}'])need('Quellenaktualität',worker,token);need('CSS',styles,'.dwd-precip-type-radar__source-summary{');if(!baseline.includes('scripts/test-mid-single-precip-composite-09290.mjs'))failures.push('Baseline-Test fehlt');if(failures.length){console.error('Einzelkomposit-Prüfung fehlgeschlagen:\n- '+failures.join('\n- '));process.exit(1)}console.log('Eine einzige farbige, georeferenzierte DWD-nahe Satellit-/Radar-/Niederschlagsartkarte ohne falsche Ersatzklassen erfolgreich geprüft.');
