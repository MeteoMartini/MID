import {readFile} from 'node:fs/promises';const [radar,worker,styles,baseline]=await Promise.all([readFile(new URL('../src/DwdPrecipitationTypeRadar.tsx',import.meta.url),'utf8'),readFile(new URL('../worker/metar-proxy.js',import.meta.url),'utf8'),readFile(new URL('../src/styles.css',import.meta.url),'utf8'),readFile(new URL('../MID_BASELINE.json',import.meta.url),'utf8')]);const failures=[];const need=(l,t,x)=>{if(!t.includes(x))failures.push(`${l}: ${x}`)};const reject=(l,t,x)=>{if(t.includes(x))failures.push(`${l} sollte fehlen: ${x}`)};for(const token of ['dwd-precip-type-radar__original-image','Originalbild · zoombar',"buildWorkerUrl(base,'dwd-precipitation-type-image'",'dwd-precip-type-radar__zoom-toolbar'])need('Einzelprodukt',radar,token);for(const token of ['DwdPrecipitationMap','MID-Komposit · georeferenziert','HymecNgOverlay','RainViewer'])reject('Keine Rekonstruktion',radar,token);for(const token of ['DWD_PRECIPITATION_TYPE_IMAGE','dwdPrecipitationTypeSourceTimes'])need('Originalquelle',worker,token);need('CSS',styles,'.dwd-precip-type-radar__original-image{');if(!baseline.includes('scripts/test-mid-single-precip-composite-09290.mjs'))failures.push('Baseline-Test fehlt');if(failures.length){console.error('DWD-Einzelproduktprüfung fehlgeschlagen:\n- '+failures.join('\n- '));process.exit(1)}console.log('Eine einzige zoombare amtliche DWD-Kombinationsgrafik ohne nachgebautes Komposit geprüft.');