import assert from 'node:assert/strict';
import {readFileSync} from 'node:fs';
const worker=readFileSync(new URL('../worker-src/20-composite-models.js',import.meta.url),'utf8');
const radar=readFileSync(new URL('../src/RadarPanel.tsx',import.meta.url),'utf8');
const composite=readFileSync(new URL('../src/CompositeData.ts',import.meta.url),'utf8');
assert.ok(worker.includes('function synopticThetaEFrontalZones('),'Großräumige θe-Frontzonendiagnose fehlt.');
assert.ok(worker.includes("basis:'θe 850 hPa'")&&worker.includes("type:'frontal-zone'"),'Frontzonen müssen als modell-diagnostische θe-850-Struktur gekennzeichnet sein.');
assert.ok(worker.includes('gradientKPer100Km')&&worker.includes('synopticQuantile'),'Frontzonen brauchen Gradientenschwelle und robuste Quantilselektion.');
assert.ok(composite.includes("type:'frontal-zone'")&&composite.includes('frontalZones?:ModelFrontalZone[]'),'Frontend-Typen müssen die regionalen Frontalzonen transportieren.');
assert.ok(radar.includes('function RegionalFrontalZones(')&&radar.includes("type==='none'?'#9a7b38':COMPOSITE_FRONT_COLORS[type]")&&radar.includes("typedFront=type==='cold'||type==='warm'||type==='occlusion'")&&radar.includes('Math.floor(prepared.length/165)')&&radar.includes("className:`mid-synoptic-front foreground frontal-zone ${type}`")&&radar.includes('candidate.score>=68')&&radar.includes("candidate.confidence!=='low'")&&radar.includes('zone.score>=58'),'Regionale Frontalzonen müssen sichtbar, geglättet und qualitätsgefiltert sein; belastbare Typen nutzen die synoptischen Frontfarben.');
assert.ok(!radar.includes('function SynopticFrontCanvasFallback(')&&!radar.includes('function CompositeFronts(')&&!radar.includes('const MemoCompositeFronts='),'Veraltete parallele Front-Fallbacks dürfen nicht als toter oder potenziell doppelter Renderpfad im Build verbleiben.');
assert.ok(radar.includes('regionalFrontCount')&&radar.includes('localFrontCount')&&radar.includes('frontCount=Math.min(2,regionalFrontCount)+(regionalFrontCount?0:localFrontCount)'),'Status/Layerinfo muss regionale Frontzonen priorisieren und lokale Typkandidaten nur ohne regionale Doppelzählung verwenden.');
console.log('Synoptik: regionale θe-850-Frontalzonen und lokale Fronttypen geprüft.');
