import {readFile} from 'node:fs/promises';const [radar,worker,pkg,baseline]=await Promise.all([readFile(new URL('../src/DwdPrecipitationTypeRadar.tsx',import.meta.url),'utf8'),readFile(new URL('../worker/metar-proxy.js',import.meta.url),'utf8'),readFile(new URL('../package.json',import.meta.url),'utf8'),readFile(new URL('../MID_BASELINE.json',import.meta.url),'utf8')]);const failures=[];const need=(l,t,x)=>{if(!t.includes(x))failures.push(`${l}: ${x}`)};for(const token of ['type PointInspection={loading:boolean;','type RadarPointInfo=','fetchWorkerJson<RadarPointInfo>','dwd-precipitation-type-info','formatDwdSourceTimestamp(meta?.radarAt)','formatDwdSourceTimestamp(meta?.satelliteAt)'])need('Frontend',radar,token);for(const token of ['async function dwdPrecipitationTypeImageResponse()','async function dwdPrecipitationTypeInfo(request)',"mode==='dwd-precipitation-type-image'", "mode==='dwd-precipitation-type-info'",'x-mid-radar-at','x-mid-satellite-at'])need('Worker',worker,token);const pv=JSON.parse(pkg).version,bv=JSON.parse(baseline).releaseVersion;if(pv!==bv)failures.push(`Versionen nicht synchron: ${pv}/${bv}`);if(failures.length){console.error('DWD Originalprodukt-/WorkerPayload Buildfix fehlgeschlagen:\n- '+failures.join('\n- '));process.exit(1)}console.log('DWD-Originalbild- und Pixelanalyse-Workerpayloads geprüft.');