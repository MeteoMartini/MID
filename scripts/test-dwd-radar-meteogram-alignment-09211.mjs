import {readFile} from 'node:fs/promises';const [radar,cockpit,styles,pkg,baseline]=await Promise.all([readFile(new URL('../src/DwdPrecipitationTypeRadar.tsx',import.meta.url),'utf8'),readFile(new URL('../src/ForecastCockpit.tsx',import.meta.url),'utf8'),readFile(new URL('../src/styles.css',import.meta.url),'utf8'),readFile(new URL('../package.json',import.meta.url),'utf8'),readFile(new URL('../MID_BASELINE.json',import.meta.url),'utf8')]);const failures=[];const need=(l,t,x)=>{if(!t.includes(x))failures.push(`${l}: ${x}`)};for(const token of ['dwd-precip-type-radar__original-viewport','dwd-precip-type-radar__original-image','dwd-precip-type-radar__zoom-toolbar','Originalbild · zoombar'])need('DWD-Originalbild',radar,token);for(const token of ['24-h-Wetterprofil','chartWidth=Math.max(980,chartViewportWidth)','className="dewpoint-line"','className="wind-line"','className="cloud-band low"','className={`impact-band'])need('Wetterprofil',cockpit,token);for(const token of ['.dwd-precip-type-radar__original-viewport{','.cockpit-weather-profile__signals{'])need('CSS',styles,token);const pv=JSON.parse(pkg).version,bv=JSON.parse(baseline).releaseVersion;if(pv!==bv)failures.push(`Versionen nicht synchron: ${pv}/${bv}`);if(failures.length){console.error('DWD-Originalbild-/Wetterprofil-Ausrichtung fehlgeschlagen:\n- '+failures.join('\n- '));process.exit(1)}console.log('Zoombares DWD-Originalprodukt und neues 24-h-Wetterprofil gemeinsam geprüft.');