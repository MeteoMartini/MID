import {readFile} from 'node:fs/promises';
const [radar,map,hymec,overlay,worker,baseline]=await Promise.all([readFile(new URL('../src/DwdPrecipitationTypeRadar.tsx',import.meta.url),'utf8'),readFile(new URL('../src/DwdPrecipitationMap.tsx',import.meta.url),'utf8'),readFile(new URL('../src/HymecNgSource.ts',import.meta.url),'utf8'),readFile(new URL('../src/HymecNgOverlay.tsx',import.meta.url),'utf8'),readFile(new URL('../worker/metar-proxy.js',import.meta.url),'utf8'),readFile(new URL('../MID_BASELINE.json',import.meta.url),'utf8')]);
const failures=[];const need=(label,text,token)=>{if(!text.includes(token))failures.push(`${label}: ${token}`)};const reject=(label,text,token)=>{if(text.includes(token))failures.push(`${label} sollte fehlen: ${token}`)};
for(const token of ['latitude=Number(location.latitude)','longitude=Number(location.longitude)','coordinateLabel(latitude,longitude)','<DwdPrecipitationMap','markerVisible={markerVisible}'])need('Location-Durchleitung',radar,token);for(const token of ['center={[latitude,longitude]} zoom={8}','map.setView([latitude,longitude]','Marker position={[latitude,longitude]}','PointPicker onPoint={onPoint}','<Tooltip permanent'])need('Leaflet-WGS84-Gesamtkarte',map,token);for(const token of ['projectWgs84(latitude,longitude,raster.projection)','inverseProjectedPoint','hymecNgSourceIndex'])need('Hymec native Georeferenzierung',hymec,token);need('Hymec Overlay',overlay,'sourceX=Math.floor((projectedX-raster.minX)/raster.xScale)');need('Hymec Overlay',overlay,'sourceY=Math.floor((raster.maxY-projectedY)/raster.yScale)');for(const token of ['DWD_HYMECNG_ROOTS','dwd-hymecng-meta','dwd-hymecng-file'])need('Worker',worker,token);for(const token of ['DwdLocationLocator','dwd-precip-type-radar__source-image','dwdPrecipitationTypeImagePosition','radarCropWindow','RasterPolynomial','DWD_SOURCE_RASTER_GRID'])reject('Keine getrennte/PNG-Verortung',radar,token);for(const token of ['50.78362','7.059056','Mondorf','Wiesbaden'])reject('Keine ortsspezifische Kalibrierung',radar,token);if(!baseline.includes('scripts/test-dwd-native-georef-09250.mjs'))failures.push('Georeferenzierungsregression fehlt in der Baseline.');if(failures.length){console.error('DWD WGS84-Gesamtproduktprüfung fehlgeschlagen:\n- '+failures.join('\n- '));process.exit(1)}console.log('Exakte Standortmarkierung und native HymecNG-Georeferenzierung erfolgen innerhalb derselben Gesamtkarte.');
