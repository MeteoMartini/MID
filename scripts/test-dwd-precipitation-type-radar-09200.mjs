import {readFile} from 'node:fs/promises';
const [radar,worker,styles,pkg,baseline]=await Promise.all([readFile(new URL('../src/DwdPrecipitationTypeRadar.tsx',import.meta.url),'utf8'),readFile(new URL('../worker/metar-proxy.js',import.meta.url),'utf8'),readFile(new URL('../src/styles.css',import.meta.url),'utf8'),readFile(new URL('../package.json',import.meta.url),'utf8'),readFile(new URL('../MID_BASELINE.json',import.meta.url),'utf8')]);
const failures=[];const need=(l,t,x)=>{if(!t.includes(x))failures.push(`${l}: ${x}`)};const reject=(l,t,x)=>{if(t.includes(x))failures.push(`${l} sollte fehlen: ${x}`)};
for(const token of ["DWD_PRODUCT_PAGE='https://www.dwd.de/DE/leistungen/wolken_niederschlagsart/wolken_niederschlagsart.html'",'Wolken + Niederschlagsart',"buildWorkerUrl(base,'dwd-precipitation-type-image'",'x-mid-radar-at','x-mid-satellite-at','dwd-precip-type-radar__original-viewport','dwd-precip-type-radar__original-image','dwd-precip-type-radar__zoom-toolbar','changeZoom','resetZoom','dwd-precipitation-type-info','Antippen zur Auswertung'])need('Originalbild',radar,token);for(const token of ['DwdPrecipitationMap','loadCompositeTimes','loadHymecNgMetadata','MID-Komposit · georeferenziert'])reject('Keine eigene Rekonstruktion',radar,token);for(const token of ['async function dwdPrecipitationTypeImageResponse()','x-mid-radar-at','x-mid-satellite-at',"mode==='dwd-precipitation-type-image'", "mode==='dwd-precipitation-type-info'"])need('Worker',worker,token);for(const token of ['.dwd-precip-type-radar__original-viewport{','.dwd-precip-type-radar__original-image{','.dwd-precip-type-radar__zoom-toolbar{'])need('CSS',styles,token);need('Baseline',baseline,'scripts/test-dwd-precipitation-type-radar-09200.mjs');
const pv=JSON.parse(pkg).version,bv=JSON.parse(baseline).releaseVersion;if(pv!==bv)failures.push(`Versionen nicht synchron: ${pv}/${bv}`);if(failures.length){console.error('DWD-Originalproduktprüfung fehlgeschlagen:\n- '+failures.join('\n- '));process.exit(1)}console.log('Amtliches DWD-Originalbild mit Zoom, Originalzeitständen und Pixelanalyse geprüft.');