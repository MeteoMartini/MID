import {mkdir,writeFile} from 'node:fs/promises';const failures=[],checks=[];const query=new URLSearchParams({latitude:'50.815',longitude:'7.037',timezone:'auto',forecast_days:'3',models:'best_match',wind_speed_unit:'kn',hourly:'temperature_2m,precipitation_probability,weather_code,wind_speed_10m,wind_direction_10m',daily:'weather_code,temperature_2m_max,temperature_2m_min,sunrise,sunset,moonrise,moonset,moon_phase'});try{const r=await fetch(`https://api.open-meteo.com/v1/forecast?${query}`),p=await r.json();checks.push({status:r.status});if(!r.ok||!Array.isArray(p?.hourly?.time))failures.push('Open-Meteo Forecastvertrag ungültig')}catch(error){failures.push(String(error))}await mkdir('artifacts',{recursive:true});await writeFile('artifacts/api-contracts.json',JSON.stringify({schema:1,ok:!failures.length,failures,checks},null,2)+'\n');if(failures.length){console.error(failures.join('\n'));process.exit(1)}console.log('API-Vertragsprüfung bestanden.');
