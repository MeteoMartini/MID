import {readFile} from 'node:fs/promises';
const [modules,app,seasonal,panel,styles,pkg,baseline]=await Promise.all([readFile(new URL('../src/dashboardModules.ts',import.meta.url),'utf8'),readFile(new URL('../src/App.tsx',import.meta.url),'utf8'),readFile(new URL('../src/seasonalForecast.ts',import.meta.url),'utf8'),readFile(new URL('../src/LongRangePanel.tsx',import.meta.url),'utf8'),readFile(new URL('../src/styles.css',import.meta.url),'utf8'),readFile(new URL('../package.json',import.meta.url),'utf8'),readFile(new URL('../MID_BASELINE.json',import.meta.url),'utf8')]);
const failures=[],need=(scope,text,token)=>{if(!text.includes(token))failures.push(`${scope}: fehlt ${token}`)};
for(const token of ["|'long-range'","{id:'long-range',label:'Trend 14d+'",'Witterungstrend Tag 15–46 und saisonaler Multi-Modell-Vergleich'])need('dashboardModules',modules,token);
for(const token of ["lazy(()=>import('./LongRangePanel'))","case'long-range'",'title="Trend 14d+"','<MemoLazyLongRange location={loc!}'])need('App',app,token);
for(const token of ['https://seasonal-api.open-meteo.com/v1/seasonal','temperature_2m_anomaly','precipitation_anomaly','ECMWF','NOAA CFSv2','Met Office','Météo-France','DWD','CMCC','NCEP','JMA','ECCC','BOM',"forecast_days:'217'"])need('seasonalForecast',seasonal,token);
for(const token of ['Witterungs- & Saisontrends','WITTERUNGSTREND','SAISONVORHERSAGEN','Temperatur','Niederschlag','Poor-Man’s-Ensemble','unabhängiger Multi-Modell-Vergleich'])need('LongRangePanel',panel,token);
for(const token of ['.long-range-panel{','.long-range-grid{','.long-range-chart{','.long-range-model-strip{'])need('styles',styles,token);
const pv=JSON.parse(pkg).version,bv=JSON.parse(baseline).releaseVersion;if(pv!==bv)failures.push(`Versionen nicht synchron: ${pv}/${bv}`);const parts=value=>String(value).split('.').map(part=>Number(part)||0),atLeast=(value,minimum)=>{const a=parts(value),b=parts(minimum),length=Math.max(a.length,b.length);for(let index=0;index<length;index++){const av=a[index]??0,bv=b[index]??0;if(av>bv)return true;if(av<bv)return false}return true};if(!atLeast(pv,'0.9.33.2'))failures.push(`Version ${pv} liegt vor dem Langfrist-Vertrag 0.9.33.2`);
if(failures.length){console.error('MID Langfristprüfung fehlgeschlagen:\n- '+failures.join('\n- '));process.exit(1)}console.log(`MID ${pv}: Trend-14d+-/Langfrist-Sektion, ECMWF + NOAA CFSv2 und gemeinsamen Einzelmodellvergleich geprüft.`);
