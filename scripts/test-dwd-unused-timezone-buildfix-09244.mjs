import {readFile} from 'node:fs/promises';
const [radar,baseline]=await Promise.all([readFile(new URL('../src/DwdPrecipitationTypeRadar.tsx',import.meta.url),'utf8'),readFile(new URL('../MID_BASELINE.json',import.meta.url),'utf8')]);
const failures=[];if(radar.includes("timezone=location.timezone||'Europe/Berlin'"))failures.push('Ungenutzte lokale timezone-Variable ist wieder vorhanden.');if(!radar.includes('formatSourceTimestamp(radarAt)'))failures.push('Niederschlagszeitstempel ist nicht an die tatsächlich gewählte Niederschlagsquelle gebunden.');if(!radar.includes('formatSourceTimestamp(satelliteAt)'))failures.push('Satellitenzeitstempel ist nicht an das tatsächlich gewählte Satellitenprodukt gebunden.');if(!radar.includes('latestRainViewerTime'))failures.push('RainViewer-Fallbackzeit fehlt.');if(!baseline.includes('scripts/test-dwd-unused-timezone-buildfix-09244.mjs'))failures.push('Baseline-Test fehlt.');if(failures.length){console.error('DWD-Zeitstempel-Buildfix fehlgeschlagen:\n- '+failures.join('\n- '));process.exit(1)}console.log('DWD-Zeitstempel-Buildfix geprüft: keine ungenutzte timezone-Variable und quellengebundene UTC-Zeitstände.');
