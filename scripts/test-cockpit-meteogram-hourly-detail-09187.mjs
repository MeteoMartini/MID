import {readFile} from 'node:fs/promises';
const [cockpit,pkg,baseline]=await Promise.all([readFile(new URL('../src/ForecastCockpit.tsx',import.meta.url),'utf8'),readFile(new URL('../package.json',import.meta.url),'utf8'),readFile(new URL('../MID_BASELINE.json',import.meta.url),'utf8')]);const failures=[];const need=(l,t,x)=>{if(!t.includes(x))failures.push(`${l}: ${x}`)};
for(const token of ['function shortTermVisibilityLabel(visibility:number)','function shortTermPrecipitationDetail(','function shortTermWindDetail(','Math.round(selectedPoint.temperature)','Math.round(selectedPoint.apparent)','Math.round(selectedPoint.dewPoint)','Math.round(selectedPoint.humidity)','Niederschlag</dt><dd>{shortTermPrecipitationDetail(selectedPoint)}</dd>','Gewitterrisiko','Wind</dt><dd>{shortTermWindDetail(selectedPoint,unit)}</dd>','shortTermVisibilityLabel(selectedPoint.visibility)','Wolken H/M/L','Thermisches Empfinden','Wetter-Hazards','selectedThermal.label','selectedImpact.label','Δ zur Lufttemperatur'])need('Einzeldaten',cockpit,token);const pv=JSON.parse(pkg).version,bv=JSON.parse(baseline).releaseVersion;if(pv!==bv)failures.push(`Versionen nicht synchron: ${pv}/${bv}`);if(failures.length){console.error('24-h-Wetterprofil-Einzeldaten fehlgeschlagen:\n- '+failures.join('\n- '));process.exit(1)}console.log('Erweiterte Einzeldaten des 24-h-Wetterprofils geprüft.');