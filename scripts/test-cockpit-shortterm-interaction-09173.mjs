import {readFile} from 'node:fs/promises';
const [cockpit,styles,pkg,baseline]=await Promise.all([readFile(new URL('../src/ForecastCockpit.tsx',import.meta.url),'utf8'),readFile(new URL('../src/styles.css',import.meta.url),'utf8'),readFile(new URL('../package.json',import.meta.url),'utf8'),readFile(new URL('../MID_BASELINE.json',import.meta.url),'utf8')]);const failures=[];const need=(l,t,x)=>{if(!t.includes(x))failures.push(`${l}: ${x}`)};
for(const token of ["[selectedId,setSelectedId]=useState('')",'meteogramId=useId().replace(/:/g,\'\')','className="cockpit-meteogram-pro__canvas"','className="cockpit-meteogram-pro__hitlayer"','onClick={()=>activatePoint(item.point)}','temperatureGradientId=`mid-profile-temp-${meteogramId}`','stroke={`url(#${temperatureGradientId})`}','className="wind-line"','className="gust-line"','selectedVisualPoint?.point.id===item.point.id?\'active\':\'\''])need('Interaktion',cockpit,token);for(const token of ['.cockpit-meteogram-pro__hitlayer{position:absolute;inset:0;','.cockpit-weather-profile .wind-line{','.cockpit-weather-profile .gust-line{'])need('CSS',styles,token);const packageVersion=JSON.parse(pkg).version,baselineVersion=JSON.parse(baseline).releaseVersion;if(packageVersion!==baselineVersion)failures.push(`Versionen nicht synchron: ${packageVersion}/${baselineVersion}`);if(failures.length){console.error('Interaktives Wetterprofil fehlgeschlagen:\n- '+failures.join('\n- '));process.exit(1)}console.log('Interaktives 24-h-Wetterprofil mit Einzeldaten geprüft.');