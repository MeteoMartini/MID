function pushConfigured(env){return Boolean(env?.MID_PUSH_SUBSCRIPTIONS&&env?.VAPID_PUBLIC_KEY&&env?.VAPID_PRIVATE_KEY&&env?.VAPID_SUBJECT)}
function pushNormaliseOrigin(value){try{const raw=String(value||'').trim();return raw?new URL(raw).origin.toLowerCase():''}catch{return''}}
function pushOrigins(env){const configured=String(env?.MID_ALLOWED_ORIGINS||env?.MID_ALLOWED_ORIGIN||'').split(/[\s,;]+/);return[...new Set([...PUSH_DEFAULT_ORIGINS,...configured].map(pushNormaliseOrigin).filter(Boolean))]}
function pushOriginAllowed(request,env){const origin=pushNormaliseOrigin(request.headers.get('origin'));if(!origin)return false;if(/^https?:\/\/(?:localhost|127\.0\.0\.1)(?::\d+)?$/i.test(origin))return true;return pushOrigins(env).includes(origin)}
function pushB64Decode(value=''){const normalized=String(value).replace(/-/g,'+').replace(/_/g,'/'),padded=normalized+'='.repeat((4-normalized.length%4)%4),raw=atob(padded),bytes=new Uint8Array(raw.length);for(let i=0;i<raw.length;i++)bytes[i]=raw.charCodeAt(i);return bytes}
function pushB64Encode(value){const bytes=value instanceof Uint8Array?value:new Uint8Array(value);let raw='';for(let i=0;i<bytes.length;i++)raw+=String.fromCharCode(bytes[i]);return btoa(raw).replace(/\+/g,'-').replace(/\//g,'_').replace(/=+$/,'')}
function pushConcat(...parts){const arrays=parts.map(part=>part instanceof Uint8Array?part:new Uint8Array(part)),length=arrays.reduce((sum,item)=>sum+item.length,0),out=new Uint8Array(length);let offset=0;for(const item of arrays){out.set(item,offset);offset+=item.length}return out}
async function pushHmac(key,data){const cryptoKey=await crypto.subtle.importKey('raw',key,{name:'HMAC',hash:'SHA-256'},false,['sign']);return new Uint8Array(await crypto.subtle.sign('HMAC',cryptoKey,data))}
async function pushHkdfExpand(prk,info,length){let result=new Uint8Array(0),previous=new Uint8Array(0),counter=1;while(result.length<length){previous=await pushHmac(prk,pushConcat(previous,info,new Uint8Array([counter++])));result=pushConcat(result,previous)}return result.slice(0,length)}
async function pushHash(value){return pushB64Encode(new Uint8Array(await crypto.subtle.digest('SHA-256',PUSH_ENCODER.encode(String(value)))))}
async function vapidToken(endpoint,env){const publicRaw=pushB64Decode(env.VAPID_PUBLIC_KEY),privateRaw=pushB64Decode(env.VAPID_PRIVATE_KEY);if(publicRaw.length!==65||publicRaw[0]!==4||privateRaw.length!==32)throw new Error('Ungültige VAPID-Schlüssel.');const jwk={kty:'EC',crv:'P-256',x:pushB64Encode(publicRaw.slice(1,33)),y:pushB64Encode(publicRaw.slice(33,65)),d:pushB64Encode(privateRaw),ext:true,key_ops:['sign']},key=await crypto.subtle.importKey('jwk',jwk,{name:'ECDSA',namedCurve:'P-256'},false,['sign']),header=pushB64Encode(PUSH_ENCODER.encode(JSON.stringify({typ:'JWT',alg:'ES256'}))),payload=pushB64Encode(PUSH_ENCODER.encode(JSON.stringify({aud:new URL(endpoint).origin,exp:Math.floor(Date.now()/1000)+12*3600,sub:String(env.VAPID_SUBJECT||'mailto:mid@meteomartini.invalid')}))),unsigned=`${header}.${payload}`,signature=new Uint8Array(await crypto.subtle.sign({name:'ECDSA',hash:'SHA-256'},key,PUSH_ENCODER.encode(unsigned)));return`${unsigned}.${pushB64Encode(signature)}`}
async function encryptPushPayload(subscription,payload){const clientPublic=pushB64Decode(subscription?.keys?.p256dh),authSecret=pushB64Decode(subscription?.keys?.auth);if(clientPublic.length!==65||authSecret.length<16)throw new Error('Ungültige Push-Abonnementschlüssel.');const clientKey=await crypto.subtle.importKey('raw',clientPublic,{name:'ECDH',namedCurve:'P-256'},false,[]),serverKeys=await crypto.subtle.generateKey({name:'ECDH',namedCurve:'P-256'},true,['deriveBits']),shared=new Uint8Array(await crypto.subtle.deriveBits({name:'ECDH',public:clientKey},serverKeys.privateKey,256)),serverPublic=new Uint8Array(await crypto.subtle.exportKey('raw',serverKeys.publicKey)),prkKey=await pushHmac(authSecret,shared),keyInfo=pushConcat(PUSH_ENCODER.encode('WebPush: info\0'),clientPublic,serverPublic),ikm=await pushHkdfExpand(prkKey,keyInfo,32),salt=crypto.getRandomValues(new Uint8Array(16)),prk=await pushHmac(salt,ikm),cek=await pushHkdfExpand(prk,PUSH_ENCODER.encode('Content-Encoding: aes128gcm\0'),16),nonce=await pushHkdfExpand(prk,PUSH_ENCODER.encode('Content-Encoding: nonce\0'),12),aesKey=await crypto.subtle.importKey('raw',cek,'AES-GCM',false,['encrypt']),record=pushConcat(PUSH_ENCODER.encode(JSON.stringify(payload)),new Uint8Array([2])),ciphertext=new Uint8Array(await crypto.subtle.encrypt({name:'AES-GCM',iv:nonce,tagLength:128},aesKey,record)),header=new Uint8Array(21+serverPublic.length),view=new DataView(header.buffer);header.set(salt,0);view.setUint32(16,4096,false);header[20]=serverPublic.length;header.set(serverPublic,21);return pushConcat(header,ciphertext)}
async function sendWebPush(subscription,payload,env){const body=await encryptPushPayload(subscription,payload),jwt=await vapidToken(subscription.endpoint,env),response=await fetch(subscription.endpoint,{method:'POST',headers:{Authorization:`vapid t=${jwt}, k=${env.VAPID_PUBLIC_KEY}`,'Content-Encoding':'aes128gcm','Content-Type':'application/octet-stream',TTL:'300',Urgency:'normal'},body});return{ok:response.ok,status:response.status,expired:response.status===404||response.status===410}}
function pushFavoriteUrl(appUrl,favorite){try{const url=new URL(String(appUrl||'https://meteomartini.github.io/MID/'));url.searchParams.set('mid-favorite',String(favorite?.id||''));url.searchParams.set('mid-lat',String(favorite?.latitude??''));url.searchParams.set('mid-lon',String(favorite?.longitude??''));url.searchParams.set('mid-name',String(favorite?.name||'Ort').replace(/^Modelllaufänderung\s*·\s*/i,'').replace(/^Aktueller Standort\s*·\s*/i,''));if(favorite?.country)url.searchParams.set('mid-country',String(favorite.country));return url.toString()}catch{return String(appUrl||'https://meteomartini.github.io/MID/')}}
function pushLocationPayload(favorite){return{favoriteId:String(favorite?.id||''),latitude:number(favorite?.latitude),longitude:number(favorite?.longitude),locationName:String(favorite?.name||'Ort').replace(/^Modelllaufänderung\s*·\s*/i,'').replace(/^Aktueller Standort\s*·\s*/i,''),country:String(favorite?.country||'')}}
function validPushFavorites(value){return(Array.isArray(value)?value:[]).map(item=>({id:String(item?.id||'').slice(0,100),name:String(item?.name||'Favorit').slice(0,100),latitude:number(item?.latitude),longitude:number(item?.longitude),country:String(item?.country||'').slice(0,40),rules:{precipitationStart:Boolean(item?.rules?.precipitationStart),thunderstormApproach:Boolean(item?.rules?.thunderstormApproach),forecastMaterialChange:Boolean(item?.rules?.forecastMaterialChange)}})).filter(item=>item.id&&item.latitude!==undefined&&item.longitude!==undefined&&(item.rules.precipitationStart||item.rules.thunderstormApproach||item.rules.forecastMaterialChange))}
const PUSH_NOTIFICATION_INTERVALS=[15,30,60,120,180],PUSH_PRECIPITATION_LEADS=[15,30,45,60,90,120],PUSH_PRECIPITATION_AMOUNTS=[.1,.2,.5,1,2,5];
function validPushNotificationInterval(value){const minutes=Math.round(Number(value));return PUSH_NOTIFICATION_INTERVALS.includes(minutes)?minutes:60}
function validPushPrecipitationAlert(value){const leadMinutes=Math.round(Number(value?.leadMinutes)),minimumAmountMm=Number(value?.minimumAmountMm);return{leadMinutes:PUSH_PRECIPITATION_LEADS.includes(leadMinutes)?leadMinutes:60,minimumAmountMm:PUSH_PRECIPITATION_AMOUNTS.includes(minimumAmountMm)?minimumAmountMm:.1}}
function pushNotificationReady(lastAt,intervalMinutes,now=Date.now()){const previous=Date.parse(String(lastAt||''));return!Number.isFinite(previous)||now-previous>=validPushNotificationInterval(intervalMinutes)*60000}
function validPushSubscription(value){const endpoint=String(value?.endpoint||''),p256dh=String(value?.keys?.p256dh||''),auth=String(value?.keys?.auth||'');if(!/^https:\/\//i.test(endpoint)||!p256dh||!auth)return null;return{endpoint,expirationTime:value?.expirationTime??null,keys:{p256dh,auth}}}
async function pushSubscribe(request,env){
 if(!pushConfigured(env))return json({error:'Push-Dienst ist im Worker noch nicht konfiguriert.',version:WORKER_VERSION},503,{'cache-control':'no-store'});
 if(!pushOriginAllowed(request,env))return json({error:'Origin für Push-Verwaltung nicht zugelassen.',version:WORKER_VERSION},403,{'cache-control':'no-store'});
 let body;try{body=await request.json()}catch{return json({error:'Ungültiger JSON-Inhalt.',version:WORKER_VERSION},400,{'cache-control':'no-store'})}
 const subscription=validPushSubscription(body?.subscription);if(!subscription)return json({error:'Ungültiges Push-Abonnement.',version:WORKER_VERSION},400,{'cache-control':'no-store'});
 const key=`sub:${await pushHash(subscription.endpoint)}`,existing=await env.MID_PUSH_SUBSCRIPTIONS.get(key,{type:'json'}).catch(()=>null),notificationIntervalMinutes=validPushNotificationInterval(body?.notificationIntervalMinutes),precipitationAlert=validPushPrecipitationAlert(body?.precipitationAlert),ventilation=validVentilationPushConfig(body?.ventilation),entry={subscription,favorites:validPushFavorites(body?.favorites),ventilation,notificationIntervalMinutes,precipitationAlert,lastNotificationAt:existing?.lastNotificationAt||'',lastTestNotificationAt:existing?.lastTestNotificationAt||'',appUrl:String(body?.appUrl||env.MID_APP_URL||'https://meteomartini.github.io/MID/').slice(0,500),userAgent:String(body?.userAgent||'').slice(0,300),state:existing?.state&&typeof existing.state==='object'?existing.state:{},createdAt:existing?.createdAt||new Date().toISOString(),updatedAt:new Date().toISOString()};
 await env.MID_PUSH_SUBSCRIPTIONS.put(key,JSON.stringify(entry));return json({ok:true,favorites:entry.favorites.length,ventilationRooms:ventilation?.roomRules?.length||0,notificationIntervalMinutes,precipitationAlert,version:WORKER_VERSION},200,{'cache-control':'no-store'})
}
function pushEntryRuleCount(favorites,ventilation){return favorites.reduce((sum,item)=>sum+Number(Boolean(item.rules.precipitationStart))+Number(Boolean(item.rules.thunderstormApproach))+Number(Boolean(item.rules.forecastMaterialChange)),0)+(ventilation?.roomRules?.length||0)}
function pushEntryLastError(entry){const state=entry?.state&&typeof entry.state==='object'?entry.state:{};for(const row of Object.values(state))if(row&&typeof row==='object')for(const key of['precipitationError','thunderError','forecastChangeError','ventilationError']){const value=String(row[key]||'').trim();if(value)return value.slice(0,240)}return''}
function pushSchedulerHealth(heartbeat){const stamp=String(heartbeat?.completedAt||heartbeat?.startedAt||''),epoch=Date.parse(stamp),ageMs=Number.isFinite(epoch)?Date.now()-epoch:Number.POSITIVE_INFINITY;return{schedulerHealthy:Number.isFinite(ageMs)&&ageMs>=0&&ageMs<=PUSH_SCHEDULER_HEALTH_MS,schedulerLastRunAt:stamp||undefined,schedulerAgeMs:Number.isFinite(ageMs)?ageMs:undefined}}
async function pushStatus(request,env){
 if(!pushConfigured(env))return json({configured:false,registered:false,schedulerHealthy:false,error:'Push-Dienst ist im Worker noch nicht vollständig konfiguriert.',version:WORKER_VERSION},200,{'cache-control':'no-store'});
 if(!pushOriginAllowed(request,env))return json({error:'Origin für Push-Verwaltung nicht zugelassen.',version:WORKER_VERSION},403,{'cache-control':'no-store'});
 let body;try{body=await request.json()}catch{return json({error:'Ungültiger JSON-Inhalt.',version:WORKER_VERSION},400,{'cache-control':'no-store'})}const endpoint=String(body?.endpoint||'');if(!/^https:\/\//i.test(endpoint))return json({error:'Ungültige Push-Abonnementadresse.',version:WORKER_VERSION},400,{'cache-control':'no-store'});
 const key=`sub:${await pushHash(endpoint)}`,[entry,heartbeat]=await Promise.all([env.MID_PUSH_SUBSCRIPTIONS.get(key,{type:'json'}).catch(()=>null),env.MID_PUSH_SUBSCRIPTIONS.get(PUSH_SCHEDULER_HEARTBEAT_KEY,{type:'json'}).catch(()=>null)]),favorites=entry?validPushFavorites(entry.favorites):[],scheduler=pushSchedulerHealth(heartbeat);return json({ok:true,configured:true,registered:Boolean(entry),activeFavorites:favorites.length,activeRules:pushEntryRuleCount(favorites,validVentilationPushConfig(entry?.ventilation)),updatedAt:entry?.updatedAt,checkedAt:entry?.checkedAt,lastNotificationAt:entry?.lastNotificationAt,lastError:entry?pushEntryLastError(entry):'',...scheduler,schedulerSubscriptions:number(heartbeat?.subscriptions)||0,schedulerComplete:Boolean(heartbeat?.complete),version:WORKER_VERSION},200,{'cache-control':'no-store'})
}
async function pushTest(request,env){
 if(!pushConfigured(env))return json({error:'Push-Dienst ist im Worker noch nicht konfiguriert.',version:WORKER_VERSION},503,{'cache-control':'no-store'});if(!pushOriginAllowed(request,env))return json({error:'Origin für Push-Verwaltung nicht zugelassen.',version:WORKER_VERSION},403,{'cache-control':'no-store'});let body;try{body=await request.json()}catch{return json({error:'Ungültiger JSON-Inhalt.',version:WORKER_VERSION},400,{'cache-control':'no-store'})}const endpoint=String(body?.endpoint||'');if(!/^https:\/\//i.test(endpoint))return json({error:'Ungültige Push-Abonnementadresse.',version:WORKER_VERSION},400,{'cache-control':'no-store'});const key=`sub:${await pushHash(endpoint)}`,entry=await env.MID_PUSH_SUBSCRIPTIONS.get(key,{type:'json'}).catch(()=>null);if(!entry)return json({error:'Dieses Gerät ist im MID-Worker nicht als Push-Empfänger registriert.',version:WORKER_VERSION},404,{'cache-control':'no-store'});const subscription=validPushSubscription(entry.subscription);if(!subscription)return json({error:'Das gespeicherte Push-Abonnement ist ungültig.',version:WORKER_VERSION},409,{'cache-control':'no-store'});const lastTest=Date.parse(String(entry.lastTestNotificationAt||''));if(Number.isFinite(lastTest)&&Date.now()-lastTest<30000)return json({error:'Bitte vor einer weiteren Testmitteilung kurz warten.',version:WORKER_VERSION},429,{'cache-control':'no-store'});const sent=await sendWebPush(subscription,{title:'MID · Testbenachrichtigung',body:'Die MID-Push-Verbindung zu diesem Gerät funktioniert.',tag:'mid-push-test',url:String(entry.appUrl||env.MID_APP_URL||'https://midwx.app/'),timestamp:Date.now(),renotify:false},env);if(sent.expired){await env.MID_PUSH_SUBSCRIPTIONS.delete(key);return json({error:'Das Push-Abonnement ist beim Browser nicht mehr gültig. Bitte Benachrichtigungen erneut aktivieren.',version:WORKER_VERSION},410,{'cache-control':'no-store'})}if(!sent.ok)return json({error:`Push-Anbieter meldet HTTP ${sent.status}.`,version:WORKER_VERSION},502,{'cache-control':'no-store'});const sentAt=new Date().toISOString();await env.MID_PUSH_SUBSCRIPTIONS.put(key,JSON.stringify({...entry,lastTestNotificationAt:sentAt,updatedAt:sentAt}));return json({ok:true,sentAt,version:WORKER_VERSION},200,{'cache-control':'no-store'})
}
function pushPrecipitationKind(signal){return signal.snowfall>=.02||[68,69,71,73,75,77,85,86].includes(signal.code)?'Schnee':signal.showers>=.02||FORECAST_SHOWER_CODES.has(signal.code)?'Schauer':'Regen'}
function pushPrecipitationAmount(signal){return Math.max(0,number(signal?.precipitation)??0,(number(signal?.rain)??0)+(number(signal?.showers)??0),(number(signal?.snowfall)??0)*.1)}
function pushRadarFrameAmount(frame){const direct=number(frame?.amountMm);if(Number.isFinite(direct))return Math.max(0,direct);const rate=Math.max(0,number(frame?.rate)||0);return rate*5/60}
async function pushRadarPrecipitationState(favorite,alert){const radar=await dwdRadarNowcast(favorite.latitude,favorite.longitude,favorite.country,'all',true).catch(()=>null);if(!radar||radar.source==='model'||radar.coverage===false||radar.temporaryUnavailable)return null;const threshold=Math.max(.05,number(radar.siteEchoThreshold)||.05),now=Date.now(),active=Math.max(0,number(radar.currentRate)||0)>=threshold,series=Array.isArray(radar.nowcastSeries)?radar.nowcastSeries:[],rows=[];for(const frame of series){const time=Date.parse(String(frame?.time||'')),minutes=Math.round((time-now)/60000),rate=Math.max(0,number(frame?.rate)||0),amount=pushRadarFrameAmount(frame),meaningful=rate>=threshold||amount>=.01;if(!Number.isFinite(time)||minutes<0||minutes>Math.max(180,alert.leadMinutes+90)||!meaningful)continue;rows.push({time,minutes,key:String(frame.time||time),amount,rate})}let upcomingMinutes,upcomingKey='',expectedAmountMm=0;if(!active&&rows.length){const groups=[];for(const row of rows){const last=groups.at(-1),previous=last?.at(-1);if(last&&previous&&row.time-previous.time<=10*60000)last.push(row);else groups.push([row])}const event=groups.find(group=>group[0].minutes>=0&&group[0].minutes<=alert.leadMinutes&&group.reduce((sum,row)=>sum+row.amount,0)>=alert.minimumAmountMm);if(event){upcomingMinutes=Math.max(0,event[0].minutes);upcomingKey=event[0].key;expectedAmountMm=event.reduce((sum,row)=>sum+row.amount,0)}}return{active,upcomingMinutes,upcomingKey,kind:'Niederschlag',expectedAmountMm,leadMinutes:alert.leadMinutes,minimumAmountMm:alert.minimumAmountMm,source:'radar'}}
async function pushWeatherState(favorite,alertSettings){const alert=validPushPrecipitationAlert(alertSettings),url=new URL('https://api.open-meteo.com/v1/forecast');url.searchParams.set('latitude',String(favorite.latitude));url.searchParams.set('longitude',String(favorite.longitude));url.searchParams.set('timezone','GMT');url.searchParams.set('forecast_minutely_15','24');url.searchParams.set('past_minutely_15','1');url.searchParams.set('current','precipitation,rain,showers,snowfall,weather_code,cloud_cover');url.searchParams.set('minutely_15',CORE_FORECAST_MINUTELY);const[data,radar]=await Promise.all([fetchJson(url.toString()),pushRadarPrecipitationState(favorite,alert)]),current=data?.current??{},currentSignal=reconcileForecastPrecipitation(current.precipitation,100,current.weather_code,current.cloud_cover,{rain:current.rain,showers:current.showers,snowfall:current.snowfall,observed:true}),modelActive=pushPrecipitationAmount(currentSignal)>=.03,times=data?.minutely_15?.time??[],m=data?.minutely_15??{},now=Date.now(),rows=[];for(let i=0;i<times.length;i++){const time=Date.parse(`${times[i]}Z`),minutes=Math.round((time-now)/60000);if(!Number.isFinite(time)||minutes<0||minutes>360)continue;const signal=reconcileForecastPrecipitation(m.precipitation?.[i],m.precipitation_probability?.[i],m.weather_code?.[i],undefined,{rain:m.rain?.[i],showers:m.showers?.[i],snowfall:m.snowfall?.[i],sunshineDuration:m.sunshine_duration?.[i],leadHours:Math.max(0,minutes/60)}),amount=pushPrecipitationAmount(signal),meaningful=amount>=.01||signal.probability>=55&&FORECAST_PRECIPITATION_CODES.has(signal.code);rows.push({time,minutes,key:String(times[i]),signal,amount,meaningful})}let upcomingMinutes,upcomingKey='',kind=pushPrecipitationKind(currentSignal),expectedAmountMm=0,source='model';if(!modelActive){const groups=[];for(const row of rows){if(!row.meaningful)continue;const last=groups.at(-1),previous=last?.at(-1);if(last&&previous&&row.time-previous.time<=20*60000)last.push(row);else groups.push([row])}const event=groups.find(group=>group[0].minutes>=0&&group[0].minutes<=alert.leadMinutes&&group.reduce((sum,row)=>sum+row.amount,0)>=alert.minimumAmountMm);if(event){upcomingMinutes=event[0].minutes;upcomingKey=event[0].key;expectedAmountMm=event.reduce((sum,row)=>sum+row.amount,0);kind=pushPrecipitationKind(event[0].signal)}}const active=Boolean(radar?.active)||modelActive,radarBetter=radar&&!radar.active&&radar.upcomingMinutes!==undefined&&(upcomingMinutes===undefined||radar.upcomingMinutes<=upcomingMinutes+10||radar.expectedAmountMm>=Math.max(alert.minimumAmountMm,expectedAmountMm));if(radarBetter){upcomingMinutes=radar.upcomingMinutes;upcomingKey=`radar:${radar.upcomingKey}`;expectedAmountMm=Math.max(expectedAmountMm,radar.expectedAmountMm);kind=radar.kind;source='radar'}return{active,upcomingMinutes,upcomingKey,kind,expectedAmountMm,leadMinutes:alert.leadMinutes,minimumAmountMm:alert.minimumAmountMm,source}}
function pushThunderState(result){const candidates=Array.isArray(result?.nearbyCells)?result.nearbyCells:[],eligible=candidates.map(cell=>{const currentDistanceKm=number(cell?.currentDistanceKm),forecastDistanceKm=number(cell?.forecastDistanceKm),effectiveDistanceKm=number(cell?.forecastEffectiveDistanceKm??cell?.relevanceDistanceKm),arrivalMinutes=number(cell?.arrivalMinutes),near=Number.isFinite(currentDistanceKm)&&currentDistanceKm<=20,approaching=Boolean(!near&&cell?.isApproaching&&Number.isFinite(effectiveDistanceKm)&&effectiveDistanceKm<=35&&(!Number.isFinite(arrivalMinutes)||arrivalMinutes<=60));return{cell,currentDistanceKm,forecastDistanceKm,effectiveDistanceKm,arrivalMinutes,near,approaching,rank:near?currentDistanceKm:(effectiveDistanceKm??999)}}).filter(row=>row.near||row.approaching).sort((a,b)=>Number(a.rank)-Number(b.rank)),selected=eligible[0];if(!selected)return{active:false};const{cell,currentDistanceKm,forecastDistanceKm,effectiveDistanceKm,arrivalMinutes,near,approaching}=selected,status=near?(currentDistanceKm<1?'at-site':'near'):'approaching';return{active:true,key:String(cell.id||`${Math.round(Number(cell.latitude)||0)}:${Math.round(Number(cell.longitude)||0)}`),status,currentDistanceKm,forecastDistanceKm,effectiveDistanceKm,arrivalMinutes:approaching&&Number.isFinite(arrivalMinutes)&&arrivalMinutes>0?arrivalMinutes:undefined}}
function pushVisibleLocationName(favorite){return String(favorite?.name||'Ort').replace(/^Modelllaufänderung\s*·\s*/i,'').replace(/^Aktueller Standort\s*·\s*/i,'').trim()||'Ort'}
function pushLocationPhrases(favorite){const tracked=String(favorite?.id||'')==='tracked-location'||/^Aktueller Standort\s*·/i.test(String(favorite?.name||'')),name=pushVisibleLocationName(favorite);return tracked?{at:'am Standort',toward:'dem Standort',from:'vom Standort',name:'Standort'}:{at:`bei ${name}`,toward:name,from:`von ${name}`,name}}
function thunderPushBody(result,favorite){const place=pushLocationPhrases(favorite),current=Number(result?.currentDistanceKm),forecast=Number(result?.forecastDistanceKm),arrival=Number(result?.arrivalMinutes);if(result?.status==='at-site'||Number.isFinite(current)&&current<1)return`Eine Gewitterzelle befindet sich unmittelbar ${place.at}. Keine amtliche Warnung.`;if(result?.status==='near'||Number.isFinite(current)&&current<=20)return`Eine Gewitterzelle befindet sich aktuell etwa ${Math.max(1,Math.round(current))} km ${place.from} entfernt. Keine amtliche Warnung.`;const currentText=Number.isFinite(current)?` · aktuell etwa ${Math.max(1,Math.round(current))} km entfernt`:'',approachText=Number.isFinite(arrival)&&arrival>0?` · größte berechnete Annäherung in ${Math.round(arrival)} min${Number.isFinite(forecast)?` auf etwa ${Math.max(0,Math.round(forecast))} km`:''}`:'';return`Eine Gewitterzelle nähert sich ${place.toward}${currentText}${approachText}. Keine amtliche Warnung.`}
function pushForecastRounded(value,digits=1){const n=Number(value);if(!Number.isFinite(n))return 0;const factor=10**digits;return Math.round(n*factor)/factor}
async function pushForecastState(favorite){
 const url=new URL('https://api.open-meteo.com/v1/forecast');url.searchParams.set('latitude',String(favorite.latitude));url.searchParams.set('longitude',String(favorite.longitude));url.searchParams.set('timezone','auto');url.searchParams.set('forecast_days','3');url.searchParams.set('wind_speed_unit','kn');url.searchParams.set('models','best_match');url.searchParams.set('daily','temperature_2m_max,temperature_2m_min,precipitation_sum,rain_sum,showers_sum,snowfall_sum,precipitation_hours,precipitation_probability_max,weather_code,wind_gusts_10m_max');url.searchParams.set('hourly','precipitation,precipitation_probability,rain,showers,snowfall,weather_code,cloud_cover,cloud_cover_low,relative_humidity_2m,cape,lifted_index,convective_inhibition,sunshine_duration,is_day');
 const data=await fetchJson(url.toString()),daily=data?.daily??{},times=Array.isArray(daily.time)?daily.time:[],rows=times.slice(0,3).map((date,index)=>{const signal=reconcileForecastPrecipitation(daily.precipitation_sum?.[index],daily.precipitation_probability_max?.[index],daily.weather_code?.[index],undefined,{rain:daily.rain_sum?.[index],showers:daily.showers_sum?.[index],snowfall:daily.snowfall_sum?.[index],leadHours:(index+.5)*24});return{date:String(date),max:pushForecastRounded(daily.temperature_2m_max?.[index]),min:pushForecastRounded(daily.temperature_2m_min?.[index]),precipitation:pushForecastRounded(signal.precipitation),rain:pushForecastRounded(signal.rain),showers:pushForecastRounded(signal.showers),snowfall:pushForecastRounded(signal.snowfall),probability:Math.round(signal.probability),gust:pushForecastRounded(daily.wind_gusts_10m_max?.[index])}}),hourly=data?.hourly??{},hourTimes=Array.isArray(hourly.time)?hourly.time:[],offsetSeconds=number(data?.utc_offset_seconds)||0,now=Date.now()-3600000;let onset='';
 for(let index=0;index<hourTimes.length;index++){const epoch=Date.parse(`${hourTimes[index]}Z`)-offsetSeconds*1000;if(epoch<now)continue;if(epoch>now+3*86400000)break;const signal=reconcileForecastPrecipitation(hourly.precipitation?.[index],hourly.precipitation_probability?.[index],hourly.weather_code?.[index],hourly.cloud_cover?.[index],{rain:hourly.rain?.[index],showers:hourly.showers?.[index],snowfall:hourly.snowfall?.[index],lowCloud:hourly.cloud_cover_low?.[index],humidity:hourly.relative_humidity_2m?.[index],cape:hourly.cape?.[index],liftedIndex:hourly.lifted_index?.[index],convectiveInhibition:hourly.convective_inhibition?.[index],sunshineDuration:hourly.sunshine_duration?.[index],isDay:Number(hourly.is_day?.[index])===1,leadHours:(epoch-Date.now())/3600000});if(signal.precipitation>=.1||signal.probability>=65){onset=String(hourTimes[index]);break}}
 const signature=JSON.stringify({rows,onset});return{signature,rows,onset,checkedAt:new Date().toISOString()}
}
function pushForecastChangeEvents(previous,current){if(!previous?.rows?.length||!current?.rows?.length)return[];const before=new Map(previous.rows.map(row=>[row.date,row])),events=[];for(const row of current.rows.slice(0,7)){const old=before.get(row.date);if(!old)continue;const add=(metric,delta,threshold,unit,label)=>{if(Math.abs(delta)>=threshold)events.push({metric,date:row.date,delta,label,text:`${label} ${delta>0?'+':''}${Math.round(delta*10)/10} ${unit}`})};add('tmax',row.max-old.max,2.5,'K','Tmax');add('tmin',row.min-old.min,2.5,'K','Tmin');add('probability',row.probability-old.probability,25,'%-Punkte','Regenwahrscheinlichkeit');add('gust',row.gust-old.gust,12,'kt','Böen');const rainDelta=row.precipitation-old.precipitation,rainReference=Math.max(row.precipitation,old.precipitation);if(Math.abs(rainDelta)>=5&&(rainReference>=5||Math.abs(rainDelta)>=10))events.push({metric:'precipitation',date:row.date,delta:rainDelta,label:'Niederschlag',text:`Niederschlag ${rainDelta>0?'+':''}${Math.round(rainDelta*10)/10} mm`})}if(previous.onset&&current.onset){const delta=(Date.parse(current.onset)-Date.parse(previous.onset))/3600000;if(Number.isFinite(delta)&&Math.abs(delta)>=2)events.push({metric:'onset',delta,label:'Regenbeginn',text:`Regenbeginn ${Math.abs(Math.round(delta))} h ${delta>0?'später':'früher'}`})}else if(previous.onset&&!current.onset)events.push({metric:'onset',delta:0,label:'Regenbeginn',text:'Niederschlag aus dem 8-Tage-Fenster verschwunden'});else if(!previous.onset&&current.onset)events.push({metric:'onset',delta:0,label:'Regenbeginn',text:'Niederschlag neu im 8-Tage-Fenster'});return events.slice(0,8)}
function pushForecastEventText(event){const date=event.date?new Intl.DateTimeFormat('de-DE',{weekday:'short',day:'2-digit',month:'2-digit',timeZone:'UTC'}).format(new Date(`${event.date}T12:00:00Z`)):'Nächster Zeitraum';return`${date}: ${event.text}`}
async function pushMapLimited(items,limit,task){let cursor=0;const workers=Array.from({length:Math.min(limit,items.length)},async()=>{while(cursor<items.length){const index=cursor++;await task(items[index],index)}});await Promise.all(workers)}
async function evaluatePushEntry(key,entry,env,weatherCache,thunderCache,forecastCache,netatmoCache,ventilationForecastCache){
 const subscription=validPushSubscription(entry?.subscription);if(!subscription){await env.MID_PUSH_SUBSCRIPTIONS.delete(key);return}
 const favorites=validPushFavorites(entry?.favorites),state=entry?.state&&typeof entry.state==='object'?entry.state:{},appUrl=String(entry?.appUrl||env.MID_APP_URL||'https://meteomartini.github.io/MID/'),notificationIntervalMinutes=validPushNotificationInterval(entry?.notificationIntervalMinutes),precipitationAlert=validPushPrecipitationAlert(entry?.precipitationAlert);let expired=false,changed=false,lastNotificationAt=String(entry?.lastNotificationAt||'');
 const canNotify=()=>pushNotificationReady(lastNotificationAt,notificationIntervalMinutes),markNotificationSent=()=>{lastNotificationAt=new Date().toISOString();changed=true;return lastNotificationAt};
 for(const favorite of favorites){
  const old=state[favorite.id]??{},next={...old};
  if(favorite.rules.precipitationStart){
   const cacheKey=`${favorite.latitude.toFixed(4)}:${favorite.longitude.toFixed(4)}:${precipitationAlert.leadMinutes}:${precipitationAlert.minimumAmountMm}`;let weather=weatherCache.get(cacheKey);if(!weather){weather=pushWeatherState(favorite,precipitationAlert);weatherCache.set(cacheKey,weather)}
   try{
    const result=await weather,eventKey=result.upcomingKey||'',triggerUpcoming=!result.active&&result.upcomingMinutes!==undefined&&!old.rainEventKey,trigger=triggerUpcoming;let advanceState=!trigger;
    if(trigger&&canNotify()){
     const place=pushLocationPhrases(favorite),amountText=Number(result.expectedAmountMm)>=.1?` · erwartet ca. ${Number(result.expectedAmountMm).toLocaleString('de-DE',{minimumFractionDigits:Number(result.expectedAmountMm)<1?1:0,maximumFractionDigits:1})} mm`:'',sourceHint=result.source==='radar'?' · radarbasiert früh erkannt':'',body=`${result.kind} beginnt ${place.at} voraussichtlich in ${result.upcomingMinutes} Minuten${amountText}${sourceHint}.`,sent=await sendWebPush(subscription,{title:`MID · Niederschlag in Kürze ${place.at}`,body,tag:`mid-rain-${favorite.id}`,url:pushFavoriteUrl(appUrl,favorite),...pushLocationPayload(favorite),timestamp:Date.now(),renotify:true},env);
     if(sent.expired){expired=true;break}
     if(sent.ok){const sentAt=markNotificationSent();next.rainEventKey=eventKey||`active-${sentAt.slice(0,13)}`;next.lastRainNotificationAt=sentAt;advanceState=true}
    }
    if(advanceState){next.precipitationActive=result.active;if(!result.active&&result.upcomingMinutes===undefined)next.rainEventKey=''}
    changed=true
   }catch(error){next.precipitationError=error instanceof Error?error.message:String(error);changed=true}
  }
  if(favorite.rules.thunderstormApproach&&!expired){
   const cacheKey=`${favorite.latitude.toFixed(4)}:${favorite.longitude.toFixed(4)}:${favorite.country}`;let thunder=thunderCache.get(cacheKey);if(!thunder){thunder=dwdKonrad3dNowcast(favorite.latitude,favorite.longitude,favorite.country);thunderCache.set(cacheKey,thunder)}
   try{
    const result=pushThunderState(await thunder),trigger=result.active&&(!old.thunderActive||result.key!==old.thunderCellKey);let advanceState=!trigger;
    if(trigger&&canNotify()){
     const place=pushLocationPhrases(favorite),sent=await sendWebPush(subscription,{title:`MID · Gewitter ${place.at}`,body:thunderPushBody(result,favorite),tag:`mid-thunder-${favorite.id}`,url:pushFavoriteUrl(appUrl,favorite),...pushLocationPayload(favorite),timestamp:Date.now(),renotify:true},env);
     if(sent.expired){expired=true;break}
     if(sent.ok){next.lastThunderNotificationAt=markNotificationSent();advanceState=true}
    }
    if(advanceState){next.thunderActive=result.active;next.thunderCellKey=result.active?result.key:''}
    changed=true
   }catch(error){next.thunderError=error instanceof Error?error.message:String(error);changed=true}
  }
  if(favorite.rules.forecastMaterialChange&&!expired){
   const cacheKey=`${favorite.latitude.toFixed(4)}:${favorite.longitude.toFixed(4)}`;let forecast=forecastCache.get(cacheKey);if(!forecast){forecast=pushForecastState(favorite);forecastCache.set(cacheKey,forecast)}
   try{
    const result=await forecast,reference=old.forecastReferenceSnapshot||old.forecastSnapshot,events=pushForecastChangeEvents(reference,result),newSignature=result.signature!==old.forecastSnapshot?.signature,trigger=newSignature&&Boolean(reference)&&events.length>0&&old.forecastNotifiedSignature!==result.signature;let advanceState=!trigger;
    if(trigger&&canNotify()){
     const visibleName=favorite.name.replace(/^Modelllaufänderung\s*·\s*/i,''),extra=events.length>2?` · plus ${events.length-2} weitere`:'',sent=await sendWebPush(subscription,{title:`MID · Prognoseänderung bei ${visibleName}`,body:`${events.slice(0,2).map(pushForecastEventText).join(' · ')}${extra}`,tag:`mid-model-change-${favorite.id}`,url:pushFavoriteUrl(appUrl,favorite),...pushLocationPayload(favorite),timestamp:Date.now(),renotify:true},env);
     if(sent.expired){expired=true;break}
     if(sent.ok){next.forecastNotifiedSignature=result.signature;next.forecastReferenceSnapshot=result;next.lastForecastChangeNotificationAt=markNotificationSent();advanceState=true}
    }
    if(newSignature&&advanceState){if(!reference)next.forecastReferenceSnapshot=result;next.forecastSnapshot=result;next.forecastMaterialChanges=events}
    if(newSignature)changed=true
   }catch(error){next.forecastChangeError=error instanceof Error?error.message:String(error);changed=true}
  }
  state[favorite.id]=next
 }
 const ventilation=validVentilationPushConfig(entry?.ventilation);
 if(ventilation&&!expired){const old=state.__ventilation&&typeof state.__ventilation==='object'?state.__ventilation:{},next={...old};try{const advice=await ventilationAdvice(ventilation.connectionId,ventilation.roomRules,env,netatmoCache,ventilationForecastCache,ventilation.deviceId),candidates=(advice.rooms||[]).filter(room=>room.status==='open'||room.status==='later'&&Number(room.leadHours)>=0&&Number(room.leadHours)<=.75).sort((a,b)=>a.status===b.status?Number(a.leadHours||0)-Number(b.leadHours||0):a.status==='open'?-1:1),room=candidates[0];next.lastAdviceAt=advice.generatedAt;next.nextWindowKey=room?.windowKey||'';if(room&&room.windowKey!==old.notifiedWindowKey){const imminent=room.status==='open'||Number(room.leadHours)<=.75;if(imminent&&canNotify()){const target=(()=>{try{const url=new URL(appUrl);url.hash='mid-section-ventilation';return url.toString()}catch{return appUrl}})(),sent=await sendWebPush(subscription,{title:`MID · Lüften · ${room.name}`,body:`${room.status==='open'?'Jetzt':`Ab ${room.startLabel}`} ${room.durationMinutes||15} min · ${(room.reasons||[]).slice(0,2).join(' · ')}`,tag:`mid-ventilation-${room.roomId}`,url:target,timestamp:Date.now(),renotify:false},env);if(sent.expired)expired=true;else if(sent.ok){next.notifiedWindowKey=room.windowKey;next.lastVentilationNotificationAt=markNotificationSent()}}}delete next.ventilationError;changed=true}catch(error){next.ventilationError=error instanceof Error?error.message:String(error);changed=true}state.__ventilation=next}
 if(expired){await env.MID_PUSH_SUBSCRIPTIONS.delete(key);return}
 if(changed)await env.MID_PUSH_SUBSCRIPTIONS.put(key,JSON.stringify({...entry,subscription,favorites,ventilation,notificationIntervalMinutes,lastNotificationAt,state,checkedAt:new Date().toISOString()}))
}

function deviceSyncConfigured(env){return Boolean(env?.MID_PUSH_SUBSCRIPTIONS)}
function validDeviceSyncKey(value){const key=String(value||'').replace(/[^A-Za-z0-9_-]/g,'');return key.length>=32&&key.length<=160?key:''}
async function deviceSyncStorageKey(syncKey){return`sync:${await pushHash(`mid-device-sync:${syncKey}`)}`}
async function deviceSyncPush(request,env){
 if(!deviceSyncConfigured(env))return json({error:'Gerätesynchronisation ist im Worker nicht konfiguriert.',version:WORKER_VERSION},503,{'cache-control':'no-store'});
 if(!pushOriginAllowed(request,env))return json({error:'Nicht freigegebener Ursprung.',version:WORKER_VERSION},403,{'cache-control':'no-store'});
 const body=await request.json().catch(()=>null),syncKey=validDeviceSyncKey(body?.syncKey),blob=body?.blob,updatedAt=String(body?.updatedAt||new Date().toISOString());
 if(!syncKey||!blob||typeof blob.iv!=='string'||typeof blob.data!=='string')return json({error:'Ungültiger Synchronisationsstand.',version:WORKER_VERSION},400,{'cache-control':'no-store'});
 if(blob.iv.length>256||blob.data.length>1800000)return json({error:'Synchronisationsstand ist zu groß.',version:WORKER_VERSION},413,{'cache-control':'no-store'});
 const key=await deviceSyncStorageKey(syncKey),entry={schema:'mid-device-sync',version:1,updatedAt,deviceId:String(body?.deviceId||'').slice(0,160),blob:{iv:blob.iv,data:blob.data}};
 await env.MID_PUSH_SUBSCRIPTIONS.put(key,JSON.stringify(entry),{expirationTtl:180*24*3600});
 return json({ok:true,updatedAt,version:WORKER_VERSION},200,{'cache-control':'no-store'});
}
async function deviceSyncPull(request,env){
 if(!deviceSyncConfigured(env))return json({error:'Gerätesynchronisation ist im Worker nicht konfiguriert.',version:WORKER_VERSION},503,{'cache-control':'no-store'});
 if(!pushOriginAllowed(request,env))return json({error:'Nicht freigegebener Ursprung.',version:WORKER_VERSION},403,{'cache-control':'no-store'});
 const body=await request.json().catch(()=>null),syncKey=validDeviceSyncKey(body?.syncKey);if(!syncKey)return json({error:'Ungültiger Synchronisationscode.',version:WORKER_VERSION},400,{'cache-control':'no-store'});
 const entry=await env.MID_PUSH_SUBSCRIPTIONS.get(await deviceSyncStorageKey(syncKey),{type:'json'}).catch(()=>null);if(!entry?.blob)return json({error:'Synchronisationsstand nicht gefunden.',version:WORKER_VERSION},404,{'cache-control':'no-store'});
 return json({ok:true,updatedAt:entry.updatedAt,blob:entry.blob,version:WORKER_VERSION},200,{'cache-control':'no-store'});
}

const DEVICE_SYNC_ARCHIVE_MAX_CHUNKS=24;
const DEVICE_SYNC_ARCHIVE_MAX_CHUNK_CHARS=650000;
function validArchiveRevision(value){const revision=String(value||'').replace(/[^A-Za-z0-9_-]/g,'');return revision.length>=12&&revision.length<=80?revision:''}
async function deviceSyncArchivePrefix(syncKey){return`sync-archive:${await pushHash(`mid-device-sync-archive:${syncKey}`)}:`}
async function deviceSyncArchiveManifestKey(syncKey){return`${await deviceSyncArchivePrefix(syncKey)}manifest`}
async function deviceSyncArchiveChunkKey(syncKey,revision,index){return`${await deviceSyncArchivePrefix(syncKey)}${revision}:${index}`}
async function deviceSyncArchiveChunkPush(request,env){
 if(!deviceSyncConfigured(env))return json({error:'Gerätesynchronisation ist im Worker nicht konfiguriert.',version:WORKER_VERSION},503,{'cache-control':'no-store'});
 if(!pushOriginAllowed(request,env))return json({error:'Nicht freigegebener Ursprung.',version:WORKER_VERSION},403,{'cache-control':'no-store'});
 const body=await request.json().catch(()=>null),syncKey=validDeviceSyncKey(body?.syncKey),revision=validArchiveRevision(body?.revision),index=Number(body?.index),total=Number(body?.total),chunk=String(body?.chunk||'');
 if(!syncKey||!revision||!Number.isInteger(index)||index<0||!Number.isInteger(total)||total<1||total>DEVICE_SYNC_ARCHIVE_MAX_CHUNKS||index>=total||!chunk||chunk.length>DEVICE_SYNC_ARCHIVE_MAX_CHUNK_CHARS||!/^[A-Za-z0-9_-]+$/.test(chunk))return json({error:'Ungültiger Wetterzwilling-Archivteil.',version:WORKER_VERSION},400,{'cache-control':'no-store'});
 await env.MID_PUSH_SUBSCRIPTIONS.put(await deviceSyncArchiveChunkKey(syncKey,revision,index),chunk,{expirationTtl:180*24*3600});
 return json({ok:true,index,total,version:WORKER_VERSION},200,{'cache-control':'no-store'});
}
async function deviceSyncArchiveCommit(request,env){
 if(!deviceSyncConfigured(env))return json({error:'Gerätesynchronisation ist im Worker nicht konfiguriert.',version:WORKER_VERSION},503,{'cache-control':'no-store'});
 if(!pushOriginAllowed(request,env))return json({error:'Nicht freigegebener Ursprung.',version:WORKER_VERSION},403,{'cache-control':'no-store'});
 const body=await request.json().catch(()=>null),syncKey=validDeviceSyncKey(body?.syncKey),revision=validArchiveRevision(body?.revision),iv=String(body?.iv||''),chunks=Number(body?.chunks),bytes=Number(body?.bytes),updatedAt=String(body?.updatedAt||new Date().toISOString());
 if(!syncKey||!revision||!/^[A-Za-z0-9_-]{12,256}$/.test(iv)||!Number.isInteger(chunks)||chunks<1||chunks>DEVICE_SYNC_ARCHIVE_MAX_CHUNKS||!Number.isFinite(bytes)||bytes<1||bytes>DEVICE_SYNC_ARCHIVE_MAX_CHUNKS*DEVICE_SYNC_ARCHIVE_MAX_CHUNK_CHARS)return json({error:'Ungültiges Wetterzwilling-Archivmanifest.',version:WORKER_VERSION},400,{'cache-control':'no-store'});
 const manifestKey=await deviceSyncArchiveManifestKey(syncKey),previous=await env.MID_PUSH_SUBSCRIPTIONS.get(manifestKey,{type:'json'}).catch(()=>null),entry={schema:'mid-device-sync-archive',version:1,updatedAt,revision,iv,chunks,bytes,deviceId:String(body?.deviceId||'').slice(0,160)};
 await env.MID_PUSH_SUBSCRIPTIONS.put(manifestKey,JSON.stringify(entry),{expirationTtl:180*24*3600});
 if(previous?.revision&&previous.revision!==revision&&Number.isInteger(previous.chunks))await Promise.allSettled(Array.from({length:Math.min(DEVICE_SYNC_ARCHIVE_MAX_CHUNKS,previous.chunks)},async(_,index)=>env.MID_PUSH_SUBSCRIPTIONS.delete(await deviceSyncArchiveChunkKey(syncKey,previous.revision,index))));
 return json({ok:true,updatedAt,revision,chunks,bytes,version:WORKER_VERSION},200,{'cache-control':'no-store'});
}
async function deviceSyncArchiveManifest(request,env){
 if(!deviceSyncConfigured(env))return json({error:'Gerätesynchronisation ist im Worker nicht konfiguriert.',version:WORKER_VERSION},503,{'cache-control':'no-store'});
 if(!pushOriginAllowed(request,env))return json({error:'Nicht freigegebener Ursprung.',version:WORKER_VERSION},403,{'cache-control':'no-store'});
 const body=await request.json().catch(()=>null),syncKey=validDeviceSyncKey(body?.syncKey);if(!syncKey)return json({error:'Ungültiger Synchronisationscode.',version:WORKER_VERSION},400,{'cache-control':'no-store'});
 const entry=await env.MID_PUSH_SUBSCRIPTIONS.get(await deviceSyncArchiveManifestKey(syncKey),{type:'json'}).catch(()=>null);if(!entry?.revision)return json({error:'Wetterzwilling-Langzeitarchiv nicht gefunden.',found:false,version:WORKER_VERSION},404,{'cache-control':'no-store'});
 return json({ok:true,found:true,updatedAt:entry.updatedAt,revision:entry.revision,iv:entry.iv,chunks:entry.chunks,bytes:entry.bytes,version:WORKER_VERSION},200,{'cache-control':'no-store'});
}
async function deviceSyncArchiveChunkPull(request,env){
 if(!deviceSyncConfigured(env))return json({error:'Gerätesynchronisation ist im Worker nicht konfiguriert.',version:WORKER_VERSION},503,{'cache-control':'no-store'});
 if(!pushOriginAllowed(request,env))return json({error:'Nicht freigegebener Ursprung.',version:WORKER_VERSION},403,{'cache-control':'no-store'});
 const body=await request.json().catch(()=>null),syncKey=validDeviceSyncKey(body?.syncKey),revision=validArchiveRevision(body?.revision),index=Number(body?.index);if(!syncKey||!revision||!Number.isInteger(index)||index<0||index>=DEVICE_SYNC_ARCHIVE_MAX_CHUNKS)return json({error:'Ungültige Archivteil-Anforderung.',version:WORKER_VERSION},400,{'cache-control':'no-store'});
 const chunk=await env.MID_PUSH_SUBSCRIPTIONS.get(await deviceSyncArchiveChunkKey(syncKey,revision,index)).catch(()=>null);if(!chunk)return json({error:'Wetterzwilling-Archivteil nicht gefunden.',version:WORKER_VERSION},404,{'cache-control':'no-store'});
 return json({ok:true,chunk,index,version:WORKER_VERSION},200,{'cache-control':'no-store'});
}


function netatmoMissing(env){return[['MID_PUSH_SUBSCRIPTIONS',env?.MID_PUSH_SUBSCRIPTIONS],['NETATMO_CLIENT_ID',env?.NETATMO_CLIENT_ID],['NETATMO_CLIENT_SECRET',env?.NETATMO_CLIENT_SECRET],['MID_STATION_TOKEN_KEY',env?.MID_STATION_TOKEN_KEY]].filter(([,value])=>!value).map(([name])=>name)}
function netatmoConfigured(env){return netatmoMissing(env).length===0}
function validStationConnectionId(value){const id=String(value||'').replace(/[^A-Za-z0-9_-]/g,'');return id.length>=24&&id.length<=180?id:''}
function netatmoRedirectUri(request){const url=new URL(request.url);url.search='';url.searchParams.set('mode','netatmo-callback');return url.toString()}
function stationReturnUrl(value,request,env){try{const url=new URL(String(value||'')),origin=url.origin,requestOrigin=String(request.headers.get('origin')||'');if(!/^https?:$/.test(url.protocol))return'';if(/^https?:\/\/(?:localhost|127\.0\.0\.1)(?::\d+)?$/i.test(origin))return url.toString();if(origin===requestOrigin||pushOrigins(env).includes(origin))return url.toString()}catch{}return''}
async function stationTokenKey(env){const digest=await crypto.subtle.digest('SHA-256',PUSH_ENCODER.encode(String(env.MID_STATION_TOKEN_KEY)));return crypto.subtle.importKey('raw',digest,{name:'AES-GCM'},false,['encrypt','decrypt'])}
async function stationEncrypt(env,value){const iv=crypto.getRandomValues(new Uint8Array(12)),key=await stationTokenKey(env),cipher=new Uint8Array(await crypto.subtle.encrypt({name:'AES-GCM',iv},key,PUSH_ENCODER.encode(JSON.stringify(value))));return{iv:pushB64Encode(iv),data:pushB64Encode(cipher)}}
async function stationDecrypt(env,blob){const key=await stationTokenKey(env),plain=await crypto.subtle.decrypt({name:'AES-GCM',iv:pushB64Decode(blob.iv)},key,pushB64Decode(blob.data));return JSON.parse(new TextDecoder().decode(plain))}
async function netatmoStorageKey(connectionId){return`netatmo-token:${await pushHash(`mid-netatmo:${connectionId}`)}`}
async function netatmoOAuthResultKey(connectionId){return`netatmo-oauth-result:${await pushHash(`mid-netatmo-oauth-result:${connectionId}`)}`}
async function netatmoSaveOAuthResult(connectionId,result,env){if(!connectionId)return;await env.MID_PUSH_SUBSCRIPTIONS.put(await netatmoOAuthResultKey(connectionId),JSON.stringify({...result,at:new Date().toISOString()}),{expirationTtl:1800}).catch(()=>undefined)}
async function netatmoLoadOAuthResult(connectionId,env){if(!connectionId)return null;return env.MID_PUSH_SUBSCRIPTIONS.get(await netatmoOAuthResultKey(connectionId),{type:'json'}).catch(()=>null)}
async function netatmoLoadToken(connectionId,env){const entry=await env.MID_PUSH_SUBSCRIPTIONS.get(await netatmoStorageKey(connectionId),{type:'json'}).catch(()=>null);if(!entry?.blob)return null;try{return await stationDecrypt(env,entry.blob)}catch{return null}}
async function netatmoSaveToken(connectionId,token,env){await env.MID_PUSH_SUBSCRIPTIONS.put(await netatmoStorageKey(connectionId),JSON.stringify({schema:'mid-netatmo-token',version:1,updatedAt:new Date().toISOString(),blob:await stationEncrypt(env,token)}),{expirationTtl:365*24*3600})}
async function netatmoTokenRequest(params){const response=await fetchWithDeadline('https://api.netatmo.com/oauth2/token',{method:'POST',headers:{Accept:'application/json','Content-Type':'application/x-www-form-urlencoded'},body:new URLSearchParams(params)},12000),body=await response.text();let data={};try{data=JSON.parse(body)}catch{}if(!response.ok||data?.error)throw new Error(data?.error_description||data?.error||`Netatmo OAuth HTTP ${response.status}`);return data}
async function netatmoAccessToken(connectionId,env){let token=await netatmoLoadToken(connectionId,env);if(!token)return null;if(Number(token.expiresAt)>Date.now()+120000)return token;if(!token.refresh_token)return null;const refreshed=await netatmoTokenRequest({grant_type:'refresh_token',refresh_token:String(token.refresh_token),client_id:String(env.NETATMO_CLIENT_ID),client_secret:String(env.NETATMO_CLIENT_SECRET)}),next={...token,...refreshed,refresh_token:refreshed.refresh_token||token.refresh_token,expiresAt:Date.now()+Math.max(300,Number(refreshed.expires_in)||10800)*1000};await netatmoSaveToken(connectionId,next,env);return next}
function netatmoModuleKind(module){const type=String(module?.type||'');if(type==='NAModule1')return'outdoor';if(type==='NAModule2')return'wind';if(type==='NAModule3')return'rain';if(type==='NAModule4')return'indoor';const dash=module?.dashboard_data||{};return number(dash.Temperature)!==undefined&&(number(dash.CO2)!==undefined||number(dash.Humidity)!==undefined)?'indoor':'other'}
function netatmoDevices(payload){const devices=Array.isArray(payload?.body?.devices)?payload.body.devices:[];return devices.map(device=>({id:String(device?._id||''),name:String(device?.station_name||device?.module_name||'Netatmo Wetterstation'),latitude:number(device?.place?.location?.[1]),longitude:number(device?.place?.location?.[0]),elevation:number(device?.place?.altitude),modules:(Array.isArray(device?.modules)?device.modules:[]).map(module=>({id:String(module?._id||''),name:String(module?.module_name||module?.type||'Modul'),type:String(module?.type||''),kind:netatmoModuleKind(module)}))})).filter(device=>device.id)}
function netatmoEnvironment(payload,deviceId){const devices=Array.isArray(payload?.body?.devices)?payload.body.devices:[],device=devices.find(item=>String(item?._id)===String(deviceId||''))||devices[0];if(!device)return null;const modules=Array.isArray(device.modules)?device.modules:[],sensor=(source,kind,name)=>{const dash=source?.dashboard_data||{},stamp=number(dash.time_utc);return{id:String(source?._id||''),name:String(source?.module_name||name),kind,temperature:number(dash.Temperature),humidity:number(dash.Humidity),co2:number(dash.CO2),noise:number(dash.Noise),pressure:number(dash.Pressure),timestamp:Number.isFinite(stamp)?new Date(stamp*1000).toISOString():undefined}},main=sensor(device,'indoor',device.station_name||'Innen'),rooms=[main,...modules.filter(module=>netatmoModuleKind(module)==='indoor').map(module=>sensor(module,'indoor',module.module_name||'Raum'))].filter(row=>row.id&&(row.temperature!==undefined||row.humidity!==undefined||row.co2!==undefined)),outdoorModule=modules.find(module=>netatmoModuleKind(module)==='outdoor'),outdoor=outdoorModule?sensor(outdoorModule,'outdoor',outdoorModule.module_name||'Außen'):undefined,times=[...rooms.map(row=>Date.parse(String(row.timestamp||''))),Date.parse(String(outdoor?.timestamp||''))].filter(Number.isFinite);return{provider:'Netatmo',stationName:String(device.station_name||'Netatmo Wetterstation'),latitude:number(device?.place?.location?.[1]),longitude:number(device?.place?.location?.[0]),elevation:number(device?.place?.altitude),outdoor,rooms,updatedAt:times.length?new Date(Math.max(...times)).toISOString():new Date().toISOString()}}
function netatmoObservation(payload,deviceId,moduleId){const devices=Array.isArray(payload?.body?.devices)?payload.body.devices:[],device=devices.find(item=>String(item?._id)===String(deviceId))||devices[0];if(!device)return null;const modules=Array.isArray(device.modules)?device.modules:[],outdoor=modules.find(item=>String(item?._id)===String(moduleId))||modules.find(item=>item?.type==='NAModule1')||modules.find(item=>number(item?.dashboard_data?.Temperature)!==undefined),wind=modules.find(item=>item?.type==='NAModule2'),rain=modules.find(item=>item?.type==='NAModule3'),dash=outdoor?.dashboard_data||{},windDash=wind?.dashboard_data||{},rainDash=rain?.dashboard_data||{},main=device.dashboard_data||{},timestamps=[dash.time_utc,windDash.time_utc,rainDash.time_utc,main.time_utc].map(number).filter(Number.isFinite),timestamp=timestamps.length?new Date(Math.max(...timestamps)*1000).toISOString():new Date().toISOString(),windSpeed=number(windDash.WindStrength),windGust=number(windDash.GustStrength);return{name:String(device.station_name||'Eigene Netatmo-Wetterstation'),provider:'Eigene Netatmo-Wetterstation',stationId:String(device._id||''),latitude:number(device?.place?.location?.[1]),longitude:number(device?.place?.location?.[0]),height:number(device?.place?.altitude),timestamp,temperature:number(dash.Temperature),humidity:number(dash.Humidity),pressure:number(main.Pressure),pressureReference:number(main.Pressure)!==undefined?'station':undefined,windSpeed:windSpeed===undefined?undefined:windSpeed/1.852,windGust:windGust===undefined?undefined:windGust/1.852,windDirection:number(windDash.WindAngle),windUnit:'kt',precipitation:number(rainDash.Rain),networkClass:'pws',analysisMethod:'Eigene Netatmo-Wetterstation · plausibilisiert',sourceProviders:['Netatmo Weather API']}}
async function netatmoData(connectionId,env){const token=await netatmoAccessToken(connectionId,env);if(!token?.access_token)throw new Error('Netatmo-Verbindung ist nicht autorisiert.');const response=await fetchWithDeadline('https://api.netatmo.com/api/getstationsdata',{headers:{Accept:'application/json',Authorization:`Bearer ${token.access_token}`}},12000),body=await response.text();let data={};try{data=JSON.parse(body)}catch{}if(response.status===401&&token.refresh_token){token.expiresAt=0;await netatmoSaveToken(connectionId,token,env);const refreshed=await netatmoAccessToken(connectionId,env);if(refreshed?.access_token){const retry=await fetchWithDeadline('https://api.netatmo.com/api/getstationsdata',{headers:{Accept:'application/json',Authorization:`Bearer ${refreshed.access_token}`}},12000);if(retry.ok)return retry.json()}}if(!response.ok||data?.error)throw new Error(data?.error?.message||data?.error||`Netatmo Weather API HTTP ${response.status}`);return data}
function netatmoSafeDetail(value){return String(value||'').replace(/[\r\n<>]/g,' ').replace(/\s+/g,' ').trim().slice(0,240)}
function netatmoRedirectResponse(target){return new Response(null,{status:302,headers:{Location:String(target),'Cache-Control':'no-store, no-cache, must-revalidate, max-age=0','Pragma':'no-cache','Expires':'0','Referrer-Policy':'no-referrer'}})}
function netatmoHtmlEscape(value){return String(value??'').replace(/[&<>\"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[char]||char))}
function netatmoCallbackPage(returnTarget,{ok,title,detail=''}){const safeTarget=netatmoHtmlEscape(returnTarget),safeTitle=netatmoHtmlEscape(title),safeDetail=netatmoHtmlEscape(detail);return new Response(`<!doctype html><html lang="de"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover"><meta name="color-scheme" content="light dark"><title>MID · Netatmo</title><style>body{margin:0;font-family:system-ui,-apple-system,BlinkMacSystemFont,"SF Pro Text",sans-serif;background:#eef4f7;color:#12273a;min-height:100dvh;display:grid;place-items:center;padding:24px;box-sizing:border-box}.card{max-width:560px;width:100%;background:#fff;border:1px solid #d7e3eb;border-radius:24px;padding:24px;box-sizing:border-box;box-shadow:0 16px 48px #10233818}.status{font-size:13px;font-weight:800;color:${ok?'#168a61':'#b83d3d'};margin-bottom:10px}h1{font-size:24px;margin:0 0 10px}p{color:#617386;line-height:1.5;margin:0 0 18px}.button{display:block;text-align:center;text-decoration:none;background:#087eea;color:white;padding:14px 18px;border-radius:14px;font-weight:800}@media(prefers-color-scheme:dark){body{background:#07111f;color:#f4f8fc}.card{background:#0e2034;border-color:#29415a}p{color:#a8bacb}}</style></head><body><main class="card"><div class="status">${ok?'VERBUNDEN':'NICHT VERBUNDEN'}</div><h1>${safeTitle}</h1>${safeDetail?`<p>${safeDetail}</p>`:''}<a class="button" href="${safeTarget}">Zurück zu MID</a></main></body></html>`,{status:200,headers:{'content-type':'text/html; charset=utf-8','cache-control':'no-store, no-cache, must-revalidate','referrer-policy':'no-referrer','x-frame-options':'DENY'}})}
async function netatmoPrepareAuthorization(request,env,connectionIdValue,returnUrlValue){if(!netatmoConfigured(env))throw Object.assign(new Error('Netatmo ist im Worker noch nicht konfiguriert.'),{status:503});const connectionId=validStationConnectionId(connectionIdValue),returnUrl=stationReturnUrl(returnUrlValue,request,env);if(!connectionId||!returnUrl)throw Object.assign(new Error('Ungültige Stationskennung oder Rücksprungadresse.'),{status:400});const state=pushB64Encode(crypto.getRandomValues(new Uint8Array(32))),stateKey=`netatmo-state:${await pushHash(state)}`;await env.MID_PUSH_SUBSCRIPTIONS.put(stateKey,JSON.stringify({connectionId,returnUrl,createdAt:Date.now()}),{expirationTtl:900});const authorize=new URL('https://api.netatmo.com/oauth2/authorize');authorize.searchParams.set('client_id',String(env.NETATMO_CLIENT_ID));authorize.searchParams.set('redirect_uri',netatmoRedirectUri(request));authorize.searchParams.set('response_type','code');authorize.searchParams.set('scope','read_station');authorize.searchParams.set('state',state);return{authorizeUrl:authorize.toString(),redirectUri:netatmoRedirectUri(request)}}
async function netatmoAuthStart(request,env){if(!pushOriginAllowed(request,env))return json({error:'Nicht freigegebener Ursprung.'},403,{'cache-control':'no-store'});const body=await request.json().catch(()=>null);try{return json({...await netatmoPrepareAuthorization(request,env,body?.connectionId,body?.returnUrl),version:WORKER_VERSION},200,{'cache-control':'no-store'})}catch(error){return json({error:error instanceof Error?error.message:String(error),requires:netatmoMissing(env),version:WORKER_VERSION},Number(error?.status)||500,{'cache-control':'no-store'})}}
async function netatmoAuthRedirect(request,env){const url=new URL(request.url);try{const prepared=await netatmoPrepareAuthorization(request,env,url.searchParams.get('connectionId'),url.searchParams.get('returnUrl'));return netatmoRedirectResponse(prepared.authorizeUrl)}catch(error){const fallback=stationReturnUrl(url.searchParams.get('returnUrl'),request,env)||pushOrigins(env)[0]||PUSH_DEFAULT_ORIGIN;try{const target=new URL(fallback);target.searchParams.set('mid_station','netatmo-error');target.searchParams.set('mid_station_stage','authorize');target.searchParams.set('mid_station_detail',netatmoSafeDetail(error instanceof Error?error.message:String(error)));return netatmoRedirectResponse(target.toString())}catch{return json({error:error instanceof Error?error.message:String(error),version:WORKER_VERSION},Number(error?.status)||500,{'cache-control':'no-store'})}}}
async function netatmoCallback(request,env){const url=new URL(request.url),state=String(url.searchParams.get('state')||''),code=String(url.searchParams.get('code')||''),error=String(url.searchParams.get('error')||''),errorDescription=String(url.searchParams.get('error_description')||''),stateKey=`netatmo-state:${await pushHash(state)}`,entry=state?await env.MID_PUSH_SUBSCRIPTIONS.get(stateKey,{type:'json'}).catch(()=>null):null;if(state)await env.MID_PUSH_SUBSCRIPTIONS.delete(stateKey).catch(()=>undefined);const fallback=pushOrigins(env)[0]||PUSH_DEFAULT_ORIGIN,returnUrl=entry?.returnUrl||fallback,buildTarget=(result,stage,detail)=>{const target=new URL(returnUrl);target.searchParams.set('mid_station',result);if(stage)target.searchParams.set('mid_station_stage',stage);if(entry?.connectionId)target.searchParams.set('mid_station_connection',String(entry.connectionId));if(detail)target.searchParams.set('mid_station_detail',netatmoSafeDetail(detail));return target.toString()},finishFailure=async(stage,detail,denied=false)=>{const safe=netatmoSafeDetail(detail),result=denied?'denied':'error';if(entry?.connectionId)await netatmoSaveOAuthResult(entry.connectionId,{result,stage,detail:safe},env);return netatmoCallbackPage(buildTarget(denied?'netatmo-denied':'netatmo-error',stage,safe),{ok:false,title:denied?'Netatmo-Freigabe abgebrochen':'Netatmo-Verbindung nicht abgeschlossen',detail:safe||'Die Autorisierung konnte nicht abgeschlossen werden.'})};if(error)return finishFailure('authorize',errorDescription||error,error==='access_denied');if(!entry?.connectionId)return finishFailure('state','Die OAuth-Sitzung ist abgelaufen oder konnte nicht zugeordnet werden.');if(!code)return finishFailure('authorize','Netatmo hat keinen Autorisierungscode zurückgegeben.');try{const token=await netatmoTokenRequest({grant_type:'authorization_code',client_id:String(env.NETATMO_CLIENT_ID),client_secret:String(env.NETATMO_CLIENT_SECRET),code,redirect_uri:netatmoRedirectUri(request)});await netatmoSaveToken(entry.connectionId,{...token,expiresAt:Date.now()+Math.max(300,Number(token.expires_in)||10800)*1000},env);await netatmoSaveOAuthResult(entry.connectionId,{result:'connected',stage:'token'},env);return netatmoCallbackPage(buildTarget('netatmo-connected','',''),{ok:true,title:'Netatmo wurde mit MID verbunden',detail:'Die Freigabe wurde übernommen. Du kannst jetzt zu MID zurückkehren; Station und Innenraumsensoren werden dort automatisch eingelesen.'})}catch(error){return finishFailure('token',error instanceof Error?error.message:String(error))}}
async function netatmoStatus(request,env){if(!netatmoConfigured(env))return json({configured:false,connected:false,message:'Netatmo ist im Worker nicht vollständig konfiguriert.',missing:netatmoMissing(env),requires:['MID_PUSH_SUBSCRIPTIONS','NETATMO_CLIENT_ID','NETATMO_CLIENT_SECRET','MID_STATION_TOKEN_KEY'],redirectUri:netatmoRedirectUri(request),version:WORKER_VERSION},200,{'cache-control':'no-store'});if(!pushOriginAllowed(request,env))return json({error:'Nicht freigegebener Ursprung.'},403,{'cache-control':'no-store'});const body=await request.json().catch(()=>null),connectionId=validStationConnectionId(body?.connectionId);if(!connectionId)return json({error:'Ungültige Stationskennung.'},400,{'cache-control':'no-store'});const oauthResult=await netatmoLoadOAuthResult(connectionId,env),token=await netatmoLoadToken(connectionId,env);if(!token)return json({configured:true,connected:false,provider:'Netatmo',oauthResult,redirectUri:netatmoRedirectUri(request),version:WORKER_VERSION},200,{'cache-control':'no-store'});try{const data=await netatmoData(connectionId,env);return json({configured:true,connected:true,provider:'Netatmo',oauthResult,devices:netatmoDevices(data),environment:netatmoEnvironment(data,body?.deviceId),redirectUri:netatmoRedirectUri(request),version:WORKER_VERSION},200,{'cache-control':'no-store'})}catch(error){return json({configured:true,connected:false,provider:'Netatmo',oauthResult,message:error instanceof Error?error.message:String(error),redirectUri:netatmoRedirectUri(request),version:WORKER_VERSION},200,{'cache-control':'no-store'})}}
async function netatmoObservationResponse(request,env){if(!netatmoConfigured(env))return json({error:'Netatmo ist im Worker nicht konfiguriert.'},503,{'cache-control':'no-store'});if(!pushOriginAllowed(request,env))return json({error:'Nicht freigegebener Ursprung.'},403,{'cache-control':'no-store'});const body=await request.json().catch(()=>null),connectionId=validStationConnectionId(body?.connectionId);if(!connectionId)return json({error:'Ungültige Stationskennung.'},400,{'cache-control':'no-store'});try{const data=await netatmoData(connectionId,env),observation=netatmoObservation(data,body?.deviceId,body?.moduleId);if(!observation)return json({error:'Keine Netatmo-Wetterstation gefunden.'},404,{'cache-control':'no-store'});return json({ok:true,observation,devices:netatmoDevices(data),version:WORKER_VERSION},200,{'cache-control':'no-store'})}catch(error){return json({error:error instanceof Error?error.message:String(error),version:WORKER_VERSION},502,{'cache-control':'no-store'})}}
async function netatmoDisconnect(request,env){if(!pushOriginAllowed(request,env))return json({error:'Nicht freigegebener Ursprung.'},403,{'cache-control':'no-store'});const body=await request.json().catch(()=>null),connectionId=validStationConnectionId(body?.connectionId);if(!connectionId)return json({error:'Ungültige Stationskennung.'},400,{'cache-control':'no-store'});await env.MID_PUSH_SUBSCRIPTIONS?.delete(await netatmoStorageKey(connectionId));return json({ok:true,version:WORKER_VERSION},200,{'cache-control':'no-store'})}

function validVentilationTime(value,fallback){const raw=String(value||'');return/^([01]\d|2[0-3]):[0-5]\d$/.test(raw)?raw:fallback}
function validVentilationRoomRules(value){return(Array.isArray(value)?value:[]).map(row=>({roomId:String(row?.roomId||'').slice(0,120),enabled:row?.enabled!==false,earliest:validVentilationTime(row?.earliest,'00:00'),latest:validVentilationTime(row?.latest,'23:59'),minMinutes:clamp(Math.round(Number(row?.minMinutes)||15),10,45),maxMinutes:clamp(Math.round(Number(row?.maxMinutes)||30),15,60)})).filter(row=>row.roomId&&row.enabled)}
function validVentilationPushConfig(value){if(!value?.enabled)return null;const connectionId=validStationConnectionId(value?.connectionId),roomRules=validVentilationRoomRules(value?.roomRules);return connectionId&&roomRules.length?{enabled:true,connectionId,deviceId:String(value?.deviceId||'').slice(0,120),roomRules}:null}
function ventilationClockMinutes(value){const match=String(value||'').match(/(\d{2}):(\d{2})/);return match?Number(match[1])*60+Number(match[2]):0}
function ventilationAllowedAt(time,earliest,latest){const now=ventilationClockMinutes(String(time).slice(11,16)),start=ventilationClockMinutes(earliest),end=ventilationClockMinutes(latest);return start<=end?now>=start&&now<=end:now>=start||now<=end}
function ventilationAbsoluteHumidity(temperature,humidity){const t=number(temperature),rh=number(humidity);if(t===undefined||rh===undefined)return undefined;const saturation=6.112*Math.exp(17.67*t/(t+243.5)),vapour=saturation*clamp(rh,0,100)/100;return 216.7*vapour/(273.15+t)}
function ventilationEndLabel(startLabel,duration){const total=(ventilationClockMinutes(startLabel)+Math.round(duration))%(24*60);return`${String(Math.floor(total/60)).padStart(2,'0')}:${String(total%60).padStart(2,'0')}`}
function ventilationWeatherSafe(row){const code=Number(row.weatherCode)||0,pop=number(row.precipitationProbability)||0,precip=number(row.precipitation)||0,gust=number(row.gustKmh)||0;return![95,96,99].includes(code)&&pop<=55&&precip<.4&&gust<=45}
async function ventilationForecast(latitude,longitude){const url=new URL(OPEN_METEO_FORECAST);url.searchParams.set('latitude',String(latitude));url.searchParams.set('longitude',String(longitude));url.searchParams.set('timezone','auto');url.searchParams.set('forecast_days','2');url.searchParams.set('hourly','temperature_2m,relative_humidity_2m,precipitation_probability,precipitation,weather_code,wind_gusts_10m');url.searchParams.set('wind_speed_unit','kmh');const data=await fetchJson(url.toString()),hourly=data?.hourly||{},times=Array.isArray(hourly.time)?hourly.time:[],offsetSeconds=number(data?.utc_offset_seconds)||0,now=Date.now();const rows=times.map((time,index)=>{const epoch=Date.parse(`${time}:00Z`)-offsetSeconds*1000;return{time:String(time),epoch,leadHours:(epoch-now)/3600000,temperature:number(hourly.temperature_2m?.[index]),humidity:number(hourly.relative_humidity_2m?.[index]),precipitationProbability:number(hourly.precipitation_probability?.[index]),precipitation:number(hourly.precipitation?.[index]),weatherCode:number(hourly.weather_code?.[index]),gustKmh:number(hourly.wind_gusts_10m?.[index])}}).filter(row=>Number.isFinite(row.epoch)&&row.leadHours>=-.75&&row.leadHours<=24.5);return{timezone:String(data?.timezone||''),offsetSeconds,rows}}
function ventilationRoomAdvice(room,rule,forecast){const timestamp=Date.parse(String(room?.timestamp||'')),sensorAgeMinutes=Number.isFinite(timestamp)?Math.max(0,(Date.now()-timestamp)/60000):Infinity,temperature=number(room?.temperature),humidity=number(room?.humidity),co2=number(room?.co2),indoorAbsoluteHumidity=ventilationAbsoluteHumidity(temperature,humidity);if(sensorAgeMinutes>90)return{roomId:rule.roomId,name:String(room?.name||'Raum'),status:'wait',summary:'Der Innenraumsensor ist für eine belastbare Lüftungsempfehlung zu alt.',reasons:['Innenraummessung älter als 90 Minuten'],temperature,humidity,co2,indoorAbsoluteHumidity};const needsCooling=temperature!==undefined&&temperature>=22.5,needsDrying=humidity!==undefined&&humidity>=60,needsAir=co2!==undefined&&co2>=900;if(!needsCooling&&!needsDrying&&!needsAir)return{roomId:rule.roomId,name:String(room?.name||'Raum'),status:'not-needed',summary:'Raumklima benötigt derzeit keine bevorzugte Lüftungsphase.',reasons:['Temperatur, Feuchte und CO₂ unauffällig'],temperature,humidity,co2,indoorAbsoluteHumidity};let best=null;for(const row of forecast.rows){if(!ventilationAllowedAt(row.time,rule.earliest,rule.latest)||!ventilationWeatherSafe(row))continue;const outdoorAbsoluteHumidity=ventilationAbsoluteHumidity(row.temperature,row.humidity),coolingGain=temperature!==undefined&&row.temperature!==undefined?temperature-row.temperature:0,dryingGain=indoorAbsoluteHumidity!==undefined&&outdoorAbsoluteHumidity!==undefined?indoorAbsoluteHumidity-outdoorAbsoluteHumidity:0,beneficial=needsAir||needsCooling&&coolingGain>=1.5||needsDrying&&dryingGain>=.7;if(!beneficial)continue;let score=Math.max(0,coolingGain)*2.2+Math.max(0,dryingGain)*3+(co2!==undefined&&co2>=1200?10:needsAir?5:0)-(row.precipitationProbability||0)*.04-(row.gustKmh||0)*.04-Math.max(0,row.leadHours)*.08;if(needsCooling&&coolingGain<1.5)score-=5;if(needsDrying&&dryingGain<.7)score-=5;if(!best||score>best.score)best={row,score,coolingGain,dryingGain,outdoorAbsoluteHumidity}}if(!best)return{roomId:rule.roomId,name:String(room?.name||'Raum'),status:'wait',summary:'Im zulässigen Zeitraum gibt es in den nächsten 24 Stunden kein ausreichend günstiges Wetterfenster.',reasons:['Regen, Gewitter, Böen oder ungünstige Außenluft verhindern eine Empfehlung'],temperature,humidity,co2,indoorAbsoluteHumidity};const row=best.row,startLabel=row.time.slice(11,16),duration=clamp(Math.round(15+(needsAir?5:0)+(best.coolingGain>=5?5:0)+(best.dryingGain>=2?5:0)),rule.minMinutes,rule.maxMinutes),status=row.leadHours<=.45?'open':'later',reasons=[];if(best.coolingGain>=1.5)reasons.push(`außen ${best.coolingGain.toFixed(1)} K kühler`);if(best.dryingGain>=.7)reasons.push('Außenluft absolut trockener');if(needsAir)reasons.push(`CO₂ ${Math.round(co2)} ppm`);if((row.precipitationProbability||0)<=25)reasons.push('Regenrisiko niedrig');const summary=status==='open'?`Jetzt ist ein günstiges Lüftungsfenster für etwa ${duration} Minuten.`:`Bestes zulässiges Fenster ab ${startLabel} Uhr für etwa ${duration} Minuten.`;return{roomId:rule.roomId,name:String(room?.name||'Raum'),status,start:row.time,end:ventilationEndLabel(startLabel,duration),startLabel,durationMinutes:duration,summary,reasons,temperature,humidity,co2,indoorAbsoluteHumidity,outdoorTemperature:row.temperature,outdoorHumidity:row.humidity,outdoorAbsoluteHumidity:best.outdoorAbsoluteHumidity,precipitationProbability:row.precipitationProbability,gustKmh:row.gustKmh,leadHours:row.leadHours,windowKey:`${rule.roomId}:${row.time}`}}
async function ventilationAdvice(connectionId,roomRules,env,netatmoCache,forecastCache,deviceId){let dataPromise=netatmoCache?.get(connectionId);if(!dataPromise){dataPromise=netatmoData(connectionId,env);netatmoCache?.set(connectionId,dataPromise)}const data=await dataPromise,environment=netatmoEnvironment(data,deviceId);if(!environment)throw new Error('Netatmo liefert keine Innenraumsensoren.');const latitude=number(environment.latitude),longitude=number(environment.longitude);if(latitude===undefined||longitude===undefined)throw new Error('Netatmo-Station besitzt keine Standortkoordinaten.');const forecastKey=`${latitude.toFixed(4)}:${longitude.toFixed(4)}`;let forecastPromise=forecastCache?.get(forecastKey);if(!forecastPromise){forecastPromise=ventilationForecast(latitude,longitude);forecastCache?.set(forecastKey,forecastPromise)}const forecast=await forecastPromise,roomsById=new Map(environment.rooms.map(room=>[String(room.id),room])),rules=validVentilationRoomRules(roomRules),rooms=rules.map(rule=>{const room=roomsById.get(rule.roomId);return room?ventilationRoomAdvice(room,rule,forecast):{roomId:rule.roomId,name:'Raum',status:'wait',summary:'Der zugeordnete Innenraumsensor ist aktuell nicht verfügbar.',reasons:['Sensor nicht erreichbar']}});return{ok:true,generatedAt:new Date().toISOString(),provider:'Netatmo + MID Wetterprognose',stationName:environment.stationName,timezone:forecast.timezone,rooms,outdoor:environment.outdoor}}
async function ventilationAdviceResponse(request,env){if(!netatmoConfigured(env))return json({error:'Netatmo ist im Worker nicht konfiguriert.',version:WORKER_VERSION},503,{'cache-control':'no-store'});if(!pushOriginAllowed(request,env))return json({error:'Nicht freigegebener Ursprung.',version:WORKER_VERSION},403,{'cache-control':'no-store'});const body=await request.json().catch(()=>null),connectionId=validStationConnectionId(body?.connectionId),deviceId=String(body?.deviceId||'').slice(0,120),roomRules=validVentilationRoomRules(body?.roomRules);if(!connectionId||!roomRules.length)return json({error:'Stationsverbindung und mindestens ein Raum sind erforderlich.',version:WORKER_VERSION},400,{'cache-control':'no-store'});try{return json({...await ventilationAdvice(connectionId,roomRules,env,undefined,undefined,deviceId),version:WORKER_VERSION},200,{'cache-control':'no-store'})}catch(error){return json({error:error instanceof Error?error.message:String(error),version:WORKER_VERSION},502,{'cache-control':'no-store'})}}


const FLIGHT_PROFILE_LEVELS=[1000,975,950,925,900,850,800,700,600,500,400,300,250,200,150,100];
const FLIGHT_MODELS=new Set(['best_match','dwd_icon_eu','ecmwf_ifs','ncep_gfs025','dwd_icon']);
const FLIGHT_CROSS_SECTION_ENABLED=false;
function flightRouteCodes(value){const codes=String(value||'').toUpperCase().split(/[^A-Z0-9]+/).map(item=>item.trim()).filter(Boolean);if(codes.length<2||codes.length>8||codes.some(code=>!/^[A-Z]{4}$/.test(code)))throw new Error('Route muss aus 2 bis 8 gültigen ICAO-Kennungen bestehen.');return codes}
function flightAirportRows(payload){if(Array.isArray(payload))return payload;if(Array.isArray(payload?.data))return payload.data;if(Array.isArray(payload?.features))return payload.features.map(feature=>({...feature?.properties,lat:feature?.geometry?.coordinates?.[1],lon:feature?.geometry?.coordinates?.[0]}));return[]}
function flightAirportId(row){return String(row?.icaoId??row?.icao_id??row?.id??row?.ident??row?.stationId??row?.station_id??'').toUpperCase()}
function flightAirportPoint(row,id){const latitude=number(row?.lat??row?.latitude),longitude=number(row?.lon??row?.longitude),elevationFeet=number(row?.elev??row?.elevation??row?.elevation_ft),elevationMetres=number(row?.elevation_m??row?.elev_m);if(latitude===undefined||longitude===undefined)return null;return{id,name:String(row?.name??row?.site??row?.airportName??row?.stationName??id),latitude,longitude,elevation:elevationMetres??(elevationFeet===undefined?0:elevationFeet*.3048)}}
async function flightResolveAirports(codes){
 const ids=codes.join(','),urls=[`https://aviationweather.gov/api/data/airport?ids=${encodeURIComponent(ids)}&format=json`,`https://aviationweather.gov/api/data/stationinfo?ids=${encodeURIComponent(ids)}&format=json`],byId=new Map();
 for(const url of urls){try{const response=await fetchWithDeadline(url,{headers:{Accept:'application/json','User-Agent':'MID Weather Dashboard/ICAO location lookup'}},12000);if(response.status===204)continue;if(!response.ok)continue;const payload=await response.json();for(const row of flightAirportRows(payload)){const id=flightAirportId(row);if(!id||byId.has(id))continue;const point=flightAirportPoint(row,id);if(point)byId.set(id,point)}if(codes.every(code=>byId.has(code)))break}catch{}}
 const missing=codes.filter(code=>!byId.has(code));if(missing.length)throw new Error(`ICAO-Kennung${missing.length===1?'':'en'} nicht auflösbar: ${missing.join(', ')}.`);return codes.map(code=>byId.get(code));
}
async function icaoLocation(u){
 const code=String(u.searchParams.get('icao')||'').trim().toUpperCase();if(!/^[A-Z]{4}$/.test(code))throw new Error('Ungültiger ICAO Location Indicator.');const airport=(await flightResolveAirports([code]))[0];return{id:900000000+Number.parseInt(code,36),name:airport.name,latitude:airport.latitude,longitude:airport.longitude,elevation:airport.elevation,icao:code,source:'NOAA AviationWeather / ICAO',poiType:'airport',poiCategory:'Flughafen'};
}
function flightRadians(value){return value*Math.PI/180}
function flightDegrees(value){return value*180/Math.PI}
function flightGreatCircle(a,b,fraction){const lat1=flightRadians(a.latitude),lon1=flightRadians(a.longitude),lat2=flightRadians(b.latitude),lon2=flightRadians(b.longitude),delta=2*Math.asin(Math.sqrt(Math.sin((lat2-lat1)/2)**2+Math.cos(lat1)*Math.cos(lat2)*Math.sin((lon2-lon1)/2)**2));if(delta<1e-8)return{latitude:a.latitude,longitude:a.longitude};const sinDelta=Math.sin(delta),wa=Math.sin((1-fraction)*delta)/sinDelta,wb=Math.sin(fraction*delta)/sinDelta,x=wa*Math.cos(lat1)*Math.cos(lon1)+wb*Math.cos(lat2)*Math.cos(lon2),y=wa*Math.cos(lat1)*Math.sin(lon1)+wb*Math.cos(lat2)*Math.sin(lon2),z=wa*Math.sin(lat1)+wb*Math.sin(lat2);return{latitude:flightDegrees(Math.atan2(z,Math.sqrt(x*x+y*y))),longitude:flightDegrees(Math.atan2(y,x))}}
function flightRouteGeometry(airports){
 const segmentKm=[];let totalDistanceKm=0;for(let index=1;index<airports.length;index++){const km=distance(airports[index-1].latitude,airports[index-1].longitude,airports[index].latitude,airports[index].longitude)/1000;segmentKm.push(km);totalDistanceKm+=km}if(totalDistanceKm<1)throw new Error('Die gewählte Route ist zu kurz.');
 const waypointFractions=[];let accumulated=0;for(let index=0;index<airports.length;index++){if(index>0)accumulated+=segmentKm[index-1];waypointFractions.push(accumulated/totalDistanceKm)}
 return{segmentKm,totalDistanceKm,waypoints:airports.map((airport,index)=>({...airport,fraction:waypointFractions[index],distanceKm:waypointFractions[index]*totalDistanceKm}))};
}
function flightRoutePointAtDistance(airports,segmentKm,totalDistanceKm,targetKm){
 const target=Math.max(0,Math.min(totalDistanceKm,targetKm));let passed=0,segmentIndex=0;for(;segmentIndex<segmentKm.length-1;segmentIndex++){if(target<=passed+segmentKm[segmentIndex])break;passed+=segmentKm[segmentIndex]}const local=Math.max(0,Math.min(1,(target-passed)/Math.max(.001,segmentKm[segmentIndex]))),position=flightGreatCircle(airports[segmentIndex],airports[segmentIndex+1],local);return{...position,fraction:totalDistanceKm<=0?0:target/totalDistanceKm,distanceKm:target};
}
function flightRouteSamples(airports,count){
 const geometry=flightRouteGeometry(airports),points=[];for(let index=0;index<count;index++){const fraction=index/(count-1),target=fraction*geometry.totalDistanceKm;points.push(flightRoutePointAtDistance(airports,geometry.segmentKm,geometry.totalDistanceKm,target))}
 return{...geometry,points};
}
async function flightTerrainProfile(airports,route){
 const terrainSamples=Math.max(181,Math.min(361,Math.round(route.totalDistanceKm<700?route.totalDistanceKm/3.5:route.totalDistanceKm/6)))|1,profile=[];
 for(let index=0;index<terrainSamples;index++){const fraction=index/(terrainSamples-1),target=fraction*route.totalDistanceKm;profile.push(flightRoutePointAtDistance(airports,route.segmentKm,route.totalDistanceKm,target))}
 const elevations=[];for(let offset=0;offset<profile.length;offset+=80){const chunk=profile.slice(offset,offset+80),url=new URL(OPEN_METEO_ELEVATION);url.searchParams.set('latitude',chunk.map(point=>point.latitude.toFixed(5)).join(','));url.searchParams.set('longitude',chunk.map(point=>point.longitude.toFixed(5)).join(','));const response=await fetchWithDeadline(url.toString(),{headers:{Accept:'application/json'}},18000),payload=await response.json().catch(()=>({}));if(!response.ok)throw new Error(payload?.reason||payload?.error||`Open-Meteo Elevation HTTP ${response.status}`);const list=Array.isArray(payload?.elevation)?payload.elevation:Array.isArray(payload?.elevations)?payload.elevations:[];for(const value of list)elevations.push(number(value)??0)}
 return profile.map((point,index)=>({...point,elevation:number(elevations[index])??0}));
}
function flightNearestIndex(times,target){let best=0,bestDelta=Infinity;for(let index=0;index<times.length;index++){const parsed=Date.parse(String(times[index]));if(!Number.isFinite(parsed))continue;const delta=Math.abs(parsed-target);if(delta<bestDelta){best=index;bestDelta=delta}}return best}
function flightHourlyValue(hourly,key,index){const value=number(hourly?.[key]?.[index]);return value===undefined?null:value}
async function flightCrossSection(u){
 const codes=flightRouteCodes(u.searchParams.get('route')),airports=await flightResolveAirports(codes),samples=Math.max(17,Math.min(49,Math.round(Number(u.searchParams.get('samples'))||33))),flightLevel=Math.max(0,Math.min(550,Math.round(Number(u.searchParams.get('flight_level'))||100))),modelRaw=String(u.searchParams.get('model')||'best_match'),model=FLIGHT_MODELS.has(modelRaw)?modelRaw:'best_match',startTime=new Date(String(u.searchParams.get('start')||'')),endTime=new Date(String(u.searchParams.get('end')||''));if(!Number.isFinite(startTime.getTime())||!Number.isFinite(endTime.getTime())||endTime<startTime)throw new Error('Ungültiger Start-/Endzeitraum.');if(endTime.getTime()-startTime.getTime()>36*3600000)throw new Error('Der Flugzeitraum darf höchstens 36 Stunden umfassen.');
 const route=flightRouteSamples(airports,samples),variables=['temperature_2m','precipitation',...FLIGHT_PROFILE_LEVELS.flatMap(level=>[`temperature_${level}hPa`,`relative_humidity_${level}hPa`,`cloud_cover_${level}hPa`,`wind_speed_${level}hPa`,`wind_direction_${level}hPa`,`geopotential_height_${level}hPa`])],url=new URL(OPEN_METEO_FORECAST);url.searchParams.set('latitude',route.points.map(point=>point.latitude.toFixed(5)).join(','));url.searchParams.set('longitude',route.points.map(point=>point.longitude.toFixed(5)).join(','));url.searchParams.set('hourly',variables.join(','));url.searchParams.set('start_date',startTime.toISOString().slice(0,10));url.searchParams.set('end_date',endTime.toISOString().slice(0,10));url.searchParams.set('models',model);url.searchParams.set('timezone','GMT');url.searchParams.set('wind_speed_unit','kn');url.searchParams.set('precipitation_unit','mm');url.searchParams.set('cell_selection','nearest');
 const [weatherResponse,terrainProfile]=await Promise.all([fetchWithDeadline(url.toString(),{headers:{Accept:'application/json'}},35000),flightTerrainProfile(airports,route)]),payload=await weatherResponse.json().catch(()=>({}));if(!weatherResponse.ok)throw new Error(payload?.reason||payload?.error||`Open-Meteo HTTP ${weatherResponse.status}`);const forecasts=Array.isArray(payload)?payload:[payload];if(forecasts.length!==route.points.length)throw new Error('Das Wettermodell lieferte keine vollständige Route.');
 const duration=endTime.getTime()-startTime.getTime(),points=route.points.map((point,index)=>{const raw=forecasts[index],hourly=raw?.hourly??{},target=startTime.getTime()+duration*point.fraction,times=Array.isArray(hourly.time)?hourly.time:[],timeIndex=flightNearestIndex(times,target),validTime=String(times[timeIndex]??new Date(target).toISOString()),levels=FLIGHT_PROFILE_LEVELS.map(pressure=>({pressure,height:flightHourlyValue(hourly,`geopotential_height_${pressure}hPa`,timeIndex),temperature:flightHourlyValue(hourly,`temperature_${pressure}hPa`,timeIndex),humidity:flightHourlyValue(hourly,`relative_humidity_${pressure}hPa`,timeIndex),cloud:flightHourlyValue(hourly,`cloud_cover_${pressure}hPa`,timeIndex),windSpeed:flightHourlyValue(hourly,`wind_speed_${pressure}hPa`,timeIndex),windDirection:flightHourlyValue(hourly,`wind_direction_${pressure}hPa`,timeIndex)}));return{...point,elevation:number(raw?.elevation)??0,validTime,precipitation:flightHourlyValue(hourly,'precipitation',timeIndex),levels}});
 const modelLabels={best_match:'Best Match',dwd_icon_eu:'DWD ICON-EU',ecmwf_ifs:'ECMWF IFS',ncep_gfs025:'NOAA GFS 0,25°',dwd_icon:'DWD ICON Global'};return{route:codes.join('_'),waypoints:route.waypoints,points,terrainProfile,totalDistanceKm:route.totalDistanceKm,startTime:startTime.toISOString(),endTime:endTime.toISOString(),flightLevel,model,modelLabel:modelLabels[model]||model,generatedAt:new Date().toISOString(),source:'Open-Meteo Druckniveaudaten · Open-Meteo Elevation · NOAA AviationWeather Airport-/Stationsinformationen',weatherSampleCount:route.points.length,terrainSampleCount:terrainProfile.length};
}


const DWD_SURFACE_ANALYSIS_PAGE='https://www.dwd.de/DE/leistungen/hobbymet_wk_europa/hobbyeuropakarten.html';
let dwdSurfaceAnalysisCache={expires:0,value:null};
function htmlDecode(value){return String(value||'').replace(/&amp;/g,'&').replace(/&quot;/g,'"').replace(/&#39;/g,"'")}
function absoluteDwdUrl(value,base=DWD_SURFACE_ANALYSIS_PAGE){try{const url=new URL(htmlDecode(value),base);return url.protocol==='https:'&&/(^|\.)dwd\.de$/i.test(url.hostname)?url.toString():''}catch{return''}}
function dwdAnalysisNominalTime(now=Date.now()){const date=new Date(now),hour=date.getUTCHours();date.setUTCMinutes(0,0,0);if(hour>=12)date.setUTCHours(12);else date.setUTCHours(0);return date.toISOString()}
async function discoverDwdSurfaceAnalysis(){
 if(dwdSurfaceAnalysisCache.expires>Date.now()&&dwdSurfaceAnalysisCache.value)return dwdSurfaceAnalysisCache.value;
 const checkedAt=new Date().toISOString(),result={available:false,label:'Amtliche DWD-Bodenanalyse',sourcePage:DWD_SURFACE_ANALYSIS_PAGE,nominalTime:dwdAnalysisNominalTime(),checkedAt,license:'© Deutscher Wetterdienst · Nutzungsbedingungen des DWD'};
 try{
  const response=await fetchWithDeadline(DWD_SURFACE_ANALYSIS_PAGE,{headers:{Accept:'text/html,application/xhtml+xml','User-Agent':`MID-weather-dashboard/${WORKER_VERSION}`},cf:{cacheTtl:900,cacheEverything:true}},12000);if(!response.ok)throw new Error(`DWD HTML HTTP ${response.status}`);const html=await response.text(),candidates=[];
  for(const match of html.matchAll(/(?:src|href|data-src)=["']([^"']+\.(?:png|jpe?g)(?:\?[^"']*)?)["']/gi)){const url=absoluteDwdUrl(match[1]);if(!url)continue;const text=url.toLowerCase(),score=(text.includes('/wetterkarten/')?8:0)+(text.includes('boden')?6:0)+(text.includes('ana')?6:0)+(text.includes('analyse')?5:0)+(text.includes('_na_')?3:0)-(text.includes('logo')?20:0)-(text.includes('icon')?2:0);candidates.push({url,score})}
  candidates.sort((a,b)=>b.score-a.score);for(const candidate of candidates.slice(0,12)){try{const probe=await fetchWithDeadline(candidate.url,{headers:{Accept:'image/*','Range':'bytes=0-2047','User-Agent':`MID-weather-dashboard/${WORKER_VERSION}`},cf:{cacheTtl:900,cacheEverything:true}},7000),type=String(probe.headers.get('content-type')||'').toLowerCase();if(probe.ok&&(type.startsWith('image/')||/\.(?:png|jpe?g)(?:\?|$)/i.test(candidate.url))){result.available=true;result.sourceUrl=candidate.url;result.publishedAt=safeDate(probe.headers.get('last-modified'));try{await probe.body?.cancel()}catch{}break}}catch{}}
  if(!result.available)result.error='Die aktuelle amtliche Bilddatei konnte auf der DWD-Produktseite nicht eindeutig bestimmt werden.';
 }catch(error){result.error=error instanceof Error?error.message:String(error)}
 dwdSurfaceAnalysisCache={expires:Date.now()+(result.available?20:5)*60000,value:result};return result;
}
async function dwdSurfaceAnalysisImageResponse(){const source=await discoverDwdSurfaceAnalysis();if(!source.available||!source.sourceUrl)return json({error:source.error||'Keine amtliche DWD-Bodenanalyse verfügbar.',version:WORKER_VERSION},503,{'cache-control':'no-store'});const response=await fetchWithDeadline(source.sourceUrl,{headers:{Accept:'image/png,image/jpeg,image/*,*/*','User-Agent':`MID-weather-dashboard/${WORKER_VERSION}`},cf:{cacheTtl:1200,cacheEverything:true}},15000);if(!response.ok)return json({error:`DWD Kartenbild HTTP ${response.status}`,version:WORKER_VERSION},response.status,{'cache-control':'no-store'});const type=String(response.headers.get('content-type')||'image/png');return new Response(response.body,{status:200,headers:{'content-type':type,'access-control-allow-origin':'*','cache-control':'public, max-age=1200','x-mid-worker-version':WORKER_VERSION,'x-mid-source':'Amtliche DWD-Bodenanalyse'}})}
const DWD_PRECIPITATION_TYPE_PAGE='https://www.dwd.de/DE/leistungen/wolken_niederschlagsart/wolken_niederschlagsart.html';
const DWD_PRECIPITATION_TYPE_IMAGE='https://www.dwd.de/DWD/wetter/sat/satwetter/njob_satrad.png';
const DWD_PRECIPITATION_TYPE_RADAR_INDEX=DWD_HYMECNG_ROOTS[0];
const DWD_PRECIPITATION_TYPE_SATELLITE_INDEX='https://opendata.dwd.de/weather/satellite/clouds/TS/';
let dwdPrecipitationTypeSourceIndexCache={expires:0,radar:'',satellite:''};
let dwdHymecNgIndexCache={expires:0,html:'',root:''};
let dwdHymecNgLatestCache={expires:0,value:null};
const DWD_PRECIPITATION_TYPE_COLORS=[
 {label:'großer Hagel',color:'#b000d6',rgb:[176,0,214]},{label:'kleiner Hagel',color:'#d000f5',rgb:[208,0,245]},{label:'Graupel',color:'#ffd633',rgb:[255,214,51]},{label:'gefrierender Regen',color:'#ff1c00',rgb:[255,28,0]},{label:'gefr. Sprühregen',color:'#d80000',rgb:[216,0,0]},{label:'Schnee',color:'#ff21d1',rgb:[255,33,209]},{label:'Schneeregen',color:'#ff70df',rgb:[255,112,223]},{label:'Regen',color:'#00c778',rgb:[0,199,120]},{label:'Sprühregen',color:'#19dca4',rgb:[25,220,164]},{label:'nicht klassifizierbar',color:'#7f7f7f',rgb:[127,127,127]},{label:'kein Niederschlag',color:'#c9c9c9',rgb:[201,201,201]}
];
async function fetchDwdPrecipitationTypeImage(){const response=await fetchWithDeadline(DWD_PRECIPITATION_TYPE_IMAGE,{headers:{Accept:'image/png,image/*,*/*','Referer':'https://www.dwd.de/DE/leistungen/wolken_niederschlagsart/wolken_niederschlagsart.html','User-Agent':`MID-weather-dashboard/${WORKER_VERSION} (+https://github.com/MeteoMartini/MID)`},cf:{cacheTtl:240,cacheEverything:true}},15000);if(!response.ok)throw new Error(`DWD Niederschlagsartenbild HTTP ${response.status}`);return response}
function dwdPrecipitationTypeObservedAt(response){const modified=safeDate(response.headers.get('last-modified'));if(modified)return modified;return new Date(Math.floor(Date.now()/300000)*300000).toISOString()}
function dwdSourceTimestampIso(dateValue,timeValue){const match=String(dateValue||'').match(/^(\d{2})\.(\d{2})\.(\d{4})$/),clock=String(timeValue||'').match(/^(\d{2}):(\d{2})$/);if(!match||!clock)return undefined;const stamp=Date.UTC(Number(match[3]),Number(match[2])-1,Number(match[1]),Number(clock[1]),Number(clock[2]));return Number.isFinite(stamp)?new Date(stamp).toISOString():undefined}
function dwdPrecipitationTypeSourceTimesFromHtml(html){const normalized=String(html||'').replace(/<script[\s\S]*?<\/script>/gi,' ').replace(/<style[\s\S]*?<\/style>/gi,' ').replace(/<[^>]+>/g,' ').replace(/&nbsp;|&#160;/gi,' ').replace(/&amp;/gi,'&').replace(/\s+/g,' ').trim(),sat=normalized.match(/(?:Mon|Tue|Wed|Thu|Fri|Sat|Sun)?\s*(\d{2}\.\d{2}\.\d{4})\s+(\d{2}:\d{2})\s+UTC\s+(?:Satdaten|Satellitendaten)[^.;]{0,180}?(?:NWCSAF|Wolkenmaske|IR\/VIS)/i),radar=normalized.match(/(?:Mon|Tue|Wed|Thu|Fri|Sat|Sun)?\s*(\d{2}\.\d{2}\.\d{4})\s+(\d{2}:\d{2})\s+UTC\s+Radardaten\s+Niederschlagsart[^.;]{0,180}?(?:Komposit|alle\s*5\s*min)/i);return{satelliteAt:sat?dwdSourceTimestampIso(sat[1],sat[2]):undefined,radarAt:radar?dwdSourceTimestampIso(radar[1],radar[2]):undefined}}
function dwdIndexPublishedAt(dateValue,timeValue){const date=String(dateValue||'').match(/^(\d{2})-([A-Za-z]{3})-(\d{4})$/),clock=String(timeValue||'').match(/^(\d{2}):(\d{2}):(\d{2})$/),months={Jan:0,Feb:1,Mar:2,Apr:3,May:4,Jun:5,Jul:6,Aug:7,Sep:8,Oct:9,Nov:10,Dec:11};if(!date||!clock)return NaN;const month=months[date[2][0].toUpperCase()+date[2].slice(1).toLowerCase()];if(month===undefined)return NaN;return Date.UTC(Number(date[3]),month,Number(date[1]),Number(clock[1]),Number(clock[2]),Number(clock[3]))}
function dwdRadarDataTime(filename){const match=String(filename||'').match(/^composite_HymecNG_(\d{4})(\d{2})(\d{2})_(\d{2})(\d{2})_000-hd5$/i);if(!match)return NaN;return Date.UTC(Number(match[1]),Number(match[2])-1,Number(match[3]),Number(match[4]),Number(match[5]))}
function dwdSatelliteDataTime(filename){const match=String(filename||'').match(/^TSfc(\d{4})(\d{2})(\d{2})(\d{2})(\d{2})EA\.nc\.bz2$/i);if(!match)return NaN;return Date.UTC(Number(match[1]),Number(match[2])-1,Number(match[3]),Number(match[4]),Number(match[5]))}
function dwdSourceIndexEntries(indexHtml,kind){const text=String(indexHtml||'').replace(/<[^>]+>/g,' ').replace(/&nbsp;|&#160;/gi,' ').replace(/\s+/g,' '),pattern=kind==='radar'?/(composite_HymecNG_\d{8}_\d{4}_000-hd5)\s+(\d{2}-[A-Za-z]{3}-\d{4})\s+(\d{2}:\d{2}:\d{2})/g:/(TSfc\d{12}EA\.nc\.bz2)\s+(\d{2}-[A-Za-z]{3}-\d{4})\s+(\d{2}:\d{2}:\d{2})/g,rows=[];let match;while((match=pattern.exec(text))){const dataTime=kind==='radar'?dwdRadarDataTime(match[1]):dwdSatelliteDataTime(match[1]),publishedAt=dwdIndexPublishedAt(match[2],match[3]);if(Number.isFinite(dataTime)&&Number.isFinite(publishedAt))rows.push({filename:match[1],dataTime,publishedAt})}return rows}
function dwdSourceIndexTimeForCutoff(entries,cutoff){const maximum=Number.isFinite(cutoff)?cutoff+90000:Date.now()+90000,eligible=(entries||[]).filter(row=>Number.isFinite(row?.dataTime)&&Number.isFinite(row?.publishedAt)&&row.publishedAt<=maximum&&row.dataTime<=maximum).sort((a,b)=>b.dataTime-a.dataTime);return eligible[0]?.dataTime}
async function dwdSourceIndexHtml(url,label){const response=await fetchWithDeadline(url,{headers:{Accept:'text/html,*/*;q=0.2','User-Agent':`MID-weather-dashboard/${WORKER_VERSION} (+https://github.com/MeteoMartini/MID)`},cf:{cacheTtl:240,cacheEverything:true}},9000);if(!response.ok)throw new Error(`${label} HTTP ${response.status}`);return response.text()}
async function dwdPrecipitationTypeSourceIndexes(){if(dwdPrecipitationTypeSourceIndexCache.expires>Date.now()&&dwdPrecipitationTypeSourceIndexCache.radar&&dwdPrecipitationTypeSourceIndexCache.satellite)return dwdPrecipitationTypeSourceIndexCache;const settled=await Promise.allSettled([dwdSourceIndexHtml(DWD_PRECIPITATION_TYPE_RADAR_INDEX,'DWD HymecNG-Index'),dwdSourceIndexHtml(DWD_PRECIPITATION_TYPE_SATELLITE_INDEX,'DWD NWCSAF-Index')]),radar=settled[0].status==='fulfilled'?settled[0].value:'',satellite=settled[1].status==='fulfilled'?settled[1].value:'';dwdPrecipitationTypeSourceIndexCache={expires:Date.now()+(radar&&satellite?180000:30000),radar,satellite};return dwdPrecipitationTypeSourceIndexCache}
async function dwdPrecipitationTypeSourceTimes(imagePublishedAt){const cutoff=Date.parse(String(imagePublishedAt||'')),pagePromise=fetchWithDeadline(DWD_PRECIPITATION_TYPE_PAGE,{headers:{Accept:'text/html,application/xhtml+xml;q=0.9,*/*;q=0.2','User-Agent':`MID-weather-dashboard/${WORKER_VERSION} (+https://github.com/MeteoMartini/MID)`},cf:{cacheTtl:240,cacheEverything:true}},5000).then(async response=>response.ok?dwdPrecipitationTypeSourceTimesFromHtml(await response.text()):{}).catch(()=>({})),indexesPromise=dwdPrecipitationTypeSourceIndexes(),[pageTimes,indexes]=await Promise.all([pagePromise,indexesPromise]),radarDataTime=dwdSourceIndexTimeForCutoff(dwdSourceIndexEntries(indexes.radar,'radar'),cutoff),satelliteDataTime=dwdSourceIndexTimeForCutoff(dwdSourceIndexEntries(indexes.satellite,'satellite'),cutoff),radarAt=pageTimes.radarAt||(Number.isFinite(radarDataTime)?new Date(radarDataTime).toISOString():undefined),satelliteAt=pageTimes.satelliteAt||(Number.isFinite(satelliteDataTime)?new Date(satelliteDataTime).toISOString():undefined);if(!radarAt||!satelliteAt)throw new Error(`DWD-Quellzeitstempel unvollständig: Radar ${radarAt?'ok':'fehlt'}, Satellit ${satelliteAt?'ok':'fehlt'}`);return{radarAt,satelliteAt,source:'DWD-Produktseite verbindlich; Fallback HymecNG + NWCSAF OpenData'}}
function dwdHymecNgTimestamp(filename){return dwdRadarDataTime(filename)}
function dwdHymecNgEstimatedTime(publishedAt){if(!Number.isFinite(publishedAt))return NaN;const candidate=Math.round((publishedAt-60000)/300000)*300000;return Math.min(candidate,publishedAt)}
function dwdHymecNgFreshAge(ageMinutes){return Number.isFinite(ageMinutes)&&!(ageMinutes>25)&&ageMinutes>=-10}
async function dwdStableRadarFileProbe(root,filename,label){
 const target=new URL(filename,root).toString(),headers={Accept:'application/octet-stream,application/x-hdf5,*/*','Cache-Control':'no-cache','User-Agent':`MID-weather-dashboard/${WORKER_VERSION} (+https://github.com/MeteoMartini/MID)`};let response;
 try{response=await fetchWithDeadline(target,{method:"HEAD",headers,cf:{cacheTtl:30,cacheEverything:false}},7000)}catch{}
 let publishedAt=response?.ok?Date.parse(String(response.headers.get('last-modified')||'')):NaN,etag=response?.ok?String(response.headers.get('etag')||''):'';
 if(!response?.ok||!Number.isFinite(publishedAt)){
  try{const ranged=await fetchWithDeadline(target,{method:'GET',headers:{...headers,Range:'bytes=0-0'},cf:{cacheTtl:30,cacheEverything:false}},8000);response=ranged;publishedAt=ranged.ok?Date.parse(String(ranged.headers.get('last-modified')||'')):NaN;etag=ranged.ok?String(ranged.headers.get('etag')||''):'';try{await ranged.body?.cancel()}catch{}}catch{}
 }
 if(!response?.ok||!Number.isFinite(publishedAt))return{available:false,root,filename,label,status:response?.status||0};
 return{available:true,root,filename,label,publishedAt,etag,contentLength:Number(response.headers.get('content-length'))||undefined};
}
async function dwdHymecNgLatestProbe(){
 if(dwdHymecNgLatestCache.expires>Date.now()&&dwdHymecNgLatestCache.value)return dwdHymecNgLatestCache.value;
 const settled=await Promise.allSettled(DWD_HYMECNG_ROOTS.map(root=>dwdStableRadarFileProbe(root,DWD_HYMECNG_LATEST,'HymecNG LATEST'))),candidates=settled.filter(item=>item.status==='fulfilled'&&item.value.available).map(item=>item.value).sort((a,b)=>b.publishedAt-a.publishedAt),value=candidates[0]||null;
 dwdHymecNgLatestCache={expires:Date.now()+45000,value};return value;
}
async function dwdLegacyHgLatestProbe(){const settled=await Promise.allSettled(DWD_HG_ROOTS.map(root=>dwdStableRadarFileProbe(root,DWD_HG_LATEST,'HG Legacy LATEST'))),candidates=settled.filter(item=>item.status==='fulfilled'&&item.value.available).map(item=>item.value).sort((a,b)=>b.publishedAt-a.publishedAt);return candidates[0]||null}
async function dwdHymecNgIndex(){
 if(dwdHymecNgIndexCache.expires>Date.now()&&dwdHymecNgIndexCache.html)return dwdHymecNgIndexCache;
 const settled=await Promise.allSettled(DWD_HYMECNG_ROOTS.map(async root=>{const response=await fetchWithDeadline(root,{headers:{Accept:'text/html,application/xhtml+xml,*/*;q=0.2','Cache-Control':'no-cache','User-Agent':`MID-weather-dashboard/${WORKER_VERSION} (+https://github.com/MeteoMartini/MID)`},cf:{cacheTtl:60,cacheEverything:true}},9000);if(!response.ok)throw new Error(`${root} HTTP ${response.status}`);const html=await response.text(),entries=dwdHymecNgEntries(html),latest=entries.at(-1);if(!latest)throw new Error(`${root} enthält keine HymecNG-Dateien`);return{root,html,latestTime:latest.dataTime}}));
 const candidates=settled.filter(item=>item.status==='fulfilled').map(item=>item.value).sort((a,b)=>b.latestTime-a.latestTime);
 const selected=candidates[0];if(!selected){const errors=settled.filter(item=>item.status==='rejected').map(item=>item.reason instanceof Error?item.reason.message:String(item.reason));throw new Error(`DWD HymecNG-Index nicht verfügbar: ${errors.join(' | ')||'keine Quelle'}`)}
 dwdHymecNgIndexCache={expires:Date.now()+90000,html:selected.html,root:selected.root,latestTime:selected.latestTime};return dwdHymecNgIndexCache;
}
function dwdHymecNgEntries(html){const rows=[],pattern=/(composite_HymecNG_\d{8}_\d{4}_000-hd5)\s+(\d{2}-[A-Za-z]{3}-\d{4})\s+(\d{2}:\d{2}:\d{2})/g,text=String(html||'').replace(/<[^>]+>/g,' ').replace(/&nbsp;|&#160;/gi,' ').replace(/\s+/g,' ');let match;while((match=pattern.exec(text))){const dataTime=dwdHymecNgTimestamp(match[1]),publishedAt=dwdIndexPublishedAt(match[2],match[3]);if(Number.isFinite(dataTime))rows.push({filename:match[1],dataTime,publishedAt})}return rows.sort((a,b)=>a.dataTime-b.dataTime)}
function dwdHymecNgProxyUrl(request,filename,root,revision){const url=new URL(request.url);url.search='';url.searchParams.set('mode','dwd-hymecng-file');url.searchParams.set('file',filename);if(root)url.searchParams.set('root',String(DWD_HYMECNG_ROOTS.indexOf(root)));if(revision)url.searchParams.set('rev',String(revision));return url.toString()}
function dwdHymecNgPublicMeta(request,{filename,root,observedAt,publishedAt,ageMinutes,sourceMode,revision}){return{available:true,fresh:true,ageMinutes:Math.max(0,Math.round(ageMinutes)),product:'HymecNG',productName:'DWD HymecNG · Niederschlagsart 2 m',observedAt:new Date(observedAt).toISOString(),publishedAt:Number.isFinite(publishedAt)?new Date(publishedAt).toISOString():undefined,fileUrl:dwdHymecNgProxyUrl(request,filename,root,revision),filename,nativeResolutionM:1000,grid:'1200 × 1100 Deutschland-Komposit',georeferencing:'native-odim-projection',classificationVerified:true,classificationNote:'Aktueller DWD-HymecNG-HDF5-Snapshot; interne HDF5-Zeit, native ODIM-Georeferenzierung und defensive Klassencodierung werden im Client verifiziert.',sourceMode,sourceRoot:root,provider:'Deutscher Wetterdienst',license:'DWD Open Data'}}
async function dwdHymecNgMetadata(request){
 const url=new URL(request.url),targetRaw=url.searchParams.get('target'),target=targetRaw?Date.parse(targetRaw):NaN,now=Date.now(),liveTarget=!Number.isFinite(target)||Math.abs(target-now)<=20*60000;
 if(liveTarget){
  const latest=await dwdHymecNgLatestProbe();
  if(latest?.available){const observedAt=dwdHymecNgEstimatedTime(latest.publishedAt),ageMinutes=(now-observedAt)/60000,targetOffset=Number.isFinite(target)?Math.abs(observedAt-target)/60000:0;if(Number.isFinite(observedAt)&&dwdHymecNgFreshAge(ageMinutes)&&targetOffset<=15)return dwdHymecNgPublicMeta(request,{filename:DWD_HYMECNG_LATEST,root:latest.root,observedAt,publishedAt:latest.publishedAt,ageMinutes,sourceMode:'latest-hdf5',revision:latest.etag||latest.publishedAt})}
 }
 try{
  const index=await dwdHymecNgIndex(),entries=dwdHymecNgEntries(index.html),maximum=Number.isFinite(target)?target+150000:now+600000,minimum=Number.isFinite(target)?target-15*60000:now-25*60000,eligible=entries.filter(row=>row.dataTime<=maximum&&row.dataTime>=minimum),selected=Number.isFinite(target)?eligible.sort((a,b)=>Math.abs(a.dataTime-target)-Math.abs(b.dataTime-target))[0]:eligible.at(-1),latest=entries.at(-1),latestAge=latest?Math.round((now-latest.dataTime)/60000):undefined;
  if(selected){const targetOffset=Number.isFinite(target)?Math.abs(selected.dataTime-target)/60000:0,ageMinutes=(now-selected.dataTime)/60000;if(dwdHymecNgFreshAge(ageMinutes)&&targetOffset<=15)return dwdHymecNgPublicMeta(request,{filename:selected.filename,root:index.root,observedAt:selected.dataTime,publishedAt:selected.publishedAt,ageMinutes,sourceMode:'dated-hdf5',revision:selected.publishedAt||selected.dataTime})}
  const legacy=await dwdLegacyHgLatestProbe(),legacyAge=legacy?.available?(now-legacy.publishedAt)/60000:NaN;
  return{available:false,fresh:false,product:'HymecNG',reason:`Kein frischer DWD-HymecNG-HDF5-Snapshot verfügbar${latestAge!==undefined?`; jüngster datierter HDF5-Stand ${latestAge} min alt`:''}.${legacy?.available&&legacyAge<=60?' DWD Legacy-HG (BUFR/bzip2) ist aktuell erreichbar, wird aber ohne BUFR-Decoder nicht als Kartenersatz missbraucht.':''}`,legacyHgAvailable:Boolean(legacy?.available),legacyHgPublishedAt:legacy?.available?new Date(legacy.publishedAt).toISOString():undefined,legacyHgFormat:legacy?.available?'BUFR/bzip2':undefined,sourceMode:'unavailable',provider:'Deutscher Wetterdienst',license:'DWD Open Data'};
 }catch(error){const legacy=await dwdLegacyHgLatestProbe().catch(()=>null);return{available:false,fresh:false,product:'HymecNG',reason:`DWD HymecNG konnte nicht ermittelt werden: ${error instanceof Error?error.message:String(error)}${legacy?.available?' Legacy-HG ist als BUFR/bzip2 erreichbar, aber kein renderbarer HDF5-Ersatz.':''}`,legacyHgAvailable:Boolean(legacy?.available),legacyHgPublishedAt:legacy?.available?new Date(legacy.publishedAt).toISOString():undefined,legacyHgFormat:legacy?.available?'BUFR/bzip2':undefined,sourceMode:'unavailable',provider:'Deutscher Wetterdienst',license:'DWD Open Data'}}
}
async function dwdHymecNgFileResponse(request){
 const url=new URL(request.url),filename=String(url.searchParams.get('file')||''),isLatest=filename===DWD_HYMECNG_LATEST,observedMs=isLatest?NaN:dwdHymecNgTimestamp(filename),rootIndex=Number(url.searchParams.get('root')),preferredRoot=Number.isInteger(rootIndex)&&rootIndex>=0&&rootIndex<DWD_HYMECNG_ROOTS.length?DWD_HYMECNG_ROOTS[rootIndex]:undefined;
 if(!isLatest&&!Number.isFinite(observedMs))return json({error:'Ungültige DWD-HymecNG-Datei.',version:WORKER_VERSION},400,{'cache-control':'no-store'});if(!isLatest&&Math.abs(Date.now()-observedMs)>2*3600000)return json({error:'DWD-HymecNG-Datei ist für den Live-Layer zu alt.',version:WORKER_VERSION},409,{'cache-control':'no-store'});
 const roots=preferredRoot?[preferredRoot,...DWD_HYMECNG_ROOTS.filter(root=>root!==preferredRoot)]:DWD_HYMECNG_ROOTS;let lastStatus=502;
 for(const root of roots){const response=await fetchWithDeadline(new URL(filename,root).toString(),{headers:{Accept:'application/x-hdf5,application/octet-stream,*/*','Cache-Control':'no-cache','User-Agent':`MID-weather-dashboard/${WORKER_VERSION} (+https://github.com/MeteoMartini/MID)`},cf:{cacheTtl:isLatest?45:120,cacheEverything:true}},15000);lastStatus=response.status;if(!response.ok)continue;const modified=Date.parse(String(response.headers.get('last-modified')||'')),headerObserved=isLatest?dwdHymecNgEstimatedTime(modified):observedMs;return new Response(response.body,{status:200,headers:{'content-type':response.headers.get('content-type')||'application/x-hdf5','content-disposition':`inline; filename="${filename}"`,'access-control-allow-origin':'*','cache-control':isLatest?'public, max-age=45, stale-while-revalidate=30':'public, max-age=120, stale-while-revalidate=60','x-mid-worker-version':WORKER_VERSION,'x-mid-source':'DWD HymecNG HDF5','x-mid-observed-at':Number.isFinite(headerObserved)?new Date(headerObserved).toISOString():'','x-mid-published-at':Number.isFinite(modified)?new Date(modified).toISOString():''}})}
 return json({error:`DWD HymecNG HTTP ${lastStatus}`,version:WORKER_VERSION},lastStatus,{'cache-control':'no-store'})
}
async function dwdPrecipitationTypeImageResponse(){try{const response=await fetchDwdPrecipitationTypeImage(),type=String(response.headers.get('content-type')||'image/png'),headers=new Headers(),observedAt=dwdPrecipitationTypeObservedAt(response),publishedAt=safeDate(response.headers.get('last-modified'))||observedAt,sourceTimes=await dwdPrecipitationTypeSourceTimes(publishedAt);headers.set('content-type',type.startsWith('image/')?type:'image/png');headers.set('access-control-allow-origin','*');headers.set('access-control-allow-methods','GET,OPTIONS');headers.set('access-control-expose-headers','last-modified,x-mid-worker-version,x-mid-source,x-mid-observed-at,x-mid-radar-at,x-mid-satellite-at');headers.set('cache-control','public, max-age=240, stale-while-revalidate=180');headers.set('x-mid-worker-version',WORKER_VERSION);headers.set('x-mid-source','DWD Wolken + Niederschlagsart · Originalprodukt');headers.set('x-mid-observed-at',observedAt);headers.set('x-mid-radar-at',sourceTimes.radarAt);headers.set('x-mid-satellite-at',sourceTimes.satelliteAt);const modified=response.headers.get('last-modified');if(modified)headers.set('last-modified',modified);return new Response(response.body,{status:200,headers})}catch(error){return json({error:error instanceof Error?error.message:String(error),version:WORKER_VERSION},502,{'cache-control':'no-store'})}}
async function dwdPrecipitationTypeMeta(){const response=await fetchDwdPrecipitationTypeImage(),observedAt=dwdPrecipitationTypeObservedAt(response),publishedAt=safeDate(response.headers.get('last-modified'))||observedAt;try{await response.body?.cancel()}catch{}const sourceTimes=await dwdPrecipitationTypeSourceTimes(publishedAt);return{observedAt,publishedAt,radarAt:sourceTimes.radarAt,satelliteAt:sourceTimes.satelliteAt,componentTimeNote:`Verbindliche UTC-Zeitstände der DWD-Quelldaten (${sourceTimes.source}).`,sourcePage:DWD_PRECIPITATION_TYPE_PAGE,checkedAt:new Date().toISOString(),provider:'Deutscher Wetterdienst'}}
function dwdPrecipitationClass(r,g,b){let best=null,bestDistance=Infinity;for(const item of DWD_PRECIPITATION_TYPE_COLORS){const distance=Math.sqrt((r-item.rgb[0])**2+(g-item.rgb[1])**2+(b-item.rgb[2])**2);if(distance<bestDistance){bestDistance=distance;best=item}}if(!best)return null;const saturated=Math.max(r,g,b)-Math.min(r,g,b)>45,grayClass=best.label==='nicht klassifizierbar'||best.label==='kein Niederschlag',threshold=grayClass?22:saturated?72:42;return bestDistance<=threshold?{...best,distance:bestDistance}:null}
function dwdCloudSignal(r,g,b,precipitation){if(precipitation&&precipitation.label!=='kein Niederschlag')return'durch Niederschlagsfarbe überlagert';const max=Math.max(r,g,b),min=Math.min(r,g,b),luma=.2126*r+.7152*g+.0722*b,saturation=max?((max-min)/max):0;if(luma>=220&&saturation<.22)return'helles / dichtes Wolkensignal';if(luma>=178&&saturation<.35)return'deutliches Wolkensignal';if(luma>=125&&saturation<.48)return'mittleres Wolken-/Satellitensignal';return'schwaches Wolken-/Untergrundsignal'}
async function dwdPrecipitationTypeInfo(request){const url=new URL(request.url),x=Number(url.searchParams.get('x')),y=Number(url.searchParams.get('y')),lat=Number(url.searchParams.get('lat')),lon=Number(url.searchParams.get('lon')),expected=Date.parse(String(url.searchParams.get('expected')||''));if(!Number.isFinite(x)||!Number.isFinite(y)||x<0||x>1||y<0||y>1)throw new Error('Ungültiger Bildpunkt');const response=await fetchDwdPrecipitationTypeImage(),observedAt=dwdPrecipitationTypeObservedAt(response),observedMs=Date.parse(observedAt);if(Number.isFinite(expected)&&Number.isFinite(observedMs)&&Math.abs(expected-observedMs)>6*60000){try{await response.body?.cancel()}catch{}throw new Error('Das DWD-Originalprodukt wurde inzwischen aktualisiert. Bitte kurz neu laden.')}const image=await decodePng(await response.arrayBuffer()),cx=clamp(Math.round(x*(image.width-1)),0,image.width-1),cy=clamp(Math.round(y*(image.height-1)),0,image.height-1),counts=new Map(),samples=[];for(let oy=-2;oy<=2;oy++)for(let ox=-2;ox<=2;ox++){const px=clamp(cx+ox,0,image.width-1),py=clamp(cy+oy,0,image.height-1),i=(py*image.width+px)*4,r=image.rgba[i],g=image.rgba[i+1],b=image.rgba[i+2],a=image.rgba[i+3];if(a<30)continue;samples.push([r,g,b]);const classification=dwdPrecipitationClass(r,g,b);if(classification)counts.set(classification.label,(counts.get(classification.label)||0)+1)}let precipitation=null,bestCount=0;for(const[label,count]of counts){if(count>bestCount){bestCount=count;precipitation=DWD_PRECIPITATION_TYPE_COLORS.find(item=>item.label===label)}}const centerIndex=(cy*image.width+cx)*4,center=[image.rgba[centerIndex],image.rgba[centerIndex+1],image.rgba[centerIndex+2]],avg=samples.length?samples.reduce((acc,row)=>[acc[0]+row[0],acc[1]+row[1],acc[2]+row[2]],[0,0,0]).map(value=>Math.round(value/samples.length)):center,confidence=bestCount>=12?'high':bestCount>=5?'medium':'low';return{precipitationLabel:precipitation?.label||'kein klassifizierter Niederschlag',precipitationColor:precipitation?.color,cloudLabel:dwdCloudSignal(avg[0],avg[1],avg[2],precipitation),confidence,observedAt,latitude:Number.isFinite(lat)?lat:undefined,longitude:Number.isFinite(lon)?lon:undefined,rgb:center,note:'Niederschlagsart aus der offiziellen DWD-Farbklasse; Wolkensignal als visuelle Näherung aus dem Satelliten-Komposit.'}}
function dwdSurfaceAnalysisPublic(source,request){const proxy=new URL(request.url);proxy.search='';proxy.searchParams.set('mode','dwd-surface-analysis-image');return{available:Boolean(source.available),label:source.label,sourcePage:source.sourcePage,imageUrl:source.available?proxy.toString():undefined,nominalTime:source.nominalTime,publishedAt:source.publishedAt,checkedAt:source.checkedAt,license:source.license,error:source.error}}

const DWD_SYNOPTIC_TEXT_PRODUCTS=[
 {id:'short',label:'DWD Synoptische Übersicht Kurzfrist',url:'https://opendata.dwd.de/weather/text_forecasts/txt/SXDL31_DWAV_LATEST'},
 {id:'medium',label:'DWD Synoptische Übersicht Mittelfrist',url:'https://opendata.dwd.de/weather/text_forecasts/txt/SXDL33_DWAV_LATEST'}
];
const DWD_SYNOPTIC_VOCABULARY=['Höhentrog','Kurzwellentrog','Langwellentrog','Trogvorderseite','Trogrückseite','Trogachse','Trog','Höhenrücken','Rücken','Keil','Frontalzone','Luftmassengrenze','Kaltfront','Warmfront','Okklusion','Konvergenz','Tiefdruckrinne','Hochdruckbrücke','Zwischenhoch','Gradientverschärfung','Zyklogenese','Frontogenese','Warmluftadvektion','Kaltluftadvektion','Advektion','Absinken','Hebung','Labilisierung','Stabilisierung','Staulage','Föhn','Lee','Randtrog'];
function dwdSynopticTerms(text){const clean=String(text||'').replace(/\s+/g,' ');return DWD_SYNOPTIC_VOCABULARY.filter(term=>new RegExp(`(?:^|[^A-Za-zÄÖÜäöüß])${term}(?=$|[^A-Za-zÄÖÜäöüß])`,'i').test(clean)).slice(0,18)}
async function dwdSynopticVocabulary(){const checkedAt=new Date().toISOString(),settled=await Promise.allSettled(DWD_SYNOPTIC_TEXT_PRODUCTS.map(async product=>{const response=await fetchWithDeadline(product.url,{headers:{Accept:'text/plain,*/*','User-Agent':`MID-weather-dashboard/${WORKER_VERSION}`},cf:{cacheTtl:900,cacheEverything:true}},10000);if(!response.ok)throw new Error(`${product.label} HTTP ${response.status}`);const buffer=await response.arrayBuffer(),text=new TextDecoder('windows-1252').decode(buffer),terms=dwdSynopticTerms(text);return{...product,terms,available:true}})),products=settled.map((result,index)=>result.status==='fulfilled'?result.value:{...DWD_SYNOPTIC_TEXT_PRODUCTS[index],available:false,terms:[],error:result.reason instanceof Error?result.reason.message:String(result.reason)}),terms=[...new Set(products.flatMap(product=>product.terms))].slice(0,18);return{available:products.some(product=>product.available),checkedAt,terms,shortRangeTerms:products.find(product=>product.id==='short')?.terms||[],mediumRangeTerms:products.find(product=>product.id==='medium')?.terms||[],sources:products.map(({id,label,url,available,error})=>({id,label,url,available,error}))}}
const SYNOPTIC_MODELS=[
 {id:'dwd_icon_eu',label:'DWD ICON-EU'},
 {id:'ecmwf_ifs',label:'ECMWF IFS'},
 {id:'ncep_gfs025',label:'NOAA GFS 0,25°'}
];
const SYNOPTIC_FIELDS=['temperature_2m','dew_point_2m','pressure_msl','wind_speed_10m','wind_direction_10m','wind_gusts_10m','precipitation','cloud_cover','wind_speed_850hPa','wind_direction_850hPa'];
function synopticGrid(lat,lon){const latOffsets=[-1.5,0,1.5],lonFactor=1/Math.max(.42,Math.cos(lat*Math.PI/180)),lonOffsets=[-2,-1,0,1,2].map(value=>value*lonFactor);return{rows:latOffsets.length,cols:lonOffsets.length,points:latOffsets.flatMap(latOffset=>lonOffsets.map(lonOffset=>({latitude:lat+latOffset,longitude:lon+lonOffset}))),center:1*lonOffsets.length+2}}
function synopticHourlyValue(row,key,index){return number(row?.hourly?.[key]?.[index])}
function synopticTimeIndex(times,target){let best=0,delta=Infinity;for(let index=0;index<times.length;index++){const value=Date.parse(String(times[index]));if(!Number.isFinite(value))continue;const current=Math.abs(value-target);if(current<delta){delta=current;best=index}}return best}
function circularDelta(a,b){if(!Number.isFinite(a)||!Number.isFinite(b))return 0;return Math.abs(((a-b+540)%360)-180)}
function synopticGradient(rows,grid,key,index){const center=rows[grid.center],west=rows[grid.center-1],east=rows[grid.center+1],south=rows[2],north=rows[grid.center+grid.cols],c=synopticHourlyValue(center,key,index),w=synopticHourlyValue(west,key,index),e=synopticHourlyValue(east,key,index),s=synopticHourlyValue(south,key,index),n=synopticHourlyValue(north,key,index);if([c,w,e,s,n].some(value=>value===undefined))return{magnitude:0,dLat:0,dLon:0};const ewKm=distance(west.latitude,west.longitude,east.latitude,east.longitude)/1000,nsKm=distance(south.latitude,south.longitude,north.latitude,north.longitude)/1000,dLon=(e-w)/Math.max(1,ewKm)*100,dLat=(n-s)/Math.max(1,nsKm)*100;return{magnitude:Math.hypot(dLat,dLon),dLat,dLon}}
function synopticFrontLine(lat,lon,gradient,lengthDeg=3.4){const normal=Math.atan2(gradient.dLon,gradient.dLat),lineAngle=normal+Math.PI/2,dy=Math.cos(lineAngle)*lengthDeg,dx=Math.sin(lineAngle)*lengthDeg/Math.max(.35,Math.cos(lat*Math.PI/180));return[[lat-dy,lon-dx],[lat+dy,lon+dx]]}
function synopticFrontType(tempChange,dewChange,windShift,rain,trough,tempGradient,dewGradient){if(tempChange<=-1.6&&windShift>=20)return'cold';if(tempChange>=1.3&&windShift>=18)return'warm';if(Math.abs(tempChange)<1.8&&dewGradient>=1.2&&rain>=1&&trough>=.6)return'occlusion';if(windShift>=48&&rain>=.3&&tempGradient<1.7)return'convergence';return'trough'}
function synopticConfidence(score){return score>=68?'high':score>=48?'medium':'low'}
async function fetchSynopticModel(model,lat,lon){
 const grid=synopticGrid(lat,lon),url=new URL(OPEN_METEO_FORECAST);url.searchParams.set('latitude',grid.points.map(point=>point.latitude.toFixed(4)).join(','));url.searchParams.set('longitude',grid.points.map(point=>point.longitude.toFixed(4)).join(','));url.searchParams.set('hourly',SYNOPTIC_FIELDS.join(','));url.searchParams.set('models',model.id);url.searchParams.set('timezone','GMT');url.searchParams.set('wind_speed_unit','kn');url.searchParams.set('past_days','1');url.searchParams.set('forecast_days','4');url.searchParams.set('cell_selection','nearest');
 let response=await fetchWithDeadline(url.toString(),{headers:{Accept:'application/json','User-Agent':`MID-weather-dashboard/${WORKER_VERSION}`}},30000),payload=await response.json().catch(()=>({}));if(!response.ok){url.searchParams.set('hourly',SYNOPTIC_FIELDS.filter(field=>!field.includes('850hPa')).join(','));response=await fetchWithDeadline(url.toString(),{headers:{Accept:'application/json','User-Agent':`MID-weather-dashboard/${WORKER_VERSION}`}},30000);payload=await response.json().catch(()=>({}))}if(!response.ok)throw new Error(payload?.reason||payload?.error||`${model.label} HTTP ${response.status}`);const rows=openMeteoRows(payload).map((row,index)=>({...row,latitude:number(row?.latitude)??grid.points[index]?.latitude,longitude:number(row?.longitude)??grid.points[index]?.longitude}));if(rows.length<grid.points.length)throw new Error(`${model.label}: unvollständiges Synoptikfeld`);const center=rows[grid.center],times=Array.isArray(center?.hourly?.time)?center.hourly.time:[],now=Date.now(),start=synopticTimeIndex(times,now),runTime=safeDate(center?.generationtime_ms?now:center?.hourly?.time?.[0]);let best=null;
 for(let index=Math.max(3,start);index<times.length-3;index+=3){const valid=Date.parse(String(times[index]));if(!Number.isFinite(valid)||valid<now-2*3600000||valid>now+84*3600000)continue;const before=index-3,after=index+3,temp=synopticHourlyValue(center,'temperature_2m',index),tempBefore=synopticHourlyValue(center,'temperature_2m',before),tempAfter=synopticHourlyValue(center,'temperature_2m',after),dewBefore=synopticHourlyValue(center,'dew_point_2m',before),dewAfter=synopticHourlyValue(center,'dew_point_2m',after),pressure=synopticHourlyValue(center,'pressure_msl',index),pressureBefore=synopticHourlyValue(center,'pressure_msl',before),pressureAfter=synopticHourlyValue(center,'pressure_msl',after),windBefore=synopticHourlyValue(center,'wind_direction_10m',before),windAfter=synopticHourlyValue(center,'wind_direction_10m',after);if([temp,tempBefore,tempAfter,dewBefore,dewAfter,pressure,pressureBefore,pressureAfter].some(value=>value===undefined))continue;const tempGradient=synopticGradient(rows,grid,'temperature_2m',index),dewGradient=synopticGradient(rows,grid,'dew_point_2m',index),tempChange=tempAfter-tempBefore,dewChange=dewAfter-dewBefore,trough=Math.max(0,(pressureBefore+pressureAfter)/2-pressure),windShift=circularDelta(windBefore,windAfter),rain=Array.from({length:5},(_,offset)=>synopticHourlyValue(center,'precipitation',index-2+offset)||0).reduce((sum,value)=>sum+Math.max(0,value),0),cloud=synopticHourlyValue(center,'cloud_cover',index)||0,gustJump=Math.max(0,(synopticHourlyValue(center,'wind_gusts_10m',after)||0)-(synopticHourlyValue(center,'wind_gusts_10m',before)||0)),score=clamp(tempGradient.magnitude*16+dewGradient.magnitude*13+trough*12+windShift*.4+Math.min(17,rain*4)+Math.max(0,cloud-65)*.16+Math.abs(tempChange)*5+gustJump*.3,0,100),type=synopticFrontType(tempChange,dewChange,windShift,rain,trough,tempGradient.magnitude,dewGradient.magnitude),signals=[{key:'thermal-gradient',label:'Temperaturgradient',value:Number(tempGradient.magnitude.toFixed(1)),unit:'K / 100 km',support:clamp(tempGradient.magnitude/3,0,1)},{key:'moisture-gradient',label:'Taupunktgradient',value:Number(dewGradient.magnitude.toFixed(1)),unit:'K / 100 km',support:clamp(dewGradient.magnitude/3,0,1)},{key:'pressure-trough',label:'Druckrinne',value:Number(trough.toFixed(1)),unit:'hPa',support:clamp(trough/3,0,1)},{key:'wind-shift',label:'Winddrehung',value:Math.round(windShift),unit:'°',support:clamp(windShift/90,0,1)},{key:'precipitation',label:'Niederschlag',value:Number(rain.toFixed(1)),unit:'mm / 5 h',support:clamp(rain/5,0,1)}];if(score>=34&&(!best||score>best.score))best={modelId:model.id,modelLabel:model.label,type,arrivalAt:new Date(valid).toISOString(),score:Math.round(score),confidence:synopticConfidence(score),line:synopticFrontLine(lat,lon,tempGradient.magnitude>=dewGradient.magnitude?tempGradient:dewGradient),approachBearingDeg:synopticHourlyValue(center,'wind_direction_850hPa',index)??synopticHourlyValue(center,'wind_direction_10m',index),motionDirectionDeg:(((synopticHourlyValue(center,'wind_direction_850hPa',index)??synopticHourlyValue(center,'wind_direction_10m',index)??0)+180)%360),signals,summary:`MID-Frontkandidat · objektive Modellanalyse: ${type==='cold'?'Kaltfront':type==='warm'?'Warmfront':type==='occlusion'?'Okklusion':type==='convergence'?'Konvergenzlinie':'Trogachse'} aus thermischem und Feuchtegradient, Druckrinne, Winddrehung und Niederschlagsstützung.`,runTime}}
 return best;
}
function synopticAgreement(candidates){if(!candidates.length)return{modelCount:SYNOPTIC_MODELS.length,supportingModels:0,type:'none',typeAgreement:0,meanScore:0,confidence:'low',summary:'Kein ausreichend markanter, modellübergreifend kohärenter Frontkandidat.'};const arrivals=candidates.map(item=>Date.parse(item.arrivalAt)).filter(Number.isFinite).sort((a,b)=>a-b),middle=arrivals.length%2?arrivals[(arrivals.length-1)/2]:(arrivals[arrivals.length/2-1]+arrivals[arrivals.length/2])/2,counts=new Map();for(const candidate of candidates)counts.set(candidate.type,(counts.get(candidate.type)||0)+1);const [type,count]=[...counts.entries()].sort((a,b)=>b[1]-a[1])[0],spreadHours=arrivals.length>1?(arrivals.at(-1)-arrivals[0])/3600000:6,typeAgreement=count/candidates.length,meanScore=candidates.reduce((sum,item)=>sum+item.score,0)/candidates.length,confidence=candidates.length>=3&&spreadHours<=6&&typeAgreement>=.66?'high':candidates.length>=2&&spreadHours<=12?'medium':'low',halfWindow=Math.max(1.5,spreadHours/2+1.5);return{modelCount:SYNOPTIC_MODELS.length,supportingModels:candidates.length,type,typeAgreement:Number(typeAgreement.toFixed(2)),medianArrivalAt:new Date(middle).toISOString(),windowStart:new Date(middle-halfWindow*3600000).toISOString(),windowEnd:new Date(middle+halfWindow*3600000).toISOString(),spreadHours:Number(spreadHours.toFixed(1)),meanScore:Math.round(meanScore),confidence,summary:`${candidates.length} von ${SYNOPTIC_MODELS.length} Modellen stützen den Wetterwechsel; Timing-Spanne ${spreadHours.toFixed(1)} h, Fronttyp-Übereinstimmung ${Math.round(typeAgreement*100)} %.`}}
function synopticBearing(lat1,lon1,lat2,lon2){const rad=Math.PI/180,a=lat1*rad,b=lat2*rad,dLon=(lon2-lon1)*rad,y=Math.sin(dLon)*Math.cos(b),x=Math.cos(a)*Math.sin(b)-Math.sin(a)*Math.cos(b)*Math.cos(dLon);return(Math.atan2(y,x)/rad+360)%360}
function synopticUpstreamBearing(candidates){const directions=candidates.map(item=>number(item.approachBearingDeg)??(number(item.motionDirectionDeg)===undefined?undefined:(number(item.motionDirectionDeg)+180)%360)).filter(Number.isFinite);if(!directions.length)return undefined;const x=directions.reduce((sum,value)=>sum+Math.sin(value*Math.PI/180),0),y=directions.reduce((sum,value)=>sum+Math.cos(value*Math.PI/180),0);return(Math.atan2(x,y)*180/Math.PI+360)%360}
function stationWindKt(row){const value=number(row?.windSpeed);if(value===undefined)return undefined;return row?.windUnit==='kt'?value:value/1.852}
async function synopticObservationCorridor(lat,lon,candidates,agreement){const bearing=synopticUpstreamBearing(candidates),lengthKm=420,widthDeg=62,settled=await Promise.allSettled([brightSkyRows(lat,lon,lengthKm),metarRows(lat,lon,lengthKm)]),raw=settled.flatMap(result=>result.status==='fulfilled'?result.value:[]),arrival=Date.parse(agreement?.medianArrivalAt||''),candidateType=agreement?.type||candidates[0]?.type,stations=[];for(const row of raw){const slat=number(row?.lat),slon=number(row?.lon),distanceM=number(row?.distance);if(slat===undefined||slon===undefined||distanceM===undefined)continue;const stationBearing=synopticBearing(lat,lon,slat,slon),difference=bearing===undefined?0:circularDelta(stationBearing,bearing);if(bearing!==undefined&&difference>widthDeg/2)continue;const temp=number(row?.temp),dew=number(row?.dewp),phase=candidateType==='cold'&&temp!==undefined&&dew!==undefined&&temp-dew<=3?'transition':candidateType==='warm'&&dew!==undefined&&temp!==undefined&&temp-dew<=4?'after':'before';stations.push({id:String(row?.stationId||row?.icaoId||`${slat}:${slon}`),name:String(row?.name||'Station'),provider:String(row?.provider||'Beobachtung'),lat:slat,lon:slon,distanceKm:Number((distanceM/1000).toFixed(1)),observedAt:safeDate(row?.reportTime),temperature:temp,dewPoint:dew,pressure:number(row?.pressureMsl??row?.pressure),windSpeedKt:stationWindKt(row),windDirection:number(row?.windDirection),precipitation:number(row?.precipitation),phase,quality:clamp(number(row?.trustFactor)??80,0,100)});if(stations.length>=18)break}stations.sort((a,b)=>a.distanceKm-b.distanceKm);const transition=stations.filter(item=>item.phase==='transition').length,phaseSupport=stations.length?transition/stations.length:0,summary=bearing===undefined?'Annäherungsrichtung nicht belastbar; die nächstgelegenen offiziellen und professionellen Stationen werden genutzt.':`${stations.length} Stationen im ${lengthKm}-km-Korridor vom Standort zur annähernden Front in Richtung ${Math.round(bearing)}°; ${transition} zeigen bereits Übergangsmerkmale.`;return{bearingDeg:bearing===undefined?undefined:Math.round(bearing),widthDeg,lengthKm,stations,phaseSupport:Number(phaseSupport.toFixed(2)),summary,arrival}}
function synopticTiming(agreement,corridor){if(!agreement.medianArrivalAt)return{observationAdjustmentMinutes:0,radarAdjustmentMinutes:0,confidence:'low',reasons:['Kein modellübergreifendes Fronttiming verfügbar.']};const transitionRatio=number(corridor?.phaseSupport)||0,leadMinutes=(Date.parse(agreement.medianArrivalAt)-Date.now())/60000,observationAdjustmentMinutes=leadMinutes<=18*60?Math.round(clamp((transitionRatio-.3)*-150,-90,60)):0,assimilated=Date.parse(agreement.medianArrivalAt)+observationAdjustmentMinutes*60000,uncertaintyMinutes=Math.max(90,Math.round((agreement.spreadHours||6)*30)),reasons=[observationAdjustmentMinutes?`Der stromaufwärtige Beobachtungskorridor korrigiert die Modellmitte begrenzt um ${observationAdjustmentMinutes} min.`:'Der Beobachtungskorridor erzwingt derzeit keine belastbare Timingkorrektur.'];return{modelMedianAt:agreement.medianArrivalAt,assimilatedAt:new Date(assimilated).toISOString(),windowStart:new Date(assimilated-uncertaintyMinutes*60000).toISOString(),windowEnd:new Date(assimilated+uncertaintyMinutes*60000).toISOString(),observationAdjustmentMinutes,radarAdjustmentMinutes:0,confidence:agreement.confidence,reasons}}
async function synopticAnalysis(request,lat,lon){const [official,settled,dwdVocabulary]=await Promise.all([discoverDwdSurfaceAnalysis(),Promise.allSettled(SYNOPTIC_MODELS.map(model=>fetchSynopticModel(model,lat,lon))),dwdSynopticVocabulary().catch(error=>({available:false,checkedAt:new Date().toISOString(),terms:[],shortRangeTerms:[],mediumRangeTerms:[],sources:DWD_SYNOPTIC_TEXT_PRODUCTS.map(product=>({...product,available:false,error:error instanceof Error?error.message:String(error)}))}))]),candidates=settled.flatMap(result=>result.status==='fulfilled'&&result.value?[result.value]:[]),agreement=synopticAgreement(candidates),corridor=await synopticObservationCorridor(lat,lon,candidates,agreement).catch(error=>({widthDeg:62,lengthKm:420,stations:[],phaseSupport:0,summary:`Beobachtungskorridor derzeit nicht verfügbar: ${error instanceof Error?error.message:String(error)}`})),timing=synopticTiming(agreement,corridor),generatedAt=new Date().toISOString(),sources=[{label:'Deutscher Wetterdienst · amtliche Bodenanalyse',kind:'official-analysis',dataTime:official.nominalTime,checkedAt:official.checkedAt,detail:'Amtliche, unveränderte Referenzkarte mit Fronten, Isobaren, Druckzentren und Stationsmeldungen.'},...dwdVocabulary.sources.filter(source=>source.available).map(source=>({label:source.label,kind:'official-analysis',checkedAt:dwdVocabulary.checkedAt,detail:'Aktueller DWD-Fachtext; MID übernimmt daraus ausschließlich kontrolliertes meteorologisches Vokabular, keine Textpassagen.'})),...candidates.map(candidate=>({label:candidate.modelLabel,kind:'model',dataTime:candidate.runTime||candidate.arrivalAt,checkedAt:generatedAt,detail:'Objektive MID-Frontdiagnostik aus vollständigem Modellfeld.'})),...corridor.stations.slice(0,3).map(station=>({label:station.provider,kind:'observation',dataTime:station.observedAt,checkedAt:generatedAt,detail:`Beobachtung zur annähernden Front: ${station.name}.`}))];return{officialAnalysis:dwdSurfaceAnalysisPublic(official,request),candidates,agreement,corridor,timing,dwdVocabulary,sources,generatedAt,diagnostics:{modelErrors:settled.map((result,index)=>result.status==='rejected'?`${SYNOPTIC_MODELS[index].label}: ${result.reason instanceof Error?result.reason.message:String(result.reason)}`:'').filter(Boolean)}}}

async function runPushSchedule(env){if(!pushConfigured(env))return{ok:false,reason:'not-configured'};const startedAt=new Date().toISOString();await env.MID_PUSH_SUBSCRIPTIONS.put(PUSH_SCHEDULER_HEARTBEAT_KEY,JSON.stringify({startedAt,completedAt:'',subscriptions:0,complete:false,version:WORKER_VERSION})).catch(()=>undefined);const keys=[];let cursor,complete=false;try{do{const listed=await env.MID_PUSH_SUBSCRIPTIONS.list({prefix:'sub:',limit:250,...(cursor?{cursor}:{})});keys.push(...(listed.keys??[]));complete=listed.list_complete!==false;cursor=listed.cursor}while(!complete&&cursor);const weatherCache=new Map(),thunderCache=new Map(),forecastCache=new Map(),netatmoCache=new Map(),ventilationForecastCache=new Map();await pushMapLimited(keys,4,async item=>{const entry=await env.MID_PUSH_SUBSCRIPTIONS.get(item.name,{type:'json'}).catch(()=>null);if(entry)await evaluatePushEntry(item.name,entry,env,weatherCache,thunderCache,forecastCache,netatmoCache,ventilationForecastCache)});const completedAt=new Date().toISOString(),result={ok:true,subscriptions:keys.length,complete,startedAt,completedAt};await env.MID_PUSH_SUBSCRIPTIONS.put(PUSH_SCHEDULER_HEARTBEAT_KEY,JSON.stringify({...result,version:WORKER_VERSION}));return result}catch(error){const completedAt=new Date().toISOString(),message=error instanceof Error?error.message:String(error);await env.MID_PUSH_SUBSCRIPTIONS.put(PUSH_SCHEDULER_HEARTBEAT_KEY,JSON.stringify({ok:false,subscriptions:keys.length,complete:false,startedAt,completedAt,error:message.slice(0,300),version:WORKER_VERSION})).catch(()=>undefined);throw error}}

export {pushThunderState,thunderPushBody};
export {synopticUpstreamBearing};

const AVIATION_HAZARD_LABELS={
 thunderstorm:'Gewitter / CB',icing:'Vereisung',turbulence:'Turbulenz',cat:'CAT','mountain-wave':'Leewellen','freezing-rain':'Gefrierender Regen','volcanic-ash':'Vulkanasche','tropical-cyclone':'Tropischer Wirbelsturm','dust-sand':'Staub / Sand','radiological':'Radioaktive Wolke',llws:'Low-Level Windshear',visibility:'Sicht',ceiling:'Wolkenuntergrenze',wind:'Wind / Böen'
};
function aviationHazardKinds(text=''){
 // IWXXM/WAFS identifiers often use underscores, slashes or URI fragments. Normalize those
 // separators first so the same classifier works for TAC, decoded AWC data and IWXXM.
 const value=String(text).toUpperCase().replace(/[_/.:#-]+/g,' ').replace(/\s+/g,' '),kinds=[];
 const add=kind=>{if(!kinds.includes(kind))kinds.push(kind)};
 if(/\b(?:TS|TSRA|TSGR|CB|CUMULONIMBUS|CONVECT(?:ION|IVE)?|THUNDERSTORM|SQUALL|SQL)\b/.test(value))add('thunderstorm');
 if(/\b(?:FZRA|FREEZING RAIN|FREEZING DRIZZLE|FZDZ)\b/.test(value)){add('freezing-rain');add('icing')}
 if(/\b(?:ICE|ICING|SEV(?:ERE)? ICE|MOD(?:ERATE)? ICE)\b/.test(value))add('icing');
 if(/\b(?:CAT|CLEAR AIR TURB(?:ULENCE)?)\b/.test(value))add('cat');
 if(/\b(?:TURB|TURBULENCE)\b/.test(value)&&!/CLEAR AIR TURB/.test(value))add('turbulence');
 if(/\b(?:MTW|MOUNTAIN WAVE|LEE WAVE)\b/.test(value))add('mountain-wave');
 if(/\b(?:LLWS|LOW LEVEL WIND SHEAR|WIND SHEAR)\b/.test(value))add('llws');
 if(/\b(?:VA|VOLCANIC ASH|VOLCANO|VOLCANIC ERUPTION)\b/.test(value))add('volcanic-ash');
 if(/\b(?:TC|TROPICAL CYCLONE|HURRICANE|TYPHOON)\b/.test(value))add('tropical-cyclone');
 if(/\b(?:DS|SS|DUST STORM|SAND STORM|SANDSTORM)\b/.test(value))add('dust-sand');
 if(/\b(?:RDOACT|RADIOACTIVE|RADIOLOGICAL|NUCLEAR RELEASE|NUCLEAR EMERGENCY)\b/.test(value))add('radiological');
 return kinds;
}
function aviationLevel(text='',source=''){
 const value=String(text).toUpperCase();
 if(/\b(?:SEV|SEVERE|EXTM|EXTREME|FRQ|FREQUENT|HVY|HEAVY|EMBD|SQL|FZRA)\b/.test(value))return'caution';
 if(source==='isigmet'||source==='airsigmet')return'caution';
 return'watch';
}
function aviationFeatureTimes(properties={}){
 const start=safeDate(properties.validTimeFrom??properties.validTimeStart??properties.startTime??properties.validFrom??properties.issueTime??properties.obsTime??properties.reportTime),end=safeDate(properties.validTimeTo??properties.validTimeEnd??properties.endTime??properties.validTo??properties.expireTime);
 return{start:start?Date.parse(start):NaN,end:end?Date.parse(end):NaN};
}
function aviationTimeRelevant(properties,startEpoch,endEpoch,source){
 const times=aviationFeatureTimes(properties),now=Date.now();
 if(Number.isFinite(times.start)||Number.isFinite(times.end)){const from=Number.isFinite(times.start)?times.start:now-3*3600000,to=Number.isFinite(times.end)?times.end:(times.start+6*3600000);return to>=startEpoch-60*60000&&from<=endEpoch+60*60000}
 if(source==='pirep'||source==='metar')return startEpoch<=now+6*3600000&&endEpoch>=now-3*3600000;
 if(source==='taf')return startEpoch<=now+36*3600000;
 return startEpoch<=now+8*3600000;
}
function aviationGeometryDistanceKm(lat,lon,geometry){
 if(!geometry)return Infinity;
 const coords=[];const collect=value=>{if(!Array.isArray(value))return;if(value.length>=2&&typeof value[0]==='number'&&typeof value[1]==='number')coords.push(value);else for(const item of value)collect(item)};collect(geometry.coordinates);
 let best=Infinity;for(const coordinate of coords.slice(0,3000)){const x=number(coordinate[0]),y=number(coordinate[1]);if(x===undefined||y===undefined)continue;best=Math.min(best,distance(lat,lon,y,x)/1000)}return best;
}
function aviationGeometryRelevant(lat,lon,geometry,source){
 if(!geometry)return false;
 if(pointInGeometry(lat,lon,geometry))return true;
 const maxKm=source==='pirep'?220:source==='taf'||source==='metar'?120:source==='cwa'?160:45;
 return aviationGeometryDistanceKm(lat,lon,geometry)<=maxKm;
}
function aviationFeatures(payload){if(Array.isArray(payload))return payload.map(properties=>({type:'Feature',properties,geometry:properties?.geometry||null}));if(Array.isArray(payload?.features))return payload.features;return[]}
function aviationPropertyText(properties={}){return[properties.hazard,properties.hazardType,properties.phenomenon,properties.rawSigmet,properties.rawAirSigmet,properties.rawOb,properties.rawTAF,properties.rawText,properties.text,properties.description,properties.remarks,properties.turbulence,properties.icing].filter(Boolean).join(' ')}
function aviationIssuer(properties={}){return String(properties.icaoId??properties.originator??properties.issuingOffice??properties.mwo??properties.firId??properties.stationId??'').trim()}
function aviationSignalDetail(kind,source,properties,text,distanceKm){
 const issuer=aviationIssuer(properties),levelTop=number(properties.top??properties.altitudeHi??properties.topFt??properties.maxAltitude),levelBottom=number(properties.bottom??properties.altitudeLow??properties.bottomFt??properties.minAltitude),parts=[];
 if(issuer)parts.push(issuer);if(Number.isFinite(distanceKm)&&distanceKm>0)parts.push(`${Math.round(distanceKm)} km entfernt`);if(levelBottom!==undefined||levelTop!==undefined)parts.push(`${levelBottom!==undefined?Math.round(levelBottom):'–'}–${levelTop!==undefined?Math.round(levelTop):'–'} ft`);
 if(source==='isigmet'||source==='airsigmet')parts.unshift('SIGMET');else if(source==='gairmet'||source==='airmet')parts.unshift('AIRMET');else if(source==='pirep')parts.unshift('PIREP/AIREP');else if(source==='taf')parts.unshift('TAF');else if(source==='metar')parts.unshift('METAR');else if(source==='cwa')parts.unshift('CWA');else if(source==='tcf')parts.unshift('TCF');
 if(!parts.length)parts.push(AVIATION_HAZARD_LABELS[kind]||kind);return parts.join(' · ')
}
function aviationEpoch(value){const iso=safeDate(value);return iso?Date.parse(iso):NaN}
function aviationVisibilitySm(value){if(value===null||value===undefined)return undefined;const text=String(value).trim();if(!text)return undefined;if(/^\d+(?:\.\d+)?\+$/.test(text))return Number.parseFloat(text);const n=Number(text);return Number.isFinite(n)?n:undefined}
