import {useEffect,useState} from 'react';
import {BrainCircuit,CloudRain,SlidersHorizontal,Target} from 'lucide-react';
import {readWeatherTwinSettings,writeWeatherTwinSettings,type WeatherTwinSettings} from './forecastVerification';

export function WeatherTwinSettings(){
 const[settings,setSettings]=useState<WeatherTwinSettings>(()=>readWeatherTwinSettings());
 useEffect(()=>{const update=(event:Event)=>setSettings((event as CustomEvent<WeatherTwinSettings>).detail||readWeatherTwinSettings());window.addEventListener('mid:weather-twin-settings',update);return()=>window.removeEventListener('mid:weather-twin-settings',update)},[]);
 const patch=(change:Partial<WeatherTwinSettings>)=>setSettings(writeWeatherTwinSettings(change));
 return <section className="weather-twin-settings"><header><BrainCircuit size={22}/><span><strong>Lokaler Wetterzwilling</strong><small>Unabhängiges Prognosearchiv, lokales Lernen, überprüfbare Gewichtung und persönliche Entscheidungshilfe.</small></span></header><div><label className="settings-toggle-card"><input type="checkbox" checked={settings.enabled} onChange={event=>patch({enabled:event.target.checked})}/><Target size={21}/><span><strong>Wetterzwilling aktivieren</strong><small>Speichert Prognosestände, Rückblicke und lokale Güteprofile für jeden Standort.</small></span></label><label className={`settings-toggle-card${!settings.enabled?' disabled':''}`}><input type="checkbox" disabled={!settings.enabled} checked={settings.useAsMainForecast} onChange={event=>patch({useAsMainForecast:event.target.checked})}/><SlidersHorizontal size={21}/><span><strong>Lokal gelernte Prognose als Hauptprognose</strong><small>Nur bei ausreichender Datenbasis; Best Match bleibt Kontrollgruppe und unverändert im Archiv.</small></span></label><label className={`settings-toggle-card${!settings.enabled?' disabled':''}`}><input type="checkbox" disabled={!settings.enabled} checked={settings.nowcastAssimilation} onChange={event=>patch({nowcastAssimilation:event.target.checked})}/><CloudRain size={21}/><span><strong>Radar-/Nowcast-Assimilation</strong><small>Beobachtete Niederschlagssignale wirken ausschließlich auf die ersten Stunden der lokalen Prognose.</small></span></label></div></section>
}
