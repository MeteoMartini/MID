import {useEffect,useMemo,useState} from 'react';
import {ExternalLink,Info,MapPin,RefreshCw} from 'lucide-react';
import DwdPrecipitationMap,{type DwdCompositePrecipSource,type LayerStatus} from './DwdPrecipitationMap';
import {loadCompositeTimes,type CompositeProductTimes} from './CompositeData';
import {HYMEC_NG_CLASS_LEGEND,loadHymecNgMetadata,sampleHymecNg,type HymecNgMeta} from './HymecNgSource';
import type {Location} from './weather';

const DWD_PRODUCT_PAGE='https://www.dwd.de/DE/leistungen/wolken_niederschlagsart/wolken_niederschlagsart.html';
const COVERAGE={west:5.45,east:15.55,south:47.0,north:55.2};
const MARKER_STORAGE_KEY='mid:dwd-precipitation-marker-visible';

type SourceStatus={source:DwdCompositePrecipSource;status:LayerStatus;message?:string};
type PointInspection={loading:boolean;text?:string;error?:string};

function countryCode(location:Location){return String(location.country_code||location.country||'').trim().toUpperCase()}
export function dwdPrecipitationTypeCoverage(location:Location){const latitude=Number(location.latitude),longitude=Number(location.longitude),code=countryCode(location),within=Number.isFinite(latitude)&&Number.isFinite(longitude)&&latitude>=COVERAGE.south&&latitude<=COVERAGE.north&&longitude>=COVERAGE.west&&longitude<=COVERAGE.east;if(code&&code!=='DE'&&code!=='DEU'&&code!=='GERMANY'&&code!=='DEUTSCHLAND')return false;return within}
function formatSourceTimestamp(value:string|number|undefined){if(value===undefined||value===null||value==='')return'–';const stamp=typeof value==='number'?(value<10_000_000_000?value*1000:value):Date.parse(String(value));if(!Number.isFinite(stamp))return String(value);const date=new Date(stamp),hour=String(date.getUTCHours()).padStart(2,'0'),minute=String(date.getUTCMinutes()).padStart(2,'0');return`${hour}:${minute} UTC`}
function coordinateLabel(latitude:number,longitude:number){return `${Math.abs(latitude).toFixed(5)}° ${latitude>=0?'N':'S'} · ${Math.abs(longitude).toFixed(6)}° ${longitude>=0?'E':'W'}`}
function storedMarkerVisible(){try{return localStorage.getItem(MARKER_STORAGE_KEY)!=='0'}catch{return true}}
function latestIso(values:(string|number)[]|undefined){const times=(values||[]).map(value=>typeof value==='number'?(value<10_000_000_000?value*1000:value):Date.parse(String(value))).filter(Number.isFinite) as number[];return times.length?new Date(Math.max(...times)).toISOString():''}
function freshVerifiedHymec(meta:HymecNgMeta|null,now=Date.now()){if(!meta?.available||!meta.fileUrl||!meta.observedAt||meta.classificationVerified!==true)return null;const stamp=Date.parse(meta.observedAt),age=now-stamp;return Number.isFinite(stamp)&&age>=-10*60000&&age<=35*60000?meta:null}

export function DwdPrecipitationTypeRadar({location,enabled=true}:{location:Location;enabled?:boolean}){
 const covered=dwdPrecipitationTypeCoverage(location),latitude=Number(location.latitude),longitude=Number(location.longitude),[refreshSlot,setRefreshSlot]=useState(()=>Math.floor(Date.now()/300000)),[products,setProducts]=useState<CompositeProductTimes|null>(null),[hymec,setHymec]=useState<HymecNgMeta|null>(null),[loading,setLoading]=useState(false),[error,setError]=useState(''),[markerVisible,setMarkerVisible]=useState(storedMarkerVisible),[pointInfo,setPointInfo]=useState<PointInspection|null>(null),[satelliteStatus,setSatelliteStatus]=useState<{status:LayerStatus;message?:string}>({status:'idle'}),[radarStatus,setRadarStatus]=useState<{status:LayerStatus;message?:string}>({status:'idle'}),[precipStatus,setPrecipStatus]=useState<SourceStatus>({source:'none',status:'idle'}),usableHymec=useMemo(()=>freshVerifiedHymec(hymec),[hymec,refreshSlot]),satelliteProduct=products?.dwdReferenceSatelliteProduct||products?.satelliteDayProduct||products?.satelliteIrProduct||null,satelliteAt=String(satelliteProduct?.latestTime||latestIso(satelliteProduct?.times)||''),dwdRadarAt=latestIso(products?.dwdRadar),phaseAt=usableHymec?.observedAt||'',radarLayer=products?.dwdRadarLayer||'dwd:Niederschlagsradar',radarLatestOnly=products?.dwdRadarLatestOnly===true;
 useEffect(()=>{const timer=window.setInterval(()=>setRefreshSlot(Math.floor(Date.now()/300000)),60000);return()=>window.clearInterval(timer)},[]);
 useEffect(()=>{try{localStorage.setItem(MARKER_STORAGE_KEY,markerVisible?'1':'0')}catch{}},[markerVisible]);
 useEffect(()=>{
  if(!enabled||!covered){setProducts(null);setHymec(null);setLoading(false);setError('');return}
  const controller=new AbortController();let alive=true;setLoading(true);setError('');setPointInfo(null);
  void Promise.allSettled([loadCompositeTimes(latitude,longitude,controller.signal),loadHymecNgMetadata(undefined,controller.signal)]).then(results=>{if(!alive)return;const productResult=results[0],hymecResult=results[1];setProducts(productResult.status==='fulfilled'?productResult.value:null);setHymec(hymecResult.status==='fulfilled'?hymecResult.value:null);const failures=results.filter(result=>result.status==='rejected').map(result=>result.status==='rejected'?(result.reason instanceof Error?result.reason.message:String(result.reason)):'');setError(failures.length===results.length?'Aktuelle DWD-Einzelprodukte konnten nicht geladen werden.':'');setLoading(false)});
  return()=>{alive=false;controller.abort()}
 },[enabled,covered,latitude,longitude,refreshSlot]);
 if(!enabled||!covered)return null;
 const inspectPoint=async(pointLatitude:number,pointLongitude:number)=>{setPointInfo({loading:true});if(usableHymec){try{const sample=await sampleHymecNg(usableHymec,pointLatitude,pointLongitude);setPointInfo({loading:false,text:`${sample.label} · ${pointLatitude.toFixed(4)}° N / ${pointLongitude.toFixed(4)}° E`});return}catch(nextError){setPointInfo({loading:false,error:nextError instanceof Error?nextError.message:'HymecNG-Punktanalyse nicht verfügbar.'});return}}const limited='Aktuell keine verifizierte DWD-Niederschlagsart; das DWD-Radar zeigt unabhängig davon die aktuelle Niederschlagsintensität. Ersatzklassen werden nicht geraten.';setPointInfo({loading:false,text:limited})};
 const phaseLabel=usableHymec?'DWD HymecNG · verifizierte Klassen':hymec?.error?`DWD HymecNG nicht aktuell · ${hymec.error}`:'DWD HymecNG derzeit nicht verifiziert',satLabel=satelliteProduct?.label||'Satellitenlayer nicht verfügbar',radarLabel=radarStatus.message||`DWD ${radarLayer}`;
 return <section className="dwd-precip-type-radar" aria-label="Wolken und Niederschlagsart"><header><span><small>DWD / EUMETSAT · Satellit + Radar</small><strong>Wolken + Niederschlagsart</strong><em>MID-Komposit · georeferenziert · {location.name||'gewählter Ort'}</em></span><div className="dwd-precip-type-radar__actions"><button type="button" className={`dwd-precip-type-radar__marker-toggle${markerVisible?' active':''}`} onClick={()=>setMarkerVisible(value=>!value)} title={markerVisible?'Standortmarker ausblenden':'Standortmarker anzeigen'} aria-pressed={markerVisible}><MapPin size={16}/></button><details className="dwd-precip-type-radar__info"><summary aria-label="Legende und Quellen anzeigen" title="Legende"><Info size={16}/></summary><div className="dwd-precip-type-radar__legend" role="group" aria-label="Legende Niederschlagsart"><strong>{usableHymec?'DWD HymecNG · Niederschlagsart':'Aktive Niederschlagsart'}</strong>{usableHymec?HYMEC_NG_CLASS_LEGEND.map(item=><span key={item.label}><i style={{background:item.color}}/>{item.label}</span>):<><small>{phaseLabel}</small><small>DWD-Radarintensität wird unabhängig davon immer separat dargestellt. Die 11 HymecNG-Klassen erscheinen nur bei einem frischen, verifizierten DWD-Datensatz. Ein fremder Regen-/Schnee-Layer wird nicht als DWD-Niederschlagsart ausgegeben.</small></>}</div></details><a href={DWD_PRODUCT_PAGE} target="_blank" rel="noreferrer" title="DWD-Referenzprodukt öffnen"><ExternalLink size={16}/><span>DWD</span></a></div></header>
 <div className="dwd-precip-type-radar__timestamps"><span><b>Radar</b>{formatSourceTimestamp(dwdRadarAt)}</span><span><b>Art</b>{formatSourceTimestamp(phaseAt)}</span><span><b>Sat</b>{formatSourceTimestamp(satelliteAt)}</span>{pointInfo?<span className="dwd-precip-type-radar__point-strip"><b>Bildpunkt</b>{pointInfo.loading?'wird ausgewertet …':pointInfo.error||pointInfo.text||'–'}</span>:<span className="dwd-precip-type-radar__point-strip idle"><b>Bildpunkt</b>Antippen zur Auswertung</span>}<small>{phaseLabel}</small></div>
 <div className={`dwd-precip-type-radar__map-shell${loading?' loading':''}${error?' failed':''}`}>
  <DwdPrecipitationMap latitude={latitude} longitude={longitude} locationName={location.name} satelliteAt={satelliteAt} satelliteProduct={satelliteProduct} hymecMeta={usableHymec} radarLayer={radarLayer} radarTime={dwdRadarAt} radarLatestOnly={radarLatestOnly} markerVisible={markerVisible} onPoint={inspectPoint} onHymecStatus={()=>{}} onSatelliteStatus={(status,message)=>setSatelliteStatus({status,message})} onRadarStatus={(status,message)=>setRadarStatus({status,message})} onPrecipitationStatus={(source,status,message)=>setPrecipStatus({source,status,message})}/>
  {loading?<span className="dwd-precip-type-radar__status"><RefreshCw className="spin" size={18}/>Aktuelle Einzelprodukte werden geladen …</span>:null}
  {!loading&&error?<span className="dwd-precip-type-radar__status">{error}</span>:null}
 </div>
 <div className="dwd-precip-type-radar__source-summary"><span><b>Wolken</b>{satelliteStatus.message||satLabel}</span><span><b>Radar</b>{radarLabel}</span><span><b>Art</b>{precipStatus.message||phaseLabel}</span></div>
 <footer><span>Eine georeferenzierte Gesamtkarte mit amtlicher basemap.de-Farbkarte + Relief, DWD-nahem Satellitenbild, separatem DWD-Niederschlagsradar und ausschließlich verifizierter DWD-Niederschlagsart.</span><small>Standort: {coordinateLabel(latitude,longitude)} · Kartendarstellung © GeoBasis-DE / BKG · CC BY 4.0</small></footer></section>;
}
